# Capability Status Map

What actually works vs. what is a stub. **Check this before touching a subsystem** — the
biggest trap in this codebase is features whose **UI is fully built but backend is missing**.
Treat each row as the current contract; verify against code, and update this file when you
ship a change that moves a row.

## ✅ Complete (backend + UI, production-ready)

| Capability | Notes / key classes |
|------------|---------------------|
| Dynamic schema engine | Full field-type set (primitives, date/time, JSON/array, picklists, currency, percent, auto-number, phone/email/URL, rich text, encrypted, external ID, geolocation, relationship — see `FieldType` enum); LOOKUP (FK SET NULL) + MASTER_DETAIL (FK CASCADE); FORMULA + ROLLUP_SUMMARY computed fields; auto-number sequences; schema snapshots |
| Auto-generated JSON:API | Full CRUD per collection, includes (multi-level + Redis cache), sparse fieldsets, filter/sort/paginate, sub-resources, atomic ops `POST /{tenant}/_operations`, dynamic route registration via NATS |
| Global full-text search | Postgres `tsvector`, `GET /api/_search?q=`, per-field `searchable`, RLS-isolated, NATS-driven index maintenance |
| Auth / identity | Internal OIDC provider (`kelta-auth`, Spring Authorization Server); federated brokering (`FederatedUserMapper`); dynamic JWKS by issuer (Redis 15-min TTL); JDBC sessions; password reset/forced-change |
| MFA | TOTP (RFC 6238) + recovery codes + SMS OTP; encrypted secrets; rate-limited; `MfaController` `/api/auth/mfa/*`; `SmsProvider` SPI |
| Password policies | Per-tenant length/complexity + account lockout |
| Personal Access Tokens | `klt_` + 40 chars, SHA-256 hashed; `/api/me/tokens` (max 10); Redis revocation (`pat:revoked:`). **Admin-on-behalf-of minting (K-8 item 1)**: `POST /api/admin/users/{id}/tokens` (`MANAGE_USERS`, `AdminPersonalAccessTokenController`) mints for the target user, audited as `PAT_ADMIN_CREATED` (distinct from self-mint `PAT_CREATED`); Setup › Users mint action + `kelta users token-create` |
| RBAC | Profiles + groups; most-permissive-wins; per-profile ABAC CEL rules; 7 built-in profiles. (Permission sets were removed — V98 dropped the tables, and the residual system-collection/delegated-admin/package wiring was removed in the DB-design cleanup; authorization is profiles + Cerbos only) |
| Cerbos authz | Per-tenant policy generation/sync; gateway `RouteAuthorizationFilter` (route) + worker `CerbosRecordAuthorizationAdvice` (record); fail-closed circuit breaker (3 fails → open 10s) |
| Tenant IP allowlist (network access) | Per-tenant `ip_allowlist_enabled` + `ip_allowlist_cidrs` on `tenant` (V148, fields on `tenants` system collection). Gateway `TenantIpAllowlistFilter` (order -40) blocks non-admin `/api/**` when no chain IP (socket + every `X-Forwarded-For`/`X-Real-IP` hop, `trust-forwarded-for` toggle) matches an allowed CIDR; **`MANAGE_TENANTS` bypass** + **fail-open** (missing/disabled config) so a bad range can't lock admins out. Config cached in `GatewayCacheManager` (bootstrap load + `kelta.config.tenant.ip-allowlist.changed.*` NATS refresh via `IpAllowlistCacheInvalidationListener`); CIDRs validated server-side in `TenantIpAllowlistConfigEventPublisher`. Admin UI: `NetworkAccessPage` (`/network-access`, `MANAGE_TENANTS`, in SetupHome › Security) |
| **Field-Level Security (read + write)** | Write: `CerbosFieldWriteSecurityAdvice` blocks HIDDEN on create/update. **Read: `CerbosFieldSecurityAdvice` strips denied `attributes` + to-one `relationships`** (shipped #1038/627d4ca9). System audit fields never stripped |
| **Field history (value change tracking)** | ✅ End-to-end. Per-field `trackHistory` toggle (Fields tab) is read at runtime (`FieldDefinition.trackHistory`, hydrated from `field.track_history`). **Writer**: `FieldHistoryHook` (wildcard `BeforeSaveHook`, order 900) records create/update/delete diffs of tracked fields into `field_history` (`FieldHistoryRepository`); user collections only. **Read**: `field-history` read-only system collection + gateway route `/api/field-history`; `FieldHistorySecurityAdvice` drops FLS-denied rows and redacts MASKED old/new values by the *referenced* collection's field security. **End-user surface**: folded into the record `ActivityTimeline` (`/app/o`). Previously a UI-only stub — the write path never existed and the read route 404'd. Admin designer's separate Field History tab removed; **Setup Audit + Version History merged into one "Change History" tab** (both `collection-versions` + `field-history` gateway routes added). **Superseded per collection by collection-level record versioning** (row below): when `collection.track_history` is on, `FieldHistoryHook` skips the collection entirely |
| **Record versioning (collection-level history)** | ✅ End-to-end. Collection-level `trackHistory` toggle (`collection.track_history`, V174; collection edit form checkbox; propagates via the existing collection-changed NATS broadcast) ⇒ **all fields tracked**: `RecordVersionHook` (wildcard `BeforeSaveHook`, order 910) writes a full-record snapshot per create/update/delete into `record_version` (`RecordVersionRepository`, atomic `MAX(version_number)+1` numbering + unique-constraint retry; no-op updates skipped; DELETED keeps last snapshot, rows retained after delete). **Read**: `record-versions` read-only system collection + gateway route `/api/record-versions`; `RecordVersionSecurityAdvice` (`@Order(21)`) strips unreadable/unknown snapshot keys **and their names from `changedFields`**, masks MASKED values, fail-closed. **UI**: History tab in the shared `DetailTabBar` (end-user `/app/o` + admin `/resources`), version list → version detail rendered with the *current* layout read-only + amber "Changed" badges (`RecordHistoryTab`/`RecordVersionDetail`/`highlightedFields`); `ActivityTimeline` shows one entry per version with author + click-through (`?tab=__history__&version=N`, suppresses its client-synthesized created/updated entries when versions exist). Known limits: DELETED version's author = last updater; historical lookup ids not in the live page's display map render raw |
| Data security | AES-256-GCM field encryption (per-tenant key derivation); Postgres RLS on all system tables; per-tenant schema isolation; Postgres audit trails (`setup_audit_trail`, `security_audit_log`) |
| Multi-tenancy & governors | URL slug resolution + custom domains (`CustomDomainFilter`); per-tenant governor limits (API/day, storage, users, collections, fields, workflows, reports) with live usage |
| Visual Flow Builder | 8 state types, 16 action handlers, JSONPath data flow, durable execution (survives restart), retry/catch, versioning, React Flow designer. **Triggers working: record / API-webhook / scheduled-cron / NATS-message** (`NATS_TRIGGERED`, V153 — external systems publish to `kelta.trigger.<tenantId>.<topic>` [KELTA_TRIGGERS stream]; `NatsTriggerFlowListener` queue-group consumes, matches trigger-config `topic`, message body becomes `$.input`; builder offers "NATS Message" + topic form, replacing the never-implemented KAFKA_TRIGGERED). **Wait-state resume works**: a parked Wait (seconds>10 / timestamp / event) stays WAITING (previously it was silently overwritten COMPLETED) and records a `flow_pending_resume` row; `FlowResumePollerConfig` claims due rows (`SELECT FOR UPDATE SKIP LOCKED`, one pod resumes) → `FlowEngine.resumeExecution` re-loads the definition and continues from the Wait's `Next` (event-based waits resume via `claimPendingResumeByEvent` when an event source is wired — publisher hook still TODO; waits inside Parallel/Map branches are not resumable and fail with a clear error). Adding a handler also needs UI wiring (hardcoded `RESOURCE_GROUPS` + `*Params.tsx`; the `ActionHandlerDescriptor` SPI is **not** consumed by the builder) — see `playbooks.md`. **Audit actor ships (2026-07-18)**: every start resolves an actor (initiating user [emails translated] -> `flow.runAsUserId` [V175; designer trigger-sheet "Run as user"; MCP flow tools] -> flow owner) via worker `FlowActorResolver`; `CreateRecord`/`UpdateRecord` handlers stamp `createdBy`/`updatedBy` (`FlowAuditStamp`, UUID-only), `started_by` records the actor (webhook literal `"webhook"` replaced), cron/NATS/webhook runs now carry `ActionContext.userId`. See `integrations.md` -> Execution actor |
| MCP server (admin + user toolsets) | Built. kelta-admin (schema/field/flow/layout/picklist/validation-rule tools) + kelta-user (records/flows/approvals/search). **Many tools already exist — check `kelta-mcp/.../tool/{admin,user}/` before adding one** (e.g. `create_validation_rule` and the `validation-rules` system collection are already implemented). |
| Rate limiting | Per-tenant fixed window (Redis, TTL set only on a new window) + daily governor quota, plus per-IP limiting on unauthenticated paths (`IpRateLimitFilter`, in-memory per pod); fail-open if Redis down. **Config-driven since consumer-alerting slice 1**: `kelta.gateway.rate-limit.ip-paths` is a `<path-prefix>=<per-minute>` list matched by longest prefix with a separate bucket per prefix (it was a hardcoded exact-match set covering only `/actuator/health`), and `kelta.gateway.rate-limit.exempt-cidrs` (`RATE_LIMIT_EXEMPT_CIDRS`) exempts trusted IP/CIDR ranges from **both** limiters. CIDR matching is shared with the per-tenant IP allowlist (`io.kelta.gateway.net.CidrBlock`, address-literals only — no DNS). See `architecture.md` → Rate limiting |
| Flow/job log retention | `FlowLogRetentionSweep` (@Scheduled hourly) prunes terminal `flow_execution` rows older than `kelta.flow.retention.max-age-days` (default 60) — cascading to `flow_step_log` + `flow_pending_resume` — plus `job_execution_log` by the same age. Nothing pruned these before (`JdbcFlowStore`'s only DELETE is `deletePendingResume`), so they grew without bound. **DRY-RUN by default** (`kelta.flow.retention.dry-run:true`): it logs what it WOULD delete until an operator arms it, matching the telehealth purge posture. Only `COMPLETED|FAILED|CANCELLED` are eligible — a `WAITING` flow parked for months is never collected; age is `COALESCE(completed_at, started_at)` so a terminal row with a NULL `completed_at` still ages out; `FOR UPDATE SKIP LOCKED` + a per-cycle `batch-size × max-batches` cap keep multi-pod deletes disjoint and bounded. Metric `kelta_worker_flowlog_purged`. Harness `FlowLogRetentionScenarioTest` proves the cascade fires, WAITING/RUNNING survive, and the COALESCE path ages out on real Postgres. See `concerns.md` |
| Observability | OTel → Alloy → Tempo tracing in the cluster (Jaeger → OpenSearch is local dev only); the admin request-log detail page deep-links a trace into Grafana Explore against the Tempo datasource; spans tagged with `kelta.tenant.id`/`user.id`/`correlation.id` (`SpanBodyEnrichmentFilter`); metrics dashboard; audit; Prometheus `kelta_worker_*` metrics; 7 admin monitoring pages; per-tenant retention; **flow-run observability** (`FlowExecutionController` — list executions + status/duration/error + step detail). **Per-tenant OTLP export (Rec 7)**: `PerTenantOtlpSpanProcessor` (additive — never disturbs the default pipeline) forwards each span to its tenant's own OTLP collector based on `kelta.tenant.id`, via `TenantOtlpRegistry` + a per-tenant batching `OtlpHttpSpanExporter` cached by tenant; targets from a **self-service** source — `DbTenantOtlpRegistry` (`@Primary`) reads `tenant_otlp_target` (V145, RLS; Caffeine 60s TTL) and falls back to operator config (`kelta.observability.tenant-otlp.targets.<tenantId>.*`); a tenant sets its own collector via `GET/PUT/DELETE /api/admin/observability/otlp-target` (`VIEW_SETUP`-gated), which upserts + invalidates the cache. Exporter cache is keyed by the tenant's current `OtlpTarget` — re-pointing an endpoint (or changing headers) swaps the exporter on the next span and shuts down the stale one, no restart needed . **Span export itself requires `management.opentelemetry.tracing.export.otlp.endpoint` to be declared in each service's `application.yml`** — a native image freezes `@ConditionalOnProperty` at build time, so an env-var-only endpoint yields no exporter bean and no spans at all (fixed 2026-08-10; `concerns.md` → Dependency Risks) |
| Integration | HTTP callout, outbound message, NATS publish, inbound webhooks (`POST /api/webhooks/{flowId}`), Svix outbound, SMTP email, `PushProvider` SPI, Superset analytics, merge-field templating. **Reply headers ship (2026-08-31, support-mailbox slice 0)**: `EmailHeaders` (Reply-To / In-Reply-To / References / Auto-Submitted / Precedence / List-Unsubscribe / arbitrary extras) is a sixth component on `EmailMessage`, applied by `SmtpEmailProvider`; `DefaultEmailService.queueReply` is the reply-shaped entry point. Values are validated for CR/LF/NUL **in the `EmailHeaders` constructor, before trimming** — `In-Reply-To`/`References` on a reply are copied from the inbound message and are therefore attacker-controlled, and `String.trim()` strips control characters, so trimming first would silently accept a trailing-CRLF injection. `EmailProvider` gains a defaulted `sendAndReport` returning `SendResult`: `Message-ID` cannot be assigned (`saveChanges()` regenerates it inside `Transport.send`), only read back afterwards, and without it a recipient's reply cannot be matched to its conversation. Existing 4-/5-arg `EmailMessage` constructors are kept explicitly so every prior caller compiles unchanged. **Suppression is deliberately NOT checked in `queueEmail`** — only `CampaignRunnerService` consults `email_suppression` today, and folding it in would silently start blocking password resets and invites; the mailbox reply path checks it explicitly. |
| Embedded analytics (Superset) | Guest tokens, dashboard discovery, dataset sync (NATS-triggered), per-tenant isolation, `SupersetEmbed` |
| WebSocket realtime | `/ws/realtime` (JWT on upgrade), `RealtimeBridge` ← `kelta.record.changed`, per-session subscription limits, tenant-scoped. **FE consumption ships (2026-07-08, app-surfacing slice 4)**: `src/realtime/` — `RealtimeClient` (fresh-token reconnect w/ backoff + resubscribe) + `RealtimeProvider` in `EndUserShell` (nav collections + approval collections, 250ms debounce). **Gateway-origin fix (2026-07-08)**: `buildSocketUrl` targets `VITE_API_BASE_URL` (page-origin fallback for local dev) — it used `window.location.host`, which in production is the static-asset host (`app.kelta.io`) that cannot upgrade WebSockets, so the socket never connected. **INVALIDATION-ONLY**: pushed `data` never enters caches (no per-subscriber FLS server-side) — events invalidate React Query keys and refetch through the authorized JSON:API. **Presence ships (2026-07-08, app-intelligence slice 3, gateway+FE — PHASE 4 COMPLETE)**: socket gains `presence.join`/`presence.leave` (resource key `record:<collection>/<id>`; ≤10 resources/session) and pushes `presence.changed` snapshots (≤20 users) to co-present sessions; gateway `PresenceService` holds the fleet-wide view via NATS deltas on NEW `kelta.presence.<tenantId>` (broadcast-consumed; new KELTA_PRESENCE JetStream stream, 1-min retention; 30s heartbeats + 90s expiry cover pod/session death); the gateway now PUBLISHES to NATS for the first time (plain-Map payloads end-to-end ⇒ no new native reflect-config); identity = JWT subject + email claim. FE: `RealtimeClient.joinPresence/leavePresence` (re-join on reconnect), `useRealtimeClient` context, `usePresence(resource)`, `PresenceAvatars` (others-only, self filtered by id OR email) on the `ObjectDetailPage` header. Builder-editor presence deferred (admin shell has no realtime client). Found en route: `gateway-realtime` record subscription was a QUEUE GROUP — multi-pod gateways starved realtime subscribers (fixed: all gateway subscriptions are broadcast now, see the multi-pod delivery fix below); presence deliberately uses broadcast. **Saved views in the app ship too (slice 5)**: `ObjectListPage` gains `ViewSelector` — personal localStorage views + read-only shared PUBLIC `list-views` rows; default view auto-applies on a clean URL; a view's `visibleColumns` finally drives the column set (first-6 rule = fallback). **Server-persisted (2026-07-08, app-data-entry slice 1)**: `user-ui-preferences` system collection (V163, owner-guarded by `UserPreferenceGuardHook` — see architecture.md) + `usePreferenceValue` store; `useSavedViews` is server-backed with localStorage warm-cache/offline fallback + one-time migration — views follow the user across browsers. `UserPreferenceScenarioTest` real-DB guard. **List power pack ships (2026-07-08, app-data-entry slice 2, FE-only)**: multi-sort (shift-click levels, URL `sort=a,-b` — the server comma grammar was ALREADY fully supported, no backend change), `ColumnChooser` (visibility+order, ≥1 column), density toggle, sticky first column (opt-in `ObjectDataTable` props; admin grid untouched), SavedView v2 (`sorts`/`viewType`/`density`/`groupBy`/`typeConfig`) + `view=<id>` deep links. **Grouping + mass edit ship (2026-07-08, app-data-entry slices 3+4, FE-only)**: opt-in `groupBy` on `ObjectDataTable` — client-side grouping over the fetched page (collapsible group headers, per-group count + numeric sums for `number`/`currency`/`percent`, explicit "this page only" caption, group field silently prepended to the server sort for contiguous buckets, virtualization off while grouped); toolbar Group-by picker; `groupBy` captured in saved views. Mass edit on the main list selection bar ("Edit field") reuses `MassEditDialog` + the bulk-jobs flow now extracted to `utils/bulkUpdate.ts` (RelatedList refactored onto it) — gated on `canEdit` + `MANAGE_DATA`, result toasts link to `/bulk-jobs`. **Alt views ship (2026-07-08, app-data-entry slices 5+6+7, FE-only)**: `ObjectListPage` gains a view-type switcher (table/kanban/calendar/gallery) with per-type config pickers, all captured in SavedView v2 `viewType`/`typeConfig`. Kanban (`components/KanbanBoard/`, own @dnd-kit tree): lanes from the lane field's picklist values (+data-only values + unassigned), per-lane counts, drag → fresh-ETag `PATCH` with `If-Match` (reject ⇒ optimistic revert + toast + refetch), `canEdit`-gated. Calendar (`components/CalendarMonthView/`, no new dep): month grid on a date/datetime field, visible-month `gte`/`lte` conditions merged into the standard query (pageSize 200 clamp, pagination hidden), `+N more` overflow, month pager. Gallery (`components/GalleryGrid/`): responsive card grid, `url`-field image with initial-letter placeholder fallback, ≤4 FieldRenderer body fields. `usePicklistOptions` promoted from `PageBuilderPage/widgets/builtins/inputs/` to `src/hooks/` (5 import sites updated). **Phase 2 slices 1-7 COMPLETE** **Shared views publish their renderer too (2026-09-15, KLT-196)**: `list-views` gains `view_type` (V196, NOT NULL DEFAULT `TABLE`, CHECK `TABLE|KANBAN|CALENDAR|GALLERY`) + `type_config` JSONB, so an admin can hand out the board instead of every user flipping the toolbar switch and re-picking the lane field. `mapSharedListView`/`parseTypeConfig` (`ObjectListPage/listViewMapping.ts`) fall back to the table renderer on an unrecognized `viewType` or a half-formed config; a user's toolbar switch on a shared view persists as a per-user `user-ui-preferences` override (`useViewTypeOverrides`, `prefType: 'list-view-type'`) that never writes the shared row. Authored from Setup › List Views (lane select scoped to picklist fields), `kelta list-views create|update --view-type/--lane-field/--card-fields`, and the MCP `create_listview`/`update_listview` `viewType`/`typeConfig` params. **Multi-pod delivery fix (2026-07-08)**: all four gateway NATS subscriptions (`gateway-realtime`, `gateway-record-routes`, `gateway-config`, `gateway-config-assignment`) were QUEUE GROUPS — each event reached exactly one pod, so with 2+ gateway replicas the other pods' WebSocket subscribers got no `record.changed` push and their route/cache state went stale. All converted to broadcast (`NatsSubscriptionConfigTest` locks the invariant) |
| Tenant i18n authoring | **Shipped 2026-07-08 (app-intelligence slice 4)**: `ui-translations` tenant-scoped system collection (V165, `UNIQUE (tenant_id, locale, translation_key)`, RLS) served by the dynamic route; writes broadcast `kelta.config.translation.changed.<tenantId>` (`TranslationConfigEventPublisher` → `TranslationCacheInvalidationListener` evicts `SystemCollectionCache` fleet-wide, Rule 1); bootstrap fetches the overlay (`BootstrapConfig.translations`, failure-tolerant, 2000-row v1 ceiling) and `TenantTranslationsBridge` pushes it into `I18nContext` — `t()` resolves tenant overlay > locale bundle > en bundle > inline fallback, same `{{param}}` interpolation; admin `TranslationsPage` (`/translations`, Setup, `CUSTOMIZE_APPLICATION`) does locale-filtered CRUD; `UiTranslationScenarioTest` guards the unique constraint on the real DB. Metadata-label keyspace (`meta.collection.<name>.displayName`) is convention-only until a consumer wires it | missing-key report; per-locale lazy fetch beyond 2000 rows; metadata-label consumers |
| Image transforms | `GET /api/images/{path}?w=&h=&fit=&format=&quality=`; resize/crop/convert; bomb protection |
| Direct file serving | `GET /api/files/{path}` with range support, tenant-scoped |
| Notes & attachments | Full S3 lifecycle: presigned-PUT upload + finalize, list/download (`AttachmentUrlEnricher`), delete with S3 cleanup, `AttachmentCleanupHook` cascade on parent delete (idx `idx_attachment_tenant_record`, V142) |
| Metadata dependency & impact | `MetadataDependencyService` per-tenant graph; `GET /api/metadata/impact?type=&id=&direction=` and `/api/metadata/graph`; Tarjan SCC cycle detection (#1039). **Deployment plans (Rec 5)**: `POST /api/metadata/deployment-plan` `{changeSet:[{type,id}], includeDependencies}` → dependency-ordered apply sequence (Kahn topo-sort, deps first), with `missing`/`cyclic` items flagged |
| Config-health governance (Rec 6) | `ConfigHealthAnalyzer` scans tenant config via pluggable `HealthRule` beans (circular master-detail, collections without layouts/fields, orphan fields, flows without error handling, over-permissive profiles) → 0–100 score + findings; `GET /api/config-health`. **Dashboard UI**: `ConfigHealthPage` at `monitoring/health` (Monitoring hub tab, `VIEW_SETUP`-gated) — score gauge + per-severity counts + findings list sorted error→warning→info, with re-scan; 4 Vitest. AiProposal one-click remediation still pending; positive Playwright e2e post-deploy. (2026-07-06: the over-permissive-profile rule was querying the V47-dropped `system_permission` table and would fail the scan — fixed to read `profile_system_permission`) |
| Developer experience | `@kelta/sdk` (`KeltaClient`, `QueryBuilder`) + **OpenAPI→TS type generator** (`@kelta/sdk/cli`: `openapi-parser` + `type-generator`, `kelta-generate-types` bin / `generateTypesFromSpec`); `@kelta/cli` — auth/collections/records + **`metadata export`/`diff`/`apply`** (Rec 10: GitOps for tenant config over the Package API — `POST /api/packages/{export,import/preview,import}`; `apply --dry-run`) + **`sdk types`** (Rec 10: authenticated, tenant-aware codegen — fetches `GET /api/docs/openapi.json` and delegates to the `@kelta/sdk` generator, so no OpenAPI URL/token needs passing once logged in); OpenAPI 3.0 + Swagger at `/api/docs`, Zod schemas, i18n (6 langs, RTL), theme, a11y, 70+ UI pages |
| Operational | Graceful shutdown (persist in-flight flows), optimistic locking for scheduled tasks, multi-layer cache (Caffeine→Redis→worker) w/ NATS invalidation, security headers, health endpoints |
| Configuration packages | `PackageController` (`/api/packages/{export,import/preview,import,history}`) + `PackageService` — real export/import/dry-run. `packages`/`package-items` system collections. (Verified 2026-07-02; the prior 🔴 "no PackageController" was stale.) |
| Schema migration planner | End-to-end. `MigrationController` (`/api/migrations/{snapshot,plan,versions,execute,{runId}/rollback}`, gated in-controller on `CUSTOMIZE_APPLICATION`) + gateway static route `/api/migrations/**`. **Snapshots** capture the full `FieldDefinition` (columnName/defaults/enumValues/referenceConfig/validation/fieldTypeConfig — `CollectionVersionService.FieldSnapshot`; a lenient reader tolerates legacy `{name,type,nullable}` rows) into `collection_version`. **Plan** is read-only (`buildPlan` diff → ADD/REMOVE/MODIFY + risk + record count, no DDL). **Execute** (`MigrationExecutionService`) is the destructive path: auto-snapshots the current schema as a restore point, syncs the `field` metadata to the target, applies real `ALTER TABLE`/`DROP COLUMN` via `SchemaMigrationEngine.migrateSchemaDestructive` (every TYPE_CHANGED gated by `validateTypeChange`/`isTypeChangeCompatible` — incompatible rejected unless `force`), re-registers the local `CollectionRegistry` + broadcasts `kelta.config.collection.changed`, and records a `migration_run` + `migration_step` trail (`dryRun` returns the plan, applies nothing; `rollback` re-migrates to a run's restore-point `from_version`). `MigrationsPage` execute + rollback wired (runs read back via the generic `/api/migration-runs` route; status/steps normalized client-side). Real-DB guard: `SchemaMigrationScenarioTest` (create → snapshot → add field → rollback drops the column + data behaves) — Mockito worker tests can't cover destructive DDL. **Security-sensitive → not auto-merged.** Follow-up: positive Playwright e2e post-deploy |
| SCIM 2.0 provisioning | Full `scim/` package: `ScimUser/Group/DiscoveryController` + services + `ScimFilterParser` + `ScimAuthenticationFilter`; gateway route `/scim/v2/**`; 6 test classes. (Verified 2026-07-02; the prior ⚪ "not started" was stale.) |
| Data export | `DataExportController` (`/api/data-exports`, CSV/JSON, presigned-download) + `DataExportService`; gateway static route added + gated on `VIEW_ALL_DATA` (2026-07-02). Scheduled `DATA_EXPORT`/`REPORT_EXPORT`/`FLOW` jobs run via `ScheduledJobExecutorService` |
| Scheduled jobs | `ScheduledJobExecutorService` dispatches all four job types — `FLOW`, `SCRIPT` (via `ScriptExecutor`), `REPORT_EXPORT` (CSV via `ReportExecutionService`), `DATA_EXPORT` (via `DataExportService`) — with conditional-claim polling. (Verified 2026-07-04; the prior 🟡 "SCRIPT/REPORT_EXPORT dispatcher not built" was stale.) |
| Standalone script execution | `ScriptExecutionController` `POST /api/scripts/{id}/execute` runs an active, HTTP-invocable (`API_ENDPOINT`/`EVENT_HANDLER`) script through the sandboxed `GraalVmScriptExecutor` with `input`+`context` bindings; logs to `script_execution_log`; end-user Quick Actions `useScriptExecution` calls it (script `output.action` drives refresh/redirect/toast/open_record). **Per-script authz ships** (V156 `script.required_permission`): a script may declare a system permission — execution then 403s unless the caller's profile holds it (fail-closed on missing identity; unset = any API user, back-compat); `ScriptsPage` form has a permission select (reuses the shared `SYSTEM_PERMISSIONS` catalog) |
| Duplicate detection + merge | **Detect**: `POST /api/collections/{name}/duplicates` `{matchFields,limit}` → groups of records sharing the same match-field values (`DuplicateDetectionService`, read-only, MANAGE_DATA-gated). Bounded scan (5000 rows via the authorized query path — no raw SQL, schema/RLS-safe); reports `truncated`. **Data-masking guard (2026-07-05)**: the returned `groups[].values` are the match-field values, and a grouping key can't be masked without collapsing distinct values, so a match on a field masked for the requester is **rejected 400** (`CerbosPermissionResolver` → `MaskingPrincipal` in the controller; `RecordMaskingService.maskableConfigs`/`maskedFieldsFor` in the service) — mirrors the report/dashboard group-by guard, closing a plaintext + record-grouping leak to a `MANAGE_DATA` caller without `unmask`. null/system principal not masked. **Merge (write path)**: `POST /api/collections/{name}/merge` `{masterId, duplicateIds[], fieldOverrides?}` (`RecordMergeService` + `RecordMergeController`, MANAGE_DATA-gated, `@Transactional`, `security.audit` logged) — keeps the master (applies optional overrides via the normal `update`), re-parents every inbound LOOKUP/MASTER_DETAIL FK (discovered via `CollectionRegistry`) from each duplicate onto the master **before** deleting the duplicates (so cascade children aren't lost), all through the authorized `QueryEngine` (validation/hooks/events fire). Deliberately not auto-merged (bulk delete + re-parent). 5 service unit tests + `RecordMergeScenarioTest` (real-Postgres FK re-parent + delete). UI: `DeduplicationPage` (admin, `/:tenant/dedup`) detects a group, picks a master, merges | — |
| Support mailbox — auto-reply + admin UI (slice 7) | Parent spec `specs/support-mailbox/README.md`. **Slice 7 ships (V194)**: `mailbox_auto_reply_decision` — **one row per inbound message whether or not anything was sent**, which is the whole point of shadow mode; a boolean on the message would say "we did not auto-reply" without saying whether we *would* have. `SupportAutoReplyPolicy` is 15 **vetoes, any one decisive** — a scoring model that could be outvoted would eventually let something through on aggregate confidence. Includes: mailbox kill switch, template not eligible, low confidence, **ambiguous match**, blocked category (money/access/legal, denylist so a new category is not auto-sendable until someone says so, and a malformed list falls back to the defaults rather than to "nothing blocked"), `dmarc != pass`, unverified sender for account-specific copy, **first contact only**, attachments, body >4k chars, bulk/bounce/auto-submitted, per-thread and per-day budgets. `SupportAutoReplySweep` runs as a sweep, not inline in the webhook — an SMTP round-trip inside a request SNS retries on timeout would turn one email into several replies. **`shadow-mode=true` is the default**; live logs a WARN at startup. The send path takes only a template key, so "auto-send is template-only" is a property of the code rather than a convention. **Admin UI ships too** (`MailboxAdminPage` at `/:tenantSlug/mailboxes`): ingest/webhook + secret rotation (shown once), SLA policy (with the frozen-at-creation semantics stated, or an admin lowering it thinks it did not save), automation with a confirm dialog and the shadow report **beside the switch**, membership, and escalation contacts — which had **no API at all** before this slice, so a level with nobody on it is called out in amber rather than left blank. Reports `followUpRate` next to the SLA numbers: auto-replying "thanks for writing!" to everything scores perfect first-response compliance while helping nobody. Tests: `SupportAutoReplyPolicyTest` (14). Found en route: the mailbox UI files from slices 4-5 had reached main **unformatted** — `prettier --write` applied across all of them. | Live auto-send still needs `shadow-mode=false` in config *and* `autoReplyEnabled` per mailbox — two independent switches, deliberately. No template-authoring UI yet (templates are written through the generic collection route). AI drafts are slice 8 |
| Support mailbox — canned answers (slice 6) | Parent spec `specs/support-mailbox/README.md`. **Slice 6 ships (V193)**: `mailbox_template` holding **matching and policy only**; the copy stays in `email_template`, referenced by `template_key`, which buys the tenant→`'system'` fallback, the existing authoring UI, `email_log` and per-tenant SMTP for free. **`auto_send_eligible` is deliberately NOT a column on `email_template`** — putting it there would place every invoice notice and campaign blast one boolean away from being sent, unreviewed, to a stranger. `mailbox-templates` is the one **writable** collection in the feature (pure config, no save-time side effects, following `email-templates` rather than the read-only pattern used elsewhere), so the guard is a **`BeforeSaveHook`**: a controller check would be reachable around via the generic JSON:API route. `MailboxTemplateGuardHook` refuses to mark a template auto-sendable when its copy references any `${var}` outside a whitelist of platform constants — because `DefaultEmailService.substitute()` does **no HTML escaping** (`Matcher.quoteReplacement` protects the regex, not the markup), so an inbound-derived variable is an HTML injection into mail sent from our own authenticated domain, and an auto-sent reply has no reviewer. It validates updates against **previous merged with the patch**, since the dangerous edit is a PATCH carrying only the boolean — validating the submitted map alone would find no `templateKey` and wave it through. It also refuses `disclosesAccountData` without `requiresVerifiedSender`. `MailboxTemplateMatcher` is **deterministic keyword scoring, never a model** — if a model picked the template, "template-matched auto-send" would quietly become "model-controlled send" and the human-approval requirement would be void. Keywords rather than author-supplied regex, which would be an unbounded computation over attacker-supplied text. Confidence is the share of a template's own keywords present; an exclude keyword vetoes outright; two templates within 0.05 are flagged **ambiguous**, because answering with a coin-flip is worse than routing to a person. Tests: `MailboxTemplateGuardHookTest` (9), `MailboxTemplateMatcherTest` (10). | **Nothing sends.** The matcher is not wired to ingest and `SupportAutoReplyService` does not exist — that is slice 7, behind a shadow mode. No admin UI for templates; they are written through the generic collection route |
| Support mailbox — replies (slice 5) | Parent spec `specs/support-mailbox/README.md`. **Slice 5 ships**: `POST /api/support/threads/{id}/reply` and a composer in the console. **The endpoint takes no recipient** — the address comes from the thread, so an authenticated console cannot be used as a relay that sends from our domain to an address of the caller's choosing. That is a design constraint, not a validation, which is why there is no field to validate. `MailboxVerpService` mints an HMAC-signed `Reply-To` thread token (`support+t<threadId>.<hmac8>@domain`) and `MailboxThreadResolver` now checks it **first**, ahead of `In-Reply-To`: it is deterministic and survives clients that strip `References`. The HMAC is load-bearing — a bare `+t<threadId>` address would let anyone who learns a thread id post into that conversation and inherit its trust. Like `VisitTokenService`, the service **refuses to start without a secret** rather than falling back to a repo-published default; `make setup` generates `KELTA_MAILBOX_VERP_SECRET` alongside the other signing keys. `MailboxReplyService` checks `email_suppression` **explicitly**, because `DefaultEmailService.queueEmail` still does not — replying to a hard-bounced address burns the sending domain for a message nobody reads. Loop guards refuse to answer a bounce, an `Auto-Submitted` message (but not `Auto-Submitted: no`, which RFC 3834 uses for ordinary mail), or an unattended local part; refusals return **409 with a named reason** the console surfaces, not a generic error. `In-Reply-To`/`References` are copied from the customer's message and are therefore attacker-controlled — slice 0's `EmailHeaders` validation catches an injection attempt and the reply is sent **without** threading hints rather than dropped. This is the first slice that consumes `EmailHeaders`, `SendResult` and `queueReply` from slice 0. Tests: `MailboxReplyServiceTest` (11), `MailboxVerpServiceTest` (8). Worker start verified in a real compose stack. | Replies are plain text; templates own HTML and arrive with slice 6. Outbound `Message-ID` is captured from `SendResult` but a customer's reply threads via the VERP token in practice. No attachment sending |
| Support mailbox — console UI (slice 4b) | Parent spec `specs/support-mailbox/README.md`. **Slice 4b ships**: `MailboxConsolePage` at `/:tenantSlug/app/mailbox` (end-user shell, no `RequirePermission` wrapper — matching chat/approvals, since membership is enforced server-side and `RequirePermission` fails open while loading anyway), `useMailbox.ts`, and **`MailboxHtmlBody`, the only place in the product that renders third-party HTML**. Two independent controls, either sufficient: `sandbox=""` (the most restrictive value, not the least — opaque origin, no scripts/forms/popups/navigation) and a `default-src 'none'` meta CSP. `sandbox`/`frame-ancestors` are deliberately NOT in the meta CSP: both are ignored in meta form and would read as protection that cannot fire. Height is fixed because auto-sizing needs `postMessage` needs `allow-scripts` — the one thing that must never happen here. Remote images stay parked in `data-kelta-remote-src` until an explicit click, and the notice states the consequence ("loading them tells the sender you opened this message") rather than just offering a button. Plain text is the default view; the iframe is behind a toggle. Tabs re-filter one list (ChatConsolePage idiom) rather than swapping panels, rows sort worst-SLA-first, and `worstSlaState` reports a breach on either clock rather than the milder of the two. Polling at 30s is primary for the same structural reason as chat — conversation-scoped push cannot deliver arrivals for threads nobody joined — plus SLA state changes with the clock, not with an event. Tests: `MailboxHtmlBody.test.tsx` (9), including an exact `sandbox === ''` assertion. | Read-only: there is no composer, and the footer says so rather than showing one that silently does nothing — replying is slice 5. No admin UI for mailboxes or escalation contacts yet (the Setup tile links to `/mailboxes`, which does not exist until that page is built). Attachments list but do not download (slice 9) |
| Support mailbox — console API + SLA escalation (slice 4a) | Parent spec `specs/support-mailbox/README.md`. **Slice 4a ships (V192)**: `mailbox_escalation`, `mailbox_escalation_delivery`, `mailbox_escalation_contact`, `mailbox_thread_read`, plus the `support.sla_escalation` system email template. `MailboxController` (`/api/support/threads/**`) gives agents list/read/claim/assign/status; **`MailboxAccessGuard` is the whole authorization story** — per-mailbox membership, not a system permission, and **absence of access is 404 rather than 403** because a 403 confirms a thread exists and turns any id-taking endpoint into an enumeration oracle. A thread is authorised against its **owning** mailbox, or membership of one mailbox would read every thread in the tenant. `MailboxSlaSweep` is a `@Scheduled` platform bean (not `scheduled_job`, which is tenant-scoped with a `job_type` CHECK) firing a WARN/BREACH/BREACH_2/BREACH_3 chain on both clocks; **the claim is an INSERT guarded by a unique constraint**, so concurrent pods race, one wins, losers get zero rows — no leader election, no read-then-write window. Claim-then-send drops one notification on a crash rather than double-paging, and a dropped WARN self-heals into a BREACH. `MailboxEscalationDispatchService` copies `AlertDispatchService`'s discipline: PENDING delivery rows written **before** any send, per-channel try/catch so a stale push token does not cost someone the email. Status transitions settle or pause the SLA clock in the same statement — which is precisely why the collection is read-only over JSON:API, since a bare status write would leave a row reporting RESOLVED while its clock still says PENDING. Found by running it: `settleStates` was marking resolved and spam threads BREACHED because, unlike `claimDue`, it did not exclude terminal statuses — nobody was paged, but the console would have shown a permanent red badge on finished threads; the two now agree. Verified against real Postgres: of five seeded threads (overdue / already-answered / resolved / spam / paused) only the overdue one claims, and a second identical claim returns zero rows. Tests: `MailboxAccessGuardTest` (9). | The console UI is slice 4b. Sending a reply is slice 5, so `recordFirstResponse` exists but nothing calls it yet; SMS is not wired (billable, no entitlement resolution in this slice) though the channel CHECK permits it. Escalation contacts have no admin API yet — rows are seeded directly |
| Support mailbox — ingest (slice 3) | Parent spec `specs/support-mailbox/README.md`. **Slice 3 ships**: `POST /api/webhooks/mail/{mailboxKey}` (`MailboxWebhookController`) on the already-allowlisted `/api/webhooks/**` prefix — deliberately a **different top-level segment** from the authenticated `/api/support/**`, because `PublicPathMatcher` matches by `startsWith` and a nested ingest path would mean one careless prefix edit exposes the whole feature. `InboundMailAdapter` SPI + `SesSnsInboundAdapter` and `GenericHmacInboundAdapter`; the provider is read from the **mailbox row, never the request**, or anyone holding a mailbox key could pick the weakest verifier. `SnsSignatureVerifier` gains an optional **TopicArn pin** (a valid signature only proves *some* SNS topic in *some* account signed it — anyone can create a topic and publish to a public endpoint); this also closes the same gap in `SesNotificationController`. `MimeParser` (jakarta.mail, no new dep) with the rules written down: attachment-ness decided by disposition/filename **before** MIME type, `multipart/alternative` takes the **last** match per RFC 2046, and `message/rfc822` is attached rather than descended into — recursing is how a forwarded receipt silently replaces what the customer wrote. **`MailboxHtmlSanitizer` is the platform's first HTML sanitiser** (jsoup, built up from `Safelist.none()`; `relaxed()` permits `style`): scripts/style/base/form/svg gone, remote `img src` parked in `data-kelta-remote-src` for opt-in loading rather than deleted, links forced to `rel=noopener`, comments stripped explicitly, 2 MiB / 50k-node parse caps. Sanitised **once at ingest**, not on read — on-read sanitising makes any future policy bug exploitable against every historical message at once. `mailbox_inbound_event` claim happens **before** parsing so a poison message stops retrying, and raw MIME is persisted to Garage before parse so it can be re-driven. `SesInboundMailStore` is a second, AWS-configured S3 client (no `endpointOverride`) because `S3StorageService` is bound to Garage and SES can only write to real AWS. Gateway: `/api/webhooks/mail=600` added to `ip-paths` (SNS bursts from few IPs, so a tight budget drops real mail). Tests: sanitizer (32, incl. dangling-markup and scheme-obfuscation cases), `MimeParser` (12), subject normalisation (14). Verified against real Postgres: both dedupe paths and the thread→message→attachment cascade. | Postmark/Mailgun adapters not written (one class each). Nothing notifies a human yet — the console and SLA sweep are slice 4. `requester_verified` is still always false; DMARC pass is recorded but identity matching is slice 4. No AV scanning: `virusVerdict` is honoured by discarding attachments, but nothing scans |
| Support mailbox — admin API (slice 2) | Parent spec `specs/support-mailbox/README.md`. **Slice 2 ships**: `MailboxAdminController` at the new `/api/support/**` static gateway route (`MANAGE_SUPPORT_MAILBOX`-gated in-controller, `CampaignAdminController` pattern — static routes get only the blanket `API_ACCESS` Cerbos check, so the controller owns all authz), `MailboxRepository` + `MailboxAccessRepository`, and `MailboxSecretService`. **Two kinds of material, deliberately different**: the `webhook_key` is an *identifier* (appears in provider consoles and logs; random only so the endpoint cannot be enumerated), while the HMAC secret is the authenticator and lives encrypted in the credential vault — the `mailbox` row holds only a reference and a 4-char hint. **Plaintext is returned exactly once**, on create and on rotate, and no endpoint can read it back; losing it means rotating, which is a better thing to own than an API that re-reads live signing secrets. Rotation keeps the previous secret live for an overlap window (`kelta.mailbox.secret-rotation-overlap-minutes`, default 1440) because a provider cannot switch keys at the same instant we do, and a rejected inbound message is a lost customer email rather than a retryable blip. Field mapping is an **allowlist in both directions**: inbound so `webhook_key` and the credential references cannot be set from a request body, outbound so a column added later is not published by accident. `RouteConfigService`'s static route table was extracted to a package-visible `STATIC_ROUTES` constant and `RouteConfigServiceTest` now derives its expected counts from `.length` instead of five hardcoded literals whose comment had already drifted (it said 56 while asserting 59). Tests: `MailboxAdminControllerTest` (16) covering the permission gate, secret non-disclosure, server-minted-field rejection, and cross-mailbox grant revocation. | Ingest is slice 3; no inbound webhook exists yet, and `mailbox_access` is written but not yet read for authz (slice 4). `rolesForUser` resolves direct group membership only — `group_membership` allows nested groups and the query does not walk that tree, which under-grants rather than over-grants |
| Support mailbox — schema (slice 1) | Parent spec `specs/support-mailbox/README.md`. **Slice 1 ships (V191)**: six tables (`mailbox`, `mailbox_access`, `mailbox_thread`, `mailbox_message`, `mailbox_attachment`, `mailbox_inbound_event`) + RLS + six `readOnlySystemBuilder` system collections + `VIEW_SUPPORT_MAILBOX`/`MANAGE_SUPPORT_MAILBOX`. Modelled on chat (V168) including its access decision: **no `profile_object_permission` rows are seeded**, so generic JSON:API denies everyone but `VIEW_ALL_DATA` holders and all traffic must go through `/api/support/**` where `mailbox_access` is checked — this matters more here than for chat because inbound bodies are third-party HTML and the admin Resource Browser renders rows with no sandboxing (hence `bodyHtml` is deliberately **not** declared on `mailbox-messages`). SLA **policy** lives on `mailbox`, SLA **state** on `mailbox_thread` with due times **frozen at thread creation** — deriving them at read would let a policy edit retroactively breach every open thread and rewrite history in every report already run. `mailbox_inbound_event` is a non-optional idempotency ledger: SNS retries on any non-2xx and delivers at-least-once even on 2xx. **Three indexes are deliberately not tenant-leading** (`mailbox_webhook_key_key`, the two partial SLA-sweep indexes) — webhook resolution runs before a tenant is bound, and the sweep runs cross-tenant under `admin_bypass`; both carry migration comments saying so. Verified by applying the full V1–V191 chain to a throwaway Postgres: 6/6 tables RLS-enabled and FORCEd with both policies, partial indexes as intended, permission backfill 8 rows each with 1 granted, re-run clean (idempotent, no duplicate rows), and a functional RLS check confirming per-tenant isolation plus `admin_bypass` visibility for the unbound sweep. `TenantProvisioningHookTest` now derives its expected row count from `ALL_PERMISSIONS.size()` instead of a hardcoded 208. | Ingest, console, SLA sweep, templates, auto-send and AI drafts are slices 2-8; no runtime code reads these tables yet |
| Mass-email campaigns | Bulk send on the SMTP path (V152). `campaigns`/`campaign-recipients`/`email-suppressions` system collections (RLS) + `email_campaign`/`_recipient`/`email_suppression` tables. `CampaignAdminController` (`/api/admin/campaigns`, **`MANAGE_CAMPAIGNS`**-gated): CRUD + send/schedule/cancel/stats/recipients/test/suppressions. `CampaignRunnerService` (polled by `CampaignPollerConfig`, `SELECT FOR UPDATE SKIP LOCKED` conditional-claim; resolves recipients via `QueryEngine`, `${field}` merge render, sends via `EmailService`). Public HMAC-token tracking `/api/track/{open,click,unsubscribe}` (`CampaignTrackingController` + `TrackingTokenService`, gateway `unauthenticated-paths`). Spam controls: suppression check per send, `campaignEmailsPerDay` governor, `send-rate-per-second` throttle. Unit tests (runner + token) + `CampaignCreateScenarioTest` harness scenario; `CampaignsPage` admin UI + Playwright. **Not auto-merged (spam surface).** |

## 🟡 Partial — backend exists, gaps remain

| Capability | Works | Missing |
|------------|-------|---------|
| Kelta CLI foundation (kelta-cli slice 1) | Parent spec `specs/kelta-cli/README.md`, slice `specs/kelta-cli/1-foundation.md`. **Slice 1 ships**: `@kelta/cli` rebuilt on the **command registry** (`src/registry/` — every command is a data record with a zod input schema; parser/help derive from it, slice 4's manifest and slice 5's MCP tools will too) and on `@kelta/sdk` (`KeltaClient` + `tokenProvider`; the private axios client is deleted). **Profiles**: `~/.kelta/config.json` + `~/.kelta/credentials.json` (0600), silent `~/.keltarc` migration, `profile list/use/show/remove/rename`, precedence flags > env (`KELTA_PROFILE`/`KELTA_URL`/`KELTA_TENANT`/`KELTA_TOKEN`) > file. **Output contract**: `--output table\|json\|yaml\|csv\|ndjson` (TTY→table, piped→json), `--raw` envelope, `--quiet` ids, flattened `{id, ...attributes, rel: id}` rows; errors = single-line stderr JSON carrying the platform's JSON:API `code` + stable exit codes (0/1/2/3/4/5); destructive commands need TTY confirm or `--yes` (predicate-aware: `metadata apply --dry-run` is exempt). All prior commands ported (records incl. `--filter field[.op]=value`/`--sort`/`--fields`/`--include`/`--all`, collections, metadata, sandbox, promote, sdk types — file-output flag renamed `-o/--out`; global `--output` owns format). **CLI now builds in root `build` and runs in frontend CI** (89 unit tests; caught a real bug: the shared empty-config fallback constant was mutated across loads). **Bun-compile spike PROVEN** (D1): all 5 targets cross-compile from one host (55–107 MB), linux binary verified in `debian:12-slim`, darwin binary runs the real command set. e2e smoke `e2e-tests/tests/admin/cli-smoke.spec.ts` (deploy workflow builds the CLI; spec self-skips without dist). **Slice 2 ships** (`specs/kelta-cli/2-browser-login.md`, two PRs, **both security — no auto-merge**): PR A (kelta-auth) registers `kelta-cli` as a PKCE public client (method NONE, 15-min token, deliberately NO refresh grant) and `PlatformRedirectUriValidator` accepts RFC 8252 loopback redirects for that client only (`http://127.0.0.1:<any port>/callback` or `[::1]`; loopback IP literals — `localhost` rejected; path exactly `/callback`; no userinfo/query/fragment). PR B (CLI): `kelta auth login` = one-shot 127.0.0.1 listener (state + S256 verified, 120 s timeout) → browser authorize → code exchange → mint PAT via `POST /api/me/tokens` (`--expires-in`, default 90d) → PAT stored, JWT discarded; `--token` headless path unchanged; `--no-browser` prints the URL; auth-url = profile > `--auth-url` > `api.`→`auth.` derivation. `kelta token list/create/revoke` + `auth logout --revoke` (prefix-matched server-side revocation). PKCE pinned to the RFC 7636 Appendix B vector; state forgery aborts before any token call. **Slice 3 ships** (`specs/kelta-cli/3-admin-surface.md`): admin command surface at (slightly beyond) kelta-mcp admin-toolset parity — `collections create/update/delete`, `fields list/add/update/remove` (friendly type aliases + per-type config ported from the MCP `FieldBodyBuilder`: `unique`→`uniqueConstraint` rename, picklist `globalPicklistId`, reference name-attribute + id-relationship, vector dimension), `picklists` (+`value-add/update/deactivate`; value-add sends both `picklistSourceId` AND `globalPicklistId` — MCP parity), `validation-rules` (ERROR-condition semantics stated in help text), `constraints` (plain non-JSON:API body; list/create/delete by index name), `layouts` (PATCH uses the worker's camelCase `pageLayouts` type) + `list-views` (filter/sort transform), `flows` (list/describe/create/update/`execute --wait`/runs/run `--steps`/cancel/retry `--mode`/publish/versions/webhook-url — via the worker-only `/api/flows/**` endpoints the SDK lacks), `users` (list/get/invite/portal-invite/reset-password/logins), `limits` (get/`set-tier`/`set` = raw `PUT /api/governor-limits` map), `audit setup\|security\|logins --since`, `records bulk` (`/api/operations` atomic batch, all-or-nothing), `records search` (`/api/_search`) + `semantic-search`. Name→id lookups retry thrown 404s briefly (multi-pod NATS cache-refresh race). All destructive commands `--yes`-gated. 40 new unit tests (129 total); e2e admin round-trip added to cli-smoke (collection→field→validation rule→rejected write→delete). **Slice 4 ships** (`specs/kelta-cli/4-ai-ergonomics.md`): `kelta manifest [--group]` — versioned machine catalog (manifestVersion 1: cliVersion, outputFormats, exit-code contract, every command with dangerous/requiresAuth flags + JSON Schema of its input via zod-to-json-schema; drift guard test asserts every positional/option key appears in the schema). `kelta api <METHOD> <path>` — gh-api-style escape hatch: profile auth + tenant prefix applied, any-JSON `--data`, repeatable `--header`, response VERBATIM (never flattened); non-GET treated as dangerous; sugar-level validation bypassed by design — gateway+Cerbos stay the enforcement layer. `kelta docs agent` prints the condensed agent guide; the same source generates `packages/cli/AGENTS.md` + `COMMANDS.md` (86 commands) via `npm run gen:docs`, freshness pinned by a vitest test (prettier deliberately ignores the generated files). **Authoring docs ship (KLT-211)**: `kelta docs <topic>` now also serves `docs/authoring/{jsonapi,page-layouts,list-views,dashboards,ui-pages,ui-menus}.md` (unknown topic exits 2 listing valid topics), embedded and freshness-pinned the same way; exposed as a `cli_docs` local MCP tool and, on the kelta-mcp side, as `kelta://docs/<topic>` resources on both `/mcp/user` and `/mcp/admin` (new `AdminResource` marker interface + a resources capability on the admin server, which previously advertised tools only). **Slice 6 ships** (`specs/kelta-cli/6-distribution.md`): self-updating binary distribution. `kelta-cli-downloads` nginx image cross-compiles all five targets IN the image build (node stage builds the SDK, pinned `oven/bun:1.2.19` stage compiles with `--define` identity: version `1.0.<run#>`, git sha, `<os>-<arch>` target — `BUILD_TARGET==='dev'` refuses self-update so node/dist builds can never replace themselves), serves `/cli/manifest.json` (zod-validated by the client, 60s cache) + immutable `/cli/releases/<v>/…` + `SHA256SUMS`/`latest.txt` + `install.sh`/`install.ps1` (checksum-verified installs, no jq dependency). `kelta update [--check]`: manifest fetch → numeric semver compare → sha256-verified download → atomic swap (POSIX rename; Windows exe→`.old` dance with next-run cleanup); mirror-friendly relative `path` joined to `KELTA_UPDATE_URL`. Passive nudge ≤1/24h (stamp written BEFORE the fetch — an unreachable host can't become a daily retry storm), TTY+released builds only, never in `mcp serve`, `KELTA_UPDATE_CHECK=0` opt-out. `kelta version` reports version/sha/target. Pipeline: `cli-downloads` matrix entry + `cli_downloads` path filter, deploy bump with the Harbor tag-exists guard, and an in-cluster smoke that curls the Service, asserts the manifest version equals this run's `1.0.<run#>`, downloads the linux binary and EXECUTES it (warning-skip while homelab-argo manifests are absent). | Live login round-trip owed after PR A deploys (staging first — memory precedent: probe the real flow post-deploy). Device grant deliberately absent (SSH boxes use `--token`). Dashboards/reports/campaign authoring stay `kelta api` territory. Local MCP (5), binary distribution + self-update (6). Windows binary compiles but has never been executed on Windows. Ingress/Service/Deployment for `downloads.kelta.io` live in homelab-argo (companion PR). Binaries unsigned (macOS Gatekeeper note: `curl | sh` and self-update don't set quarantine; signing tracked for public distribution). Harbor tag retention for ~90MB×5 per merge needs a policy. |
| Portal billing (consumer-alerting slice 1) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/1-portal-billing.md`. **1B ships**: `StripeApiClient` on `RestClient` (form-encoded, `JsonNode`, pinned `Stripe-Version`; **deterministic** `Idempotency-Key` for customer creation — one customer per member ever — but **per-attempt** for checkout/portal sessions, since a deterministic key would replay the previous session for the processor's 24h window and block a second same-day pass purchase). The member HTTP surface now lives in the **module**, on `/api/modules/kelta-billing/x/**` — `GET /plans` (safe fields only, no processor ids), `GET /me`, `POST /checkout-sessions`, `POST /portal-sessions`; the compiled-in `BillingController` and its whole `service/billing` HTTP stack were deleted once those routes were verified against prod; every endpoint acts on the **calling** member resolved from `X-User-Id`, so no endpoint takes a member id and there is nothing to tamper with. **Return-URL validation** (now the module's, on the semantics the deleted `ReturnUrlValidator` set) gates checkout/portal return URLs against the credential's `allowedReturnOrigins` by **origin equality** (a prefix check accepts `https://app.example.com.evil.test`), HTTPS-only, userinfo rejected, empty allowlist denies all — these URLs are handed to the processor, which redirects a paying member to them. **`MemberEntitlementQuotaHook`** is a wildcard `BeforeSaveHook` (order -90) driven by `billing-entitlement-rules` rows: cached-empty-rule fast path costs nothing on the create path of every collection; counts the member's rows via `QueryEngine.aggregate`, which validates filter fields against the collection definition and binds values — that is what makes the tenant-authored `countFilter` JSON safe. Rejections are **HTTP 400** (`beforeSaveHook`), not 422 — the platform has no 422 mapping. Fails **open** (unknown collection, bad filter, failed count → allow), matching the governor-limit hooks, unlike the fail-closed guard hooks. `BillingPassExpirySweep` (@Scheduled) advances due passes to EXPIRED and evicts caches — a tidier, not a gate, since expiry is judged at read time. Harness `BillingWebhookScenarioTest` (real stack): a redelivered signed webhook grants **exactly one** pass, and bad-signature / unknown-tenant / stale-timestamp all return an identical bare 401. Tests: 81 new unit + 2 harness. **Security slice — not auto-merged.** **1A shipped (V178)**: a reusable tenant→portal-member commerce substrate. Five tables (`billing_plan`/`billing_customer`/`billing_subscription`/`billing_pass`/`billing_entitlement_rule`, all RLS'd) + platform-scoped `billing_webhook_event` dedupe (no RLS, `livekit_webhook_event` shape). System collections: `billing-plans` + `billing-entitlement-rules` admin-writable, `billing-customers`/`-subscriptions`/`-passes` read-only (webhook-written). **Plans carry an OPAQUE `entitlements` JSONB** — the platform merges and compares values but never interprets keys, so a tenant invents a limit with no schema change. **`EntitlementService`** resolves a member: subscription plan when status ∈ {`active`,`trialing`,`past_due`} (grace while the processor retries a card), else the tenant's DEFAULT plan (lapse-to-free), then merges every **live** pass — numbers SUM, booleans OR, arrays union; expiry is judged at read time so a member is never over-entitled by a sweep that hasn't run. Caffeine-cached (5 min), invalidated fleet-wide over **`kelta.billing.entitlement.changed.*`** (new `KELTA_BILLING` stream; payload with no `userId` = whole tenant, which is what `BillingPlanRefreshHook` publishes on a plan edit). **Webhook** `POST /api/billing/webhooks/stripe/{tenantId}` — gateway-unauthenticated, HMAC over the **raw** body is the only trust anchor (constant-time compare, 5-min replay tolerance, multi-`v1` for secret rotation); path `{tenantId}` is untrusted and only picks the secret. Claim insert and mutation share **one** `@Transactional` so a processing failure rolls the claim back and the retry genuinely re-runs. Dispatch: `checkout.session.completed` (payment → idempotent pass grant; subscription → customer only), `customer.subscription.created/updated/deleted`, `invoice.*` → flow bridge on `kelta.trigger.<t>.billing.subscription`. `StripeCredentialType` (`secretKey`+`webhookSecret` secrets, `publishableKey`+`allowedReturnOrigins` metadata, test = `GET /v1/account`); **no `stripe-java`** — its Gson-reflective surface is a native-image hazard. New **`MANAGE_BILLING`** permission (V178 seed to System Administrator only, + provisioning + FE catalog). Tests: `StripeSignatureVerifierTest` (17), `EntitlementServiceImplTest` (30), `BillingWebhookServiceTest` (19). **Security slice — not auto-merged.** | Refunds/disputes deferred (the webhook dispatch table is extensible; passes only reach REFUNDED once `charge.refunded` is handled). Cerbos visibility of the billing collections to portal profiles still unverified. No real-processor round-trip pre-deploy — `stripe listen` locally, then test clocks for renewal/past_due/cancel timelines. The per-IP webhook limiter is still in-memory per pod (Redis-backed is slice 7). No frontend — the consumer app lives outside this repo |
| Public-traffic hardening (consumer-alerting slice 7) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/7-public-traffic-hardening.md`. **Slice 7 ships** — the gate for enabling portal self-signup in production. **Every limiter is Redis-backed**: the per-IP filter counted in memory, so N gateway replicas granted N× the configured budget, which is worthless for a public signup endpoint. All windows now route through one `RedisRateLimiter.checkWindow` primitive — a second copy of the window logic is how the TTL-refresh lockout (2026-07-11) comes back. **NEW per-user window** in `RateLimitFilter`: the tenant window is *shared*, so one member or one stolen PAT could lock out the whole tenant. It is checked **before** the tenant window and, when it rejects, the tenant counter is deliberately not incremented — a rejected member must not spend the shared budget. Budget = `user-share` × the tenant window (default **0.9**, floored at 1; out-of-range falls back rather than silently disabling — 0.5 was tried first and halved legitimate single-user throughput, failing the e2e suite on 429, because the tenant window is sized for the *whole* tenant and one admin or integration PAT legitimately is most of it); the key is the principal username, which is the member's email for **both** JWTs and PATs, so switching credential type cannot double a budget. **The portal public paths are limited in kelta-auth, NOT the gateway** — `/portal/**` has its own ingress and never transits the gateway, so a gateway entry would match nothing while reading like protection; `PortalPublicRateLimitFilter` (order -150, same window semantics) covers `/portal/api/{signup,login/request,challenge}` **and** the Thymeleaf `/portal/login` form, which sends the same email through the same service. Its client-IP resolution counts `trusted-proxy-count` hops back from the **right** of `X-Forwarded-For` — the leftmost entry is client-supplied, and honouring it lets anyone mint a fresh bucket per request. **Bot challenge**: `BotChallengeVerifier` seam + `AltchaBotChallengeVerifier`, a self-hosted proof-of-work scheme (no third party sees a visitor's IP; hosted CAPTCHAs fail the open-source rule). Three checks must all hold — HMAC (we issued it, so a client cannot invent an easy one), hash (the work was done), and unexpired + **not replayed** (a reusable solution buys unlimited requests). Replay state is claimed in **Redis**, not memory, and fails **closed**, unlike the limiters. Enabled without a shared `hmac-key` **refuses to start**: a per-pod random key verifies only on the issuing pod, so a share of real signups would fail invisibly. Plus a honeypot field on both endpoints — answering **202**, not an error, so a script learns nothing. Failed challenges and honeypot trips are on `security.audit` (not just the app log), and caller-supplied emails go through `AuditText.sanitize` so a newline cannot forge audit records. `PublicSurfaceTest` pins the gateway's unauthenticated prefix allowlist against the **shipped** `application.yml`. Found en route: `checkWindow` failed open on error but **hung the request** on an empty Redis reply (the calling filter never reached `chain.filter`) — fixed with `switchIfEmpty`. Tests: `AltchaBotChallengeVerifierTest` (17), `PortalPublicRateLimitFilterTest` (18), `PublicSurfaceTest` (5), + rewritten `IpRateLimitFilterTest` (21) / `RateLimitFilterTest` (17) / `RedisRateLimiterTest` (18) / portal controller tests (44). **Security slice — not auto-merged.** | **Nothing proves two replicas share a counter** — the tests assert key shape and the TTL contract against a mocked Redis; a Testcontainers-Redis harness scenario is owed (`concerns.md` → Test Coverage Gaps). Entitlement-driven per-member budgets (a paid tier buying a bigger window) deferred — the seam is `RateLimitFilter.checkPerUser`; the shipped budget derives from the tenant governor and needs no cross-service call. Route-class multipliers skipped: the per-path `ip-paths` map already expresses per-path budgets directly, and a second, differently-shaped knob with no concrete driver would rot. Edge controls (ingress rate-limit annotations, CrowdSec, `robots.txt`) live in the deployment repo and the consumer frontend, not here. The ALTCHA widget itself is frontend work, outside this repo; difficulty (`max-number`) needs tuning against real mobile clients |
| Browser Web Push / VAPID (consumer-alerting slice 6) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/6-web-push.md`. **Slice 6 ships**: browser push as a third transport under the existing `PushProvider` SPI (see `integrations.md` → *Push notifications*). **`WebPushCrypto`** implements RFC 8291 (message encryption) over RFC 8188 (`aes128gcm`) plus RFC 5869 HKDF on **JDK crypto only** — every push library pulls BouncyCastle, whose reflective surface would need worker `reflect-config.json` entries or it fails *only* on the native image in production; the VAPID JWT uses nimbus, already native-proven here via LiveKit. Hand-rolling is defensible **because it is checked against the RFC's own vector**: `WebPushCryptoTest` reproduces the RFC 8291 §5 ciphertext byte-for-byte (plus RFC 5869 case 1) — an encryption bug here does not throw, it emits a body every browser silently discards, so nothing else would catch it. `WebPushProviderTest` verifies the ES256 VAPID signature against the advertised public key and that the audience is the push service **origin**, never the endpoint (the endpoint path *is* the subscription). **`PushProvider.supports(platform)`** (default `true`) + per-device routing in `DefaultPushService`: `WebPushProvider` registers on `kelta.push.vapid.public-key` **alongside** the `kelta.push.provider`-selected mobile provider rather than instead of it, and a platform-specific provider wins over the default. Device token = **`sha256(endpoint)` hex** (endpoints exceed the 500-char column; the token carries `UNIQUE (tenant_id, device_token)`), subscription JSON in `push_device.web_push_subscription` (**V180**). `POST /api/devices` accepts `subscription`; `GET /api/devices/vapid-public-key` serves the key (404 unconfigured) — on the existing `/api/devices/**` route, since a new `/api/push/**` segment would need its own gateway static route for no gain. 404/410 prunes the device like a stale FCM/APNs token; a **missing VAPID key fails without pruning**, so an operator config gap cannot wipe every subscription. Portal members reach `/api/devices/**` on the blanket `API_ACCESS` the seeded Portal User profile already has — no policy change. `make gen-vapid` writes a dev pair. Tests: `WebPushCryptoTest` (8), `WebPushProviderTest` (15), `DefaultPushServiceTest` +6. | **Live-stack verification owed** — native-image crypto is exactly the failure class CI cannot catch, and Safari's strict VAPID subject needs a cross-browser check. Per-tenant VAPID overrides not wired (platform-level keys only; the `TenantPushSettings` precedent exists). Service worker + subscribe flow live in the consumer frontend, outside this repo. Rotating the key pair invalidates every existing subscription — no re-subscribe prompt exists |
| Availability alerting — portal member surface (consumer-alerting slice 5) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/5-portal-member-surface.md`. **Slice 5 ships**: the first member-facing surface. **`WatchController`** on the new `/api/watches/**` static route — `GET` (own only), `POST` (owner stamped from the caller, channels ∩ entitled, quota pre-check for a friendly 422 with an upgrade hint), `PATCH`/`DELETE`/`GET /{id}/alerts`, all self-scoped. **A foreign id returns 404, not 403** — 403 would confirm the id exists and turn the endpoint into an enumeration oracle. Writes go through `QueryEngine` so `WatchGuardHook` + `MemberEntitlementQuotaHook` fire; bypassing to the repository would skip both. **`WatchGuardHook`** (order -100) locks the **generic dynamic route**, which bypasses the controller entirely: blocks creating/editing/deleting another member's watch and re-owning one, **fails closed** on unresolvable identity (deliberately unlike the fail-open quota hook — this protects members' data, not revenue), admits internal-tier writes. **Portal self-signup** `POST /portal/api/signup` on kelta-auth — **always 202** (created / already-exists / staff-address / seat-limit / worker-outage are indistinguishable); only a disallowed `redirectUri` 400s, checked before any account lookup. Gated on `tenant.settings.portalAuth.selfSignupEnabled`, **default FALSE** so a deploy cannot open public account creation on existing invite-only tenants. Creation is delegated to the worker's internal `POST /api/internal/portal/signup` so the **`maxPortalUsers` seat governor** is enforced — public signup without a seat cap is unbounded account creation. Needs **both** permitAll and a CSRF exemption. Tests: `WatchGuardHookTest` (14), `PortalSignupControllerTest` (13). **Security slice — not auto-merged.** | Production enablement of self-signup waits on slice 7 (per-IP budget + bot challenge); the INTERNAL-collision explanatory email is not sent (no template exists — audited instead); no consumer frontend in this repo |
| Availability alerting — matcher + fanout (consumer-alerting slice 4) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/4-availability-matcher.md`. **Slice 4 ships**: the alert hot path, end to end. `AvailabilityEventListener` consumes `kelta.availability.event.<t>.<source>` on **queue group** `worker-availability` (exactly one pod per observation — broadcast would have every pod race for the same claims); the body is poller-authored JSON parsed as `JsonNode`, so no `PlatformEvent` payload and **no new native reflect-config surface**. `AvailabilityMatchService` pipeline: resolve target by `(source, externalId)` → record the observation and read the transition → **stop unless the slot just opened** (the volume killer: nearly every poll reports no change, and the watch query is skipped for them) → one indexed live-watch query → criteria evaluated in Java (opaque member-authored JSONB must not be welded into a query plan) → suppression-window guard → `AlertRepository.claim` insert-as-dedupe. `AlertDispatchService` sends **outside any transaction, after the alert row is committed** — the alert row IS the dedupe record, so a provider timeout must never roll it back and re-alert on retry; a failed channel is a FAILED delivery row and does not stop the other channels. Channels are watch-requested ∩ plan-entitled (`EntitlementService`), so a downgraded member loses a channel without anyone editing their watches; SMS is wired through the `SmsProvider` SPI (`TwilioSmsProvider`, consumer-alerting slice 10 — reads `platform_user.phone_number`; a member with no number records a FAILED sms delivery, never a rollback). Post-fanout, an ids-and-counts summary republishes to `kelta.trigger.<t>.availability` as a plain Map (presence precedent, no reflect entries) so tenant flows react *after* members are notified. Config `kelta.availability.{enabled,suppression-minutes:30}`. Tests: `AvailabilityMatchServiceTest` (16), `AlertDispatchServiceTest` (13), `AvailabilityEventListenerTest` (13). | Poller lives outside this repo (contract in the parent spec); member-facing watch API is slice 5; tenants must create the `availability.alert` email template before go-live; per-target fanout is unbounded — revisit batching past ~10k watches on one target |
| Win tracking + live ticker (consumer-alerting slice 9) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/9-win-tracking.md`. **Slice 9 ships (V184)**: `wins` system collection (`win`, RLS) — the social-proof + retention engine. **`WinController`** on `/api/wins/**` (static route, `API_ACCESS`, all scoping in-controller; writes via `QueryEngine` so `WinGuardHook` fires): `POST /api/wins` (member records a win, owner-stamped, server-set first-name `claimantName`), `GET /api/wins` (own wins), `GET /api/wins/recent` (the **live-wins ticker feed** — recent opt-in-`isPublic` wins **redacted** to `{claimantName, summary, category, quantity, claimedAt}`, cross-member by design but no member identity), `GET /api/wins/stats?targetId=` (indexed COUNT + last win). **`WinGuardHook`** (owner guard, faithful mirror of `WatchGuardHook`, registered in `FlowConfig`) locks the generic route. **Realtime ticker reuses existing machinery**: a win create emits `kelta.record.changed.<tenant>.wins` → `RealtimeBridge` → clients refetch `/recent` (no new NATS subject). Cerbos: no new policy — the API_ACCESS-only Portal profile default-denies generic reads. Tests: `WinGuardHookTest` + `WinControllerTest` (unit, `/recent` redaction asserted) + real-DB `WinScenarioTest` (RLS isolation + ticker/stats query shapes). **Migration V184 must deploy after slice 8's V183.** | **Anonymous (unauthenticated) ticker deferred** to the public read-surface slice — `/recent` is authenticated for now. **Materialized per-target success-stat rollups** (for SEO pages) deferred to the analytics-rollup/K10 slice — stats compute on demand. "Did you get it?" alert→claim flow automation is tenant flow config (the win API is the primitive); `alertId`/`watchId` stored for the future time-to-claim metric. `member_id` erasure of public wins owed with the rollup/erasure slice |
| SEO page generation (consumer-alerting slice 11) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/11-seo-page-generation.md`. **Slice 11 ships (V185)**: `seo-pages` read-only system collection (`seo_page`, RLS) — one **aggregate-only** row per watchable target (public metadata + `watcherCount`/`winCount`/`lastWinAt`/`stats`, a URL `slug`, `published`), **no member data**. **`SeoPageGenerationService`** (@Scheduled nightly, runs unscoped via admin-bypass, metric `kelta_worker_seo_pages_generated`): one pass reads every tenant's targets/watches/wins, upserts a page per active target on `(tenant_id, slug)` (slug = slugify(name)+short-id), applies the **§6.3 quality guardrail** (`published` only when `watcherCount ≥ kelta.seo.generation.min-watchers`[5] OR any win — thin pages stay unpublished/noindex), and prunes pages not regenerated this cycle. **Read surface (K9)**: served by the standard authenticated `/api/seo-pages` dynamic route; a static content site reads it with a **build-time service PAT** (read-only profile) — **still no anonymous bulk API** (`PublicSurfaceTest` holds). Uses `tools.jackson` ObjectMapper (Jackson-3 boot trap). Tests: `SeoPageGenerationServiceTest` + `SeoPageGenerationWiringTest` (Jackson-3 DI guard) + real-DB `SeoPageScenarioTest` (distinct-active-watcher + win aggregate, idempotent upsert, prune, RLS). | **Astro rendering deferred to slice 12** (kelta-marketing consuming `seo-pages` at build → target/Q&A/calendar pages + sitemap + schema.org). Availability-history/best-odds charts wait for an availability rollup (raw observations aren't persisted). Q&A pages need the demand-clustering work. Slug changes on target rename (new URL; old page pruned) |
| Availability alerting — watch/target model (consumer-alerting slice 3) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/3-watch-model.md`. **Slice 3 ships (V179)**: the data foundation for watch → detect → match → alert. Two tiers on purpose — `watch-targets` + `watches` are **system collections** (JSON:API, RLS, validation, audit for free, and a fixed shape the slice-4 matcher can index), while `availability_state`/`alert`/`alert_delivery` are matcher-internal plain tables written by direct JDBC (no admin surface, no per-write collection overhead). **Raw availability observations are deliberately never persisted** — only the derived transition is; storing every poll of every target is unbounded growth for data nothing reads. **Episodes are the anti-spam mechanism**: `availability_state` mints a fresh `episode_id` on each CLOSED→OPEN edge inside a single atomic `INSERT … ON CONFLICT DO UPDATE … RETURNING`, so two pods racing the same event cannot both conclude "just opened"; `alert` then carries `UNIQUE (tenant_id, watch_id, slot_key, episode_id)` so a slot open across a hundred polls alerts once, and a genuine reopen alerts again. `AlertRepository.claim` is insert-as-claim. `watch.target_id` FK is **NO ACTION** — deleting a target members are watching fails loudly rather than silently discarding their watches; `alert_delivery` cascades. `WatchCriteria` is **versioned** (`{v,dateStart,dateEnd,quantity?,minDuration?}`, ISO calendar dates not instants) because the matcher pushes the date overlap into SQL — a silent shape change would not fail, it would just stop matching. Unknown keys are ignored (forward-compatible) but malformed known keys are reported. **No refresh hooks**: `SystemCollectionCacheInvalidationListener` derives its name set from `SystemCollectionDefinitions.byName()`, so registration alone gives fleet-wide eviction — dedicated hooks would have been dead code. Tests: `WatchCriteriaTest` (21) + harness `WatchModelScenarioTest` (target identity, alert dedupe, reopen, FK block, cascade, RLS via non-superuser probe). | Matching/fanout is slice 4; member-facing watch API is slice 5; no poller in this repo (external). Cerbos default-deny for portal profiles on these collections is enforced in slice 5 |
| Analytics capture (consumer-alerting slice 8) | Parent spec `specs/consumer-alerting/README.md`, slice `specs/consumer-alerting/8-analytics-capture.md`. **Slice 8 ships (V183)**: `analytics-events` read-only system collection (`analytics_event`, RLS, tenant-scoped, high-volume — the corpus behind the demand-mining loop + later Q&A/SEO + demand clustering). **Two capture paths**: `SearchController` auto-captures every `/api/_search` as a `SEARCH_QUERY` event (verbatim query + `zeroResult` unmet-demand flag + coarse country/region geo + member), best-effort so a capture failure never affects the search response; authenticated `POST /api/analytics/events` (`AnalyticsIngestController`; gateway static route `/api/analytics/**`; `API_ACCESS`; owner-stamped from `X-User-Id`; 100-event batch cap) for client page-views / assistant-questions / UTM. `AnalyticsCaptureService` runs each write in `REQUIRES_NEW` and swallows errors (telemetry never breaks the product); clamps field widths + future `occurredAt`. **Retention from day one**: `AnalyticsRetentionSweep` (@Scheduled hourly, **dry-run default** `kelta.analytics.retention.dry-run`, `FOR UPDATE SKIP LOCKED`, batched, metric `kelta_worker_analytics_purged`) prunes rows older than `kelta.analytics.retention.max-age-days` (90) unscoped via admin-bypass. Real-DB `AnalyticsCaptureScenarioTest` proves RLS isolation + retention prune. | **Rollups & dashboards deferred** (`target-stats` / `member-metrics` / `query-clusters` + Superset — a later slice reads this table). **Anonymous homepage ingest deferred to the public read-surface slice** (a public write surface needs its per-IP budget + bot challenge first) — capture is authenticated + server-side only for now. `member_id` deletion/erasure of these rows is owed with the rollup slice |
| IP geolocation (enrichment, login geo, geo authz, per-record capture) | Gateway `io.kelta.gateway.geo`: per-pod GeoLite2-City mmdb, in-app `@Scheduled` downloader (`MAXMIND_LICENSE_KEY`, sha256-verified, atomic hot-swap, fail-open), `GeoEnrichmentFilter` (-45) → `gateway.geo` attr + trusted `X-Geo-*` headers (client-supplied ones stripped at -400) + `GatewayPrincipal.geoCountry`; shared `ClientIpResolver`; worker `GeoHeaders` parser + `GeoContext` ScopedValue; `login_history.geo_*` (V176) + LoginHistoryPage Location column; Cerbos `geoCountry` on BOTH principals with geo-suffixed decision-cache keys; per-collection `captureGeo` flag (V177) stamping `createdGeo`/`updatedGeo` JSONB system columns (FLS-exempt, version-diff-excluded, lazy `ADD COLUMN IF NOT EXISTS` on flag flip); harness `GeoCaptureScenarioTest`. See `architecture.md` → IP geolocation data flow | Live-stack geo-deny policy verification (harness Cerbos is allow-all); geo analytics/dashboards; country allow/deny list UI on top of `P.attr.geoCountry` |
| Telehealth — headless portal auth (slice 8) | Parent spec `specs/telehealth/README.md`, slice `specs/telehealth/8-portal-auth-headless.md`. **Slice 8 ships**: JSON API over the slice-1 magic-link machinery so a third-party frontend (a tenant's own portal site) can run login end-to-end. kelta-auth `PortalAuthApiController`: `POST /portal/api/login/request {email, tenant, redirectUri?}` — always 202 (enumeration-safe, same `PortalLoginService` rate limit/audit); optional `redirectUri` must **exactly match** the tenant allowlist (`tenant.settings.portalAuth.redirectUris`, read per-request — no cache, no NATS) else 400 `redirect_uri_not_allowed` (unknown tenant indistinguishable); the emailed link lands on the external site with `?token=`. `POST /portal/api/login/verify {token}` — consumes the single-use token (LOGIN and INVITE both) and mints an RS256 access token via the authorization server's `JwtEncoder` with the `KeltaTokenCustomizer` claim set incl. **`user_type`** (gateway `X-User-Type`) and `auth_method magic_link`; 8 h TTL, no refresh (v1); 401 generic otherwise. Worker: `GET|PUT /api/admin/tenant/portal-auth-settings` (`MANAGE_USERS` in-controller; PUT validates ≤10 absolute-https URLs — `http://localhost` ok — no fragments, `inviteRedirectUri` ∈ allowlist) and `PortalUserService` invite emails honor `portalAuth.inviteRedirectUri`. Thymeleaf pages + kelta-ui flow untouched. Tests: `PortalAuthApiControllerTest` (allowlist/tenant-oracle/claims), `PortalLoginServiceTest` (query-aware token append, allowlist read), `PortalAuthSettingsControllerTest` (validation + MANAGE_USERS), `PortalUserServiceTest` (invite override). **Security slice — not auto-merged.** | Refresh tokens; revocation of issued portal JWTs (TTL-bounded); allowlist authoring UI (JSON:API/admin endpoints only) |
| Telehealth — archival & retention (slice 7 of 7) | Parent spec `specs/telehealth/README.md`, slice `specs/telehealth/7-archival-retention.md`. **Slice 7 ships (V171)**: `telehealth-archives` system collection (one immutable row per archived source; `uq_telehealth_archive_source (tenant_id, source_type, source_id)`). **`ArchiveService`** turns a CLOSED chat conversation or ENDED video session into an encounter record — a canonical JSON transcript/summary (**SHA-256 pinned**; a self-contained serializer sorts object keys so identical input hashes identically regardless of Jackson) plus a `PdfTableWriter` render, both stored in S3 as `file_attachment` rows **OWNED by the archive row**; conversation message attachments are **re-parented** to the archive (pins them against live-message purge, cascades on archive delete via `AttachmentCleanupHook`). Idempotent (existing-row short-circuit + unique-index race catch); conversation flips to **ARCHIVED** (the `ChatMessageHook` already rejects writes on ARCHIVED); portal participant gets a `record_share`. **`TelehealthArchiveController`** on `/api/telehealth/**` (chat-idiom authz): `POST /archives` (staff, idempotent), `GET /archives` (staff sees tenant; PORTAL forced to own `portal_user_id`), `GET /archives/{id}` (presigned artifact URLs, audit `ARCHIVE_ACCESSED`, portal own-only), `POST /archives/{id}/legal-hold` + `GET|PUT /retention-settings` (**`MANAGE_DATA` only**; retention keys `archiveAfterDays`/`retentionYears`/`purgeLiveAfterDays` = 30/7/90 defaults all tiers, merged into `tenant.limits`). **`AutoArchiveSweep`** (`@Scheduled`, `FOR UPDATE OF c SKIP LOCKED`, per-tenant `archiveAfterDays` due-check) archives closed conversations; **`RetentionPurgeSweep`** (`@Scheduled`) is **DESTRUCTIVE but DRY-RUN by default** — `kelta.telehealth.retention.purge-dry-run:true` logs what it WOULD purge and deletes nothing; only when explicitly `false` does it delete artifact + linked recording S3 objects and stamp `purged_at` (**tombstone — the archive row is never hard-deleted**), skipping any `legal_hold` row (excluded in SQL AND re-checked per row); live `chat_message` rows purged `purgeLiveAfterDays` after archival (same dry-run gate). LiveKit `room_finished` → `archiveVideoSession` best-effort (never fails the webhook). UI: chat console **Archived tab** + read-only thread with "Archived … · retained until …" banner + PDF/JSON download (`ArchivedThreadBanner`); **Encounter record** card on the visit page; admin **Telehealth Retention** settings page (Setup → Integration → Telehealth Retention, `/telehealth-settings`) — retention form + archive list with confirm-gated legal-hold toggle (all writes `MANAGE_DATA`-gated). Audit `ARCHIVE_*`/`LEGAL_HOLD_CHANGED`/`RETENTION_SETTINGS_CHANGED`. Tests: `ArchiveServiceTest` (determinism/sha256/idempotency/retention-math/re-parent), `RetentionPurgeSweepTest` (dry-run purges nothing, real purge stamps+deletes, legal-hold skip, `normalizeKey`), `AutoArchiveSweepTest`, `TelehealthArchiveControllerTest` (MANAGE_DATA gates, portal own-only), `LiveKitWebhookServiceTest` (ENDED→archive best-effort); Vitest `TelehealthSettingsPage`/`ArchivedThreadBanner`; skip-gated `archival.spec.ts`. | Flip `purge-dry-run` to `false` only after operator backup-posture sign-off (concerns.md); e-discovery bundle export + transcript FTS are v2 (PHI stays out of FTS); WORM/object-lock is an operator infra option, documented not enforced |
| Telehealth — video backend (slice 5 of 7) | Parent spec `specs/telehealth/README.md`. **Slice 5 ships (V170 + homelab-argo#146)**: self-hosted **LiveKit** SFU — media never traverses the gateway; the worker only mints tokens and consumes webhooks. `video-sessions` system collection (lazy-created at first token request; one per appointment via partial unique index; ad-hoc per chat conversation; opaque `t_<tenantId>_<uuid>` rooms). **Token mint** (`LiveKitTokenService`, plain nimbus HS256 — iss=API key, sub=user, room-scoped `video` grant): `POST /api/telehealth/appointments/{id}/video-token` (participant + CONFIRMED + window start−15m…end+60m) and `POST /api/telehealth/conversations/{id}/video-token` (chat members) — fail-closed on the new **`telehealthEnabled`** gate (FREE false) and **`videoMinutesPerMonth`** governor (SQL month-sum of ended durations; 0/3k/30k/∞ by tier). **Webhooks** `POST /api/telehealth/webhooks/livekit` (unauthenticated path; LiveKit JWT signature + `sha256` body-digest verified; idempotent via `livekit_webhook_event` insert-as-claim): room_started/finished drive ACTIVE/ENDED + duration and publish **`kelta.video.session.<tenantId>.<sessionId>`** (NATS_TRIGGERED flows); egress_ended stamps `recording_key`. Config `KELTA_LIVEKIT_{URL,API_KEY,API_SECRET}` (dev defaults warn). Infra: compose `telehealth` profile + `make up-telehealth`; homelab-argo `livekit` app (hostNetwork, UDP 50000–50100, Traefik wss on livekit.{rzware.com,kelta.io}, **TURN-TLS disabled — patient-network release gate in concerns.md**). Audit `VIDEO_*` events. Tests: `LiveKitTokenServiceTest` (claims + webhook signature/digest), `VideoSessionServiceTest` (authz/window/feature/governor/lazy-create), `LiveKitWebhookServiceTest` (transitions/idempotency/egress). | recording egress START (webhook consumes `egress_ended` + `room_finished`→archive already wired, slice 7); TURN-TLS cert (patient-network release gate) |
| Telehealth — video UI (slice 6 of 7) | Parent spec `specs/telehealth/README.md`. **Slice 6 ships**: shared **`VideoRoom`** in `@kelta/components` (wraps `@livekit/components-react` + `livekit-client` + `components-styles`) — pre-join device check + camera-blocked troubleshooting, participant grid, control bar (mute/cam/screen-share/leave), connection quality, ended state; **lazy-loaded via a `@kelta/components/video` subpath entry** + `React.lazy` so the LiveKit bundle (~170 kB gz) is its own chunk, never in the base app chunk (build-verified: 0 livekit refs in `index`). Consumes slice-5 token endpoints verbatim (`useVideoToken`, empty body). Portal **`video-visit` page-builder widget** + **`/app/visits/:appointmentId` `VisitPage`** — upcoming card, **window±grace join gating** with live countdown (`visitWindow`), **waiting room** via `usePresence('visit:<appointmentId>')` (provider sees the patient), **consent screen** (`consentMachine`) → `POST /api/telehealth/sessions/{id}/consent {accepted}` (member-only, stamps `video-sessions.recordingConsent`, audit `RECORDING_CONSENT_CAPTURED`), always-visible recording indicator; staff **Join visit** (window-gated) on `/app/appointments` + **Start video** escalation on the chat console (conversation token → in-thread `VideoRoom`, no new endpoint); friendly 403/409/429 surfaces (`classifyVideoTokenError`). Tests: `VideoSessionServiceTest` consent cases; Vitest — `VideoRoom` (mocked LiveKit), `visitWindow`, `consentMachine`, `VisitJoinCard`, `videoVisit` widget; skip-gated `video-visit.spec.ts`. | Per-tenant "records visits" flag (consent currently config-driven via the widget); recording egress start (archival landed in slice 7); TURN-TLS cert (patient-network release gate) |
| Telehealth — scheduling & visit links (slice 4 of 7) | Parent spec `specs/telehealth/README.md`. **Slice 4 ships (V169)**: `telehealth-availability` (weekday RULE rows in a per-row timezone — wall-clock-stable across DST — + closed/additive-window EXCEPTION dates) + `telehealth-appointments` system collections (no object grants — admin-only generic JSON:API, chat precedent); **`/api/telehealth/**` static route** with in-controller authz (portal actors act on self, providers on own appointments; user types re-validated): providers/slots/book/list/cancel/complete. **Race-safe booking** — per-provider `pg_advisory_xact_lock` + `SlotService` re-check in one tx (no btree_gist), books as CONFIRMED, `AppointmentHook` validates + grants portal `record_share`; confirmation email with **RFC 5545 .ics attachment** (`IcsGenerator` + `queueEmailWithAttachments`) and a **stateless HMAC visit link** (`VisitTokenService`, `KELTA_TELEHEALTH_VISIT_SECRET`) — `GET /api/telehealth/visits/{token}` (unauthenticated path) re-checks the live row, mints a single-use 15-min portal login token, 302s into auth verify (one click email → signed-in `/app`). **Reminder sweep** `AppointmentReminderSweep` (60s @Scheduled, atomic UPDATE-claim on `reminder_sent_at`, multi-pod safe, T-60min configurable). UI: portal **`appointment-scheduler` widget** (provider select or fixed prop → 14-day slot grid → confirm + upcoming/cancel; editor mode static) + staff **`/app/appointments`** list (complete/no-show/cancel). Templates `appointment.confirmed/.reminder/.cancelled` (V169, tenant-overridable). Tests: `SlotServiceTest` (incl. DST spring-forward), `VisitTokenServiceTest`, `IcsGeneratorTest`, `AppointmentServiceTest`, `TelehealthSchedulingScenarioTest` (real stack: slots→book→409 double-book→cancel + share/RLS rows), widget Vitest, skip-gated `scheduling.spec.ts`. | Provider self-service availability UI **delivered** (`GET/PUT /api/telehealth/availability/me` + kelta-ui `ProviderAvailabilityPage` at `/app/provider-availability`, server-enforced self-ownership; expat-clinic spec §24); staff calendar view + richer visit UX (slice 6); recurrence + group visits (parent deferrals); REQUESTED/approval booking flows; per-tenant reminder offset |
| Telehealth — chat UI (slice 3 of 7) | Parent spec `specs/telehealth/README.md`. **Slice 3 ships**: staff **chat console** `/:tenant/app/chat` (Mine/Queue/All tabs — All gated on `MANAGE_CHAT`; claim/close; presence chip via `usePresence('chat:<id>')`; read receipts on thread focus; unread dots from new `myLastReadAt` field added to the conversation-list API); **portal `chat-panel` page-builder widget** (single-thread UX: newest writable conversation or start-against-queue; editor mode renders a static sample, no fetches/joins); shared **@kelta/components chat primitives** (`MessageList`/`MessageComposer`/`ConversationListItem`, presentational + prop-driven); `RealtimeClient` `joinConversation`/`leaveConversation` (ref-counted, re-join on reconnect, `chat.*` frames) + `chatQueryKeysForEvent` invalidation (rule holds — ids only, refetch over `/api/chat`); nav `/chat` path kind + bell aggregates approvals+unread chats; **hybrid liveness** — joined threads by socket invalidation, inbox/bell by 30s poll (conversation events only reach joined sockets by design); **slice-2 privacy fix** — `RealtimeBridge` skips + WS `subscribe` rejects `chat-*` collections (bodies were reaching tenant-wide subscribers); i18n `chat.*`; Vitest (components 9, client/invalidation/navTabs/widget) + skip-gated `chat-console.spec.ts`. | Composer attachments; offline outbox for sends (draft-kept-on-failure v1); typing indicators; tenant-wide id-only "conversation opened" ping for instant inbox liveness; queue picker in the widget inspector |
| Telehealth — chat backend (slice 2 of 7) | Parent spec `specs/telehealth/README.md`. **Slice 2 ships (V168)**: four chat system collections (`chat-queues`/`chat-conversations`/`chat-messages`/`chat-participants`, RLS'd tables, FK cascade, NO object-permission rows — generic JSON:API stays admin-only); scoped **`/api/chat/**`** static route with all authz in-controller (participant membership fail-closed from `X-User-Id`+`X-User-Type`; INTERNAL-only queue view/claim; new **`MANAGE_CHAT`** permission for `view=all`/cross-assign, V168 seed to SysAdmin + provisioning + FE catalog pending slice 3 UI); writes via `QueryEngine` so `ChatMessageHook` (participant sender, open conversation, immutable messages, ids-only `kelta.chat.message.*` publish + `last_message_at` bump) and `ChatConversationHook` (`kelta.chat.conversation.*` lifecycle) fire on every path; portal participants auto-granted `record_share` via `ParticipantShareSupport`; **conversation-scoped WS routing** — `chat.join`/`chat.leave` verified against `/internal/chat/.../members` (reactive 30s-cached fail-closed `ChatMembershipClient`), per-conversation index on `SubscriptionManager` (20 joins/session), `ChatMessageBridge` fans ids-only events to joined sessions only. Message bodies never ride NATS/WS/search. Audit `CHAT_*` events. Tests: `ChatServiceTest`/`ChatMessageHookTest`/gateway routing+bridge tests/`ChatBackendScenarioTest` (real stack). Playbook §7 recipe added. | Chat UI is slice 3 (console, widget, realtime client actions, unread); typing indicators; queue-membership modeling. Retention/purge landed in slice 7 (`chat_message` rows purged after the encounter archive exists — see the slice-7 row + concerns.md). |
| Telehealth — portal identity (slice 1 of 7) | Parent spec `specs/telehealth/README.md`. **Slice 1 ships (V167)**: `platform_user.user_type` INTERNAL\|PORTAL (JSON:API `users.userType`, immutable, UsersPage Type badge); seeded **Portal User** profile (API_ACCESS only; V167 backfill + `TenantProvisioningHook`); **passwordless magic-link login** on kelta-auth (`/portal/login[,/request,/verify]` Thymeleaf pages; single-use SHA-256-hashed tokens in `portal_login_token` — 15-min PORTAL_LOGIN rate-limited 3/window, 7-day PORTAL_INVITE; enumeration-safe uniform responses; portal users have **no `user_credential` row** so form login is structurally impossible; MFA not interposed — link possession is the factor); `POST /api/admin/users/portal-invite` (`MANAGE_USERS`, re-invite idempotent) + SDK `admin.users.invitePortal` + UsersPage invite dialog; **`maxPortalUsers` governor** enforced at invite (25/1k/10k/∞ by tier); JWT `user_type` claim → gateway `X-User-Type` (stripped chain-head, re-stamped); `ParticipantShareSupport` (idempotent `record_share` USER grants, `reason='participant'`) ready for slices 2/4 hooks. Audit: `PORTAL_USER_INVITED`, `PORTAL_LINK_REQUESTED`, `PORTAL_LOGIN`. Tests: worker+auth unit, `PortalIdentityScenarioTest` (real Postgres), skip-gated `portal-login.spec.ts`. **Security slice — not auto-merged.** | Slices 2–7: chat backend/UI, scheduling + visit links, LiveKit video, archival & retention (see the parent spec). Custom-domain invite links use the platform issuer URL in v1 |
| Delegated administration | Full admins define **delegated-admin scopes** (`delegated-admin-scopes` system collection, `delegated_admin_scope` table V157, RLS) so listed non-admin users manage users *within limits* without `MANAGE_USERS`. New **`MANAGE_DELEGATED_ADMINS`** system permission (V157 seeds it onto every `MANAGE_USERS` profile; `TenantProvisioningHook` seeds new tenants; frontend `SystemPermissionChecklist` catalog entry). **Scope CRUD**: `DelegatedAdminScopeController` (`/api/admin/delegated-admin-scopes`, `MANAGE_DELEGATED_ADMINS`-gated) writing through `QueryEngine`; **`DelegatedAdminScopeValidationHook`** rejects scopes whose manageable profiles grant any privileged permission (`PrivilegedPermissions.SET`) — no delegating admin-of-admins — plus id-existence + array shape/caps. **Scoped ops**: `DelegatedUserAdminController` (`/api/admin/delegated`) — `GET /me` (uncached summary), `GET /users` (server-filtered to in-scope profiles), `POST /users` (`canCreateUsers`, forced `PENDING_ACTIVATION` + invite), `PATCH /users/{id}` (field whitelist; email/managerId/mfa immutable; self-edit blocked; status ACTIVE↔INACTIVE only under `canDeactivateUsers`), invite, reset-password (`canResetPasswords`). `DelegatedAdminService.effectiveScope` reads scopes fresh (no cache/NATS) and **re-filters out now-privileged profiles at request time** (TOCTOU-safe). Validated writes run inside `DelegatedWriteContext`. **`IdentityCollectionGuardHook`** (wildcard BeforeSaveHook, incl. new `beforeDelete` SPI) requires `MANAGE_USERS`/`MODIFY_ALL_DATA` (or `MANAGE_DELEGATED_ADMINS` for scopes) on any identified HTTP write to `users`/`group-memberships`/`delegated-admin-scopes` unless a `DelegatedWriteContext` is bound — **closing the previously-unauthorized `POST /api/operations` write path** for identity collections. Gateway `IdentityHeaderStripFilter` strips client-supplied `X-User-Email`/`X-User-Profile-Id`/`X-User-Profile-Name`/`X-Cerbos-Scope` (+ the older forwarded-user headers) chain-head. Hardened the pre-existing unauthenticated `AdminInviteController` + `PasswordResetAdminController` with `MANAGE_USERS`. **UI**: `DelegatedAdminsPage` (scope CRUD, `/delegated-admins`, `MANAGE_DELEGATED_ADMINS`) + `UsersPage` **scoped mode** (delegated data source, profile-limited create, capability-gated actions, compact edit dialog) gated by `RequireUserManagementAccess` (MANAGE_USERS OR delegated) via `useDelegatedAdmin`. SDK `admin.delegated`/`admin.delegatedAdminScopes` groups. Tests: 77 worker unit + gateway filter + runtime `beforeDelete` registry + `DelegatedAdminScenarioTest` (real Postgres) + Vitest. **Security feature — not auto-merged.** | Delegated `UserDetailPage` mode (v1 edits via a modal; `/users/:id` stays MANAGE_USERS-only); delegated group-membership management; junction collections if the JSONB arrays outgrow indexing; ~~broader per-collection authorization for `/api/operations` beyond identity collections~~ **DONE (2026-07-05)** — `AtomicOperationsController` mirrors the gateway's per-collection Cerbos verb check via the new `CerbosAuthorizationService.checkCollectionAccess` (object-level, UUID-keyed), denying the whole batch fail-closed before execution; positive Playwright e2e post-deploy (live env) |
| Quick actions | `quick-actions` system collection + `quick_action` table (V151, RLS); `useQuickActions` fetches the active actions for a collection and maps them onto `QuickActionDefinition`. `QuickActionsMenu` handlers: `create_related` (navigate), `run_script` (→ `POST /api/scripts/{id}/execute`), **`update_field` real PATCH** (setValue **or interactive value dialog** — `UpdateFieldValueDialog` renders the typed `FieldControl` editor for the field, or a select over `allowedValues`, pre-filled from the record, coerce+validate before PATCH), **`log_activity` real note POST**, **`send_email` real send** (FE resolves `recipientField` from the record → `POST /api/email/send` with the admin `templateId` + record merge context; endpoint gated on `MANAGE_EMAIL_TEMPLATES`, rate-limited, admin-template-only), **`custom` renders a `componentRegistry` plugin component** by `CustomActionConfig.componentName` (explicit hint when unregistered). Harness scenario for CRUD | — |
| Record types | Data model + Record Type editor + **runtime enforcement**: `RecordTypeEnforcementHook` (BeforeSaveHook) validates picklist values against the record type's allowed set and applies type-specific picklist defaults on create; invalid `recordTypeId` rejected. (Verified 2026-07-02; prior 🔴 "no runtime enforcement" was stale.) | Layout-per-record-type *association* is metadata-driven via `usePageLayout(recordTypeId)` (client), not a server guard — enhancement only |
| Activity timeline | `ActivityTimeline` merges record create/update, approval events, sharing changes, notes, attachments, **flow runs** (`GET /api/flows/record-executions?recordId=` — executions by `trigger_record_id`, joined for flow name), and **emails** (`GET /api/email/logs?recordId=` — `email_log.source_id`; the Quick-Action send now stamps `recordId` as `source_id`) into one reverse-chronological feed on record detail. **Placement (2026-07-17): docked in the left section panel below the `RecordSectionNav` list on lg+ (`section-nav-activity`); on smaller screens and no-layout pages it renders in the main column below the field sections (same element mounted twice, React Query dedupes) — no longer below the tab bar** | — |
| Reports & dashboards | `ReportExecutionController`/`Service` build a real dynamic query from the report def (columns/filters/group/sort/paginate, 30s timeout); **CSV export ships** (`GET /api/reports/{id}/export?format=csv` → `exportCsv`); **scheduled report delivery ships** (V154 `scheduled_job.config` JSONB `{recipients:[…]}` — `ScheduledJobsPage` REPORT_EXPORT form has the recipients field; `ScheduledJobExecutorService` emails the CSV as a real attachment via `DefaultEmailService.queueEmailWithAttachments` [`EmailMessage.attachments` + SMTP multipart, new]; >10 MB → notification without attachment; delivery failure never fails the job, noted on the execution log); `DashboardDataController`/`Service` (653 lines). Gateway-routed. **PDF export ships** (`?format=pdf` → `ReportExecutionService.exportPdf` over Apache PDFBox 3 [`PdfTableWriter`: landscape A4, title + repeated bold header + page-number footer, per-column truncation, page-at-a-time layout]; `AnalyticsPage` Reports section lists reports with per-report Export CSV / Export PDF via `apiClient.getBlob`). **Authorization ships (2026-07-08, app-surfacing slice 1)**: all four execute/export/data endpoints gated in-controller on **`VIEW_ANALYTICS`** (`MANAGE_REPORTS`, the authoring permission, also passes) — new system permission (V162 backfill for existing profiles + `TenantProvisioningHook` for new tenants + FE `SystemPermissionChecklist` entry); granted by default to every built-in profile except Minimum Access; scheduled report delivery calls the service layer and is deliberately ungated (system-trust tier). Real-DB guard: `AnalyticsAuthzScenarioTest`. **End-user viewer ships (2026-07-08, app-surfacing slice 3)**: `/app/analytics` hub + `/app/dashboards/:id` (`DashboardViewPage` — CSS-grid joined client-side from `dashboard-components` rows; metric/chart/table/recent renderers; preset `timeRange`; per-widget masked-field error states; chart drill-through via `buildListUrl` into `ObjectListPage`) + `/app/reports/:id` (run/paginate/export CSV+PDF) — the FIRST UI consumer of `DashboardDataController`. Routes `RequirePermission(VIEW_ANALYTICS)`-gated (BE gate from slice 1); nav gains `dashboard`/`report` menu path kinds + a mobile Analytics group + a permission-gated UserMenu entry. **Per-widget time range override ships (2026-09-15)**: `dashboard-components.config` gains `ignoreTimeRange: true` (state widgets like "tasks in progress" skip the page's time filter entirely) and `fixedTimeRange: TODAY\|7D\|30D\|90D\|1Y` (widget always filters on its own range) — `DashboardDataService.buildTimeFilters` precedence is `ignoreTimeRange` > `fixedTimeRange` > runtime `timeRange` > `config.timeRange`; components using neither key are unchanged. `DashboardViewPage` shows a small "All time"/range chip on any opted-out widget so the page range's non-applicability is visible. **Chart groupBy on a lookup resolves a name (2026-09-15)**: `DashboardDataService.executeChartWidget` resolves a LOOKUP/MASTER_DETAIL `groupByField`'s raw ids to the target collection's display value via one batched `id IN (...)` query (capped at `maxGroups`) instead of showing raw UUIDs as bar labels — `point.label` carries the resolved name, `point.key` the raw id for drill-through; falls back to the id when the target's display field is masking-denied for the viewer. `ChartWidget` drills through on `point.key ?? point.label`, so cached payloads predating this change (no `key`) still work. Non-reference `groupByField`s are unchanged | No native dashboard authoring UI (author via the generic resource browser until the builder work); `accessLevel`/folder sharing not enforced |
| Approval processes | `ApprovalController` (submit/approve/reject/recall/status/history) + `ApprovalService` (581 lines) + `SubmitForApprovalActionHandler` registered `@Bean`. **Record locking ships**: `ApprovalRecordLockHook` (BeforeSaveHook) blocks a write when the record has a PENDING approval with `record_editability='LOCKED'` (system collections excluded); `ApprovalService.isRecordLocked`. Gateway-routed. **End-user surface + actor hardening ship (2026-07-08, app-surfacing slice 2)**: `/app/approvals` inbox (`ApprovalsInboxPage` — pending-on-me via `approval-step-instances` `filter[assignedTo]`+includes, my-submissions via `approval-instances` `filter[submittedBy]`; approve/reject with comment dialog, recall), record-detail submit-for-approval header action + pending/lock badge (`useRecordApprovalState`), `TopNavBar` bell = live pending count (its first real feed), user-menu entry. **Actor identity hardened (security)**: all four write endpoints resolve the actor from gateway-stamped `X-User-Id` → `UserIdResolver` → UUID (fail-closed 403; body `userId`/`submittedBy` now inert) — this also FIXED the latent header-email-vs-UUID mismatch that made the spoofable body field the only functional path. New `GET /api/me/identity` (canonical userId UUID + email + profileId; JWT `sub` is UUID only on direct-login, email on auth-code). `ActivityTimeline` approval fetch now server-side-filtered (was list-all + client filter). `ApprovalActorScenarioTest` real-stack guard. **Gateway route fix**: `/api/approvals/**` static route was never registered (only `approval-processes` — the action endpoints were unreachable via the gateway despite the prior "Gateway-routed" claim); registered in `RouteConfigService` + `RouteInitializer` in this slice. Timeline inline approve/reject deferred (inbox/header/bell cover the flows) | Timeline inline approve/reject buttons; approval read-visibility restriction (see concerns.md); queue-approver inbox semantics |
| Bulk data operations | `BulkOperationsController` + `BulkJobProcessorService` (real `SELECT FOR UPDATE SKIP LOCKED` poller → `BulkOperationService.processJob`, INSERT/UPDATE/UPSERT/DELETE). **File upload ships end-to-end**: `POST /api/bulk-jobs/upload` (multipart) parses a CSV/JSON file → records → queued job (`CsvImportService.parseCsvToRecords`), and `BulkJobsPage` has the file picker (file present → FormData upload path; absent → inline JSON path unchanged). **Results download ships**: `GET /api/bulk-jobs/{id}/results/download` streams all per-record results as CSV (paged repository reads, attachment header); FE "Download results" button on COMPLETED / FAILED-with-progress jobs. Gateway-routed | — |
| Unified Record Experience (spec) | Two record stacks exist today — admin **Resource Browser** (`/:tenant/resources/*`: `Resource{Browser,List,Detail,Form}Page`) and end-user runtime (`/:tenant/app/o/*`: `Object{List,Detail,Form}Page` under `EndUserShell`). Both form pages already share `LayoutFormSections` + `useLayoutRules` (client rule engine: COMPUTE/VALIDATE/DEFAULT/TRANSFORM/SCRIPT on CHANGE/BLUR/LOAD/BEFORE-SAVE). Display via `FieldRenderer` (21 types); server validation (field constraints + formula rules + `BeforeSaveHook` + `FormulaComputeHook`); flow record-change automation (`FlowEventListener`); GraalVM script engine present (`GraalVmScriptExecutor`, reachable only from flows). **Parent spec `specs/unified-record/README.md` + 8 slice child specs authored. Slice 1 SHIPPED: `FieldControl` registry (`kelta-ui/app/src/components/fieldControl/`) — `getFieldControl(type)`→`{View,Edit,InlineEdit,coerce,validate,editable}` for all 23 field types, incl. new edit/inline controls for reference/multi_picklist/json/rich_text/geolocation; View delegates to `FieldRenderer` (parity + plugin override); 25 Vitest. Not yet consumed by pages (slices 3–4).** **Slice 2 (partial) SHIPPED: in-place inline field edit on record detail — `InlineFieldValue` (registry-driven view↔edit↔commit, click-to-edit, per-field validation, reject→inline error) + opt-in `editable`/`onFieldCommit` on the shared `LayoutFieldSections`, wired into BOTH `ObjectDetailPage` (end-user, `permissions.canEdit`) and `ResourceDetailPage` (admin) so behavior is identical; partial JSON:API PATCH per field. Types needing loaded options (picklist/reference/multi) inline-edit only once options are present, else view-only; rich_text edits via full form. Full `RecordShell` frame + section-renderer consolidation still pending.** **Slice 3 SHIPPED: in-place cell edit in the list grid — opt-in `editable`/`onCellCommit` on `ObjectDataTable` renders `InlineFieldValue` in editable cells (stopPropagation so editing doesn't navigate), wired into `ObjectListPage` (gated on `permissions.canEdit`); partial JSON:API PATCH per cell.** **Slice 7 (backend, SECURITY) SHIPPED: tenant record-event scripts — `record-scripts` system collection (`record_script`, V149, RLS) + `RecordScriptHook` (wildcard `BeforeSaveHook`, order 300) runs active scripts per collection+event through the sandboxed `GraalVmScriptExecutor`. `BEFORE_*` may block a write (`{error,field}` -> per-field error, fail-closed) or return field updates to merge; `AFTER_*` = side effects. 60s TTL cache per collection. 6 worker unit tests.** **Slice 5 (backend) SHIPPED: optimistic locking — single-record `GET` emits an opaque `ETag` (base64 of `updatedAt`) via `DynamicCollectionRouter`; `PATCH`/`PUT`/`DELETE` with a stale `If-Match` throw `StaleWriteException` → **409** (`GlobalExceptionHandler`); absent `If-Match` = unchanged (back-compat). No migration (version = `updated_at`). 5 router unit tests.** **Slice 5 (frontend) SHIPPED: `apiClient.getWithMeta` captures the record `ETag`, surfaced by `useRecord`; base `patch`/`put`/`delete` + `useRecordMutation` accept `ifMatch`; both detail pages (`ObjectDetailPage` end-user, `ResourceDetailPage` admin) echo `If-Match` on inline-field PATCH and, on a `409`, reload the latest instead of clobbering the concurrent edit. +9 apiClient tests (getWithMeta + If-Match on patch/put/delete).** **Slice 6 SHIPPED: client `SCRIPT` layout-rule kind — `RuleEngine`/`useLayoutRules` gain a `script` kind (sdk `ScriptLayoutRule`) evaluating a sandboxed `@kelta/formula` expression on `onLoad`/`onChange`/`onBlur`/`onBeforeSave`; a non-empty result surfaces as a message on the target field (or form-level) and, on `onBeforeSave`, blocks submit — riding the existing `runBeforeSave` gate both form pages already call (no new write-path wiring). Boolean `true` uses the rule's static `message`; runtime errors fail open (server is authoritative). Editor in `PageLayoutsPage/RulesEditor.tsx`; persisted in `layout_rule.body` as `{expression,message}` with `kind=SCRIPT` (added to the system-collection enum, no migration). Aligned to the real `onBeforeSave` event (spec's `onBeforeSubmit`). Client-only UX — the server record-event hook (slice 7) is the authoritative gate. +8 RuleEngine, +5 mapper, +1 editor Vitest.** **Slice 4 SHIPPED: related-list inline CRUD — the shared `RelatedList` (rendered by `DetailTabBar` for BOTH stacks) gains opt-in inline edit (per-cell `InlineFieldValue`, pencil affordance), row delete (inline confirm), and inline create (registry `Edit` controls, parent FK pre-filled from the relationship's `foreignKeyField`), each gated on the child collection's own `canEdit`/`canDelete`/`canCreate`. Child mutations owned via `useRecordMutation`, refreshing the API-fetch path (`refetch`) and the parent-included path (`onChanged`→parent refetch). No backend change — reuses JSON:API child `POST`/`PATCH`/`DELETE`. Mass-edit deferred (needs a bulk backend). +3 RelatedList Vitest.** **Slice 8a SHIPPED (foundation, not yet consumed): `RecordShell` (variant-driven page skeleton — loading/status branches + breadcrumb→header→(body+rail)→tabBar→belowTabs→dialogs slots) + `RecordDetailBody` (shared section renderer: `LayoutFieldSections` when the layout has sections, else a variant fallback slot) in `kelta-ui/app/src/components/record/`. Both detail pages convert to thin `variant` wrappers over these in 8b/8c. +7 Vitest.** **Slice 8b SHIPPED: `ObjectDetailPage` (end-user) is now a `variant="enduser"` `RecordShell` — loading/error/not-found via `statusSlot`; breadcrumb (Crumb + inspect badge), header (RecordHeader + QuickActions), rail (rail blocks / system MetadataCard), and `DetailTabBar` as slots; body via `RecordDetailBody` (highlights+details fallback preserved, `data-testid="field-values"` intact). Behavior unchanged; no longer imports `LayoutFieldSections` directly.** **Slice 8c SHIPPED: `ResourceDetailPage` (admin) is now a `variant="admin"` `RecordShell` — breadcrumb + header (RecordHeader + RecordActionsBar) as slots, body via `RecordDetailBody` (admin field-grid fallback) plus the Sharing panel, `DetailTabBar` as `tabBar`, `ActivityTimeline` as `belowTabs`, Share/Delete dialogs as `dialogs`; loading/error/not-found early-returns kept verbatim. All 24 ResourceDetailPage tests green. Both detail stacks now render ONE `RecordShell`.** **Slice 8d (docs) SHIPPED — unified-record effort COMPLETE (slices 1→8): both detail stacks render one `RecordShell`/`RecordDetailBody`; docs updated (architecture.md single record-detail path, conventions.md `RecordShell` reuse rule). No dead components to delete — the duplication was inline page JSX, now folded into shared `RecordShell`/`RecordDetailBody`/`DetailTabBar`; the kelta-web `LayoutRenderer` (display-only related-list stub) is a legacy public `@kelta/components` export, deprecation deferred; the cross-cutting Playwright route-parity e2e is a post-deploy follow-up (E2E runs against a live env).** **List shells unified (2026-07-04): new `ListShell` (`components/record/ListShell.tsx`, RecordShell-style variant/slots — statusSlot/breadcrumb/header/toolbar/filters/table/pagination/belowTable/dialogs, isLoading > statusSlot > frame) consumed by BOTH `ObjectListPage` (enduser) and `ResourceListPage` (admin), all features/testids preserved; +6 Vitest.** **Related-list mass edit ships (2026-07-04)**: `RelatedList` gains opt-in row selection + "Edit field…" (typed FieldControl editor) submitting ONE bulk job (`POST /api/bulk-jobs` UPDATE, records `[{id, field: value}]`) and polling it to a terminal status — no more N-sequential-PATCH constraint. **Table dedupe DONE (2026-07-05)**: admin `ResourceListPage` now renders the shared `ObjectDataTable` (the same grid the end-user `ObjectListPage` uses) instead of its hand-built `<table>` — the last table-level duplication. Admin chrome (filter builder, column selector, saved views, export dropdown, summary stats, pagination) stays in `ResourceListPage` as `ListShell` slots; only the grid body is shared. `ObjectDataTable` gained an optional `onRowClick` prop so the admin routes row activation to `/{tenant}/resources/{collection}/{id}` (falls back to the end-user `/app/o/...` route when omitted — zero change for `ObjectListPage`). Inline edit is now the shared `InlineFieldValue` (via `editable`+`onCellCommit`, gated on the existing inline-edit toggle), replacing the legacy `InlineEditCell`; cells render via `FieldRenderer` with the page's existing `useLookupDisplayMap`. The separate admin `id` column was dropped (matching `ObjectListPage`; the `id` visibility flag still governs CSV export). e2e (`resource-browser-crud.spec.ts` + `resource-list.page.ts`) already target `<table>`/`tbody tr`, unchanged; +2 `ObjectDataTable` rowClick Vitest. **Left section nav ships (2026-07-17)**: `RecordSectionNav` (`components/record/`) — left panel via a new `RecordShell` `sectionNav` slot (grid `280px + body [+ 340px rail]`, hidden < lg; the pages own the sticky/scrollable wrapper), listing the resolved layout sections (label + field-count badge, counts mirror `LayoutFieldSections` skip-missing logic via `sectionNavItems.resolveSectionNavItems`); entries smooth-scroll to per-section anchors (`record-section-{id}` wrappers in `LayoutFieldSections`) with an IntersectionObserver scrollspy active state. `ActivityTimeline` moved out of `belowTabs`: docked in the panel below the nav list on lg+, main-column fallback on smaller screens / no-layout pages, on BOTH stacks. Rendered only when the layout has ≥1 resolvable section (fallback bodies have no anchors). e2e `record-section-nav.spec.ts` (+ `DataFactory.createDetailLayout`) | **Remaining (post-convergence):** options-dependent inline editors (picklist/reference/multi) edit only once their options are loaded; `rich_text` edits via the full form; kelta-web `LayoutRenderer` legacy stub (separate SDK deprecation); cross-cutting parity e2e (post-deploy). **Detail-stack slice plan 1→8 DONE.** |
| Extensibility / modules | `KeltaModule` SPI, 3 compile-time modules, per-tenant lifecycle w/ NATS propagation, `@kelta/plugin-sdk`. **Runtime JAR loading is real** (not a placeholder): `ModuleJarService` (S3 upload/download + local cache + SHA-256 checksum), `SandboxedModuleClassLoader` (child-first `URLClassLoader` with a platform-package allowlist — module classes can't reach disallowed platform internals), `RuntimeModuleManager` (install/enable/disable/uninstall + per-tenant load via the sandbox classloader; `installModuleWithJar` → S3 + `tenant_module` record). **JAR signature verification (Rec 9)**: `ModuleSignatureVerifier` checks a detached publisher signature over the JAR bytes against a trusted X.509/SPKI public key (`kelta.modules.signing.public-key`, PEM; Ed25519 default, SHA256withRSA/ECDSA supported) **before** any S3 upload or DB write; enforced when a key is configured (unset → no-op, logged), so only publisher-signed modules install. `POST /api/modules/install-jar` takes an optional `signature` part → 403 on failure. **Load-time re-verification ships** (V155 `tenant_module.jar_signature`): the install-time signature is persisted (`ModuleStore.saveJarSignature`) and every `loadModule` re-checks the downloaded JAR — SHA-256 must match the install checksum, and when a signing key is configured the stored signature must re-verify — before any classloading. **Failure now quarantines rather than stubbing** (2026-08-31): a JAR that fails signature, checksum or classloading registers handlers that throw `ModuleUnavailableException`, and the module records `QUARANTINED` with the reason (`tenant_module.last_error`, V189). Previously it fell back to stub handlers returning `ActionResult.success({"status":"EXECUTED","mode":"stub"})` while reporting `ACTIVE`, so a cryptographically rejected module was indistinguishable from a working one and flows calling it passed. Handlers are registered rather than left absent so a flow step fails attributably instead of as `ResourceNotFound`; the exception is thrown rather than returned because `TaskStateExecutor` gives every failed `ActionResult` the generic code `ActionFailed`. A manifest-only module quarantines for the same reason; stub mode survives only as the explicit `kelta.modules.stub-mode` dev opt-in, reported as `STUB`. `GET /api/modules/{id}/health` exposes `isLoaded()` per pod (it existed with no endpoint returning it, so the API reported a DB column while loading is per-pod). `module_provisioned_resource` (V190) records what each module **created** vs **adopted**, with a content hash, so uninstall can eventually act safely. **Plugin-component visibility ships**: the app `componentRegistry` singleton + plugin-sdk `ComponentRegistry` gain `list*()` methods, and `ModulesPage` renders a live "Plugin Components" card (field renderers / page components / quick actions / column renderers with counts + name chips; explicit empty state). **A runtime-loaded module can now own hooks and collections, not just action handlers**: (a) `BeforeSaveHookRegistry` gained a tenant dimension (`registerTenantHook`/`removeTenantHooks`, plus `getHooks(tenantId, collection)`/`hasHooks(tenantId, collection)` overloads that put tenant hooks ahead of global ones) and `RuntimeModuleManager` registers a JAR module's `getBeforeSaveHooks()` per installing tenant on load and removes those exact instances on unload — hooks are held by identity because a module may return fresh objects per call. `DefaultQueryEngine`'s delete pre-fetch guard uses the tenant-aware `hasHooks` (via `TenantContext`), or a module hook would be silently skipped on delete. (b) `ModuleManifest` gained a `collections` list (`CollectionManifest`/`FieldManifest`) and `ModuleCollectionProvisioner` creates them at install through `queryEngine.create` on the `collections`/`fields` system collections — the same path an admin or `ExternalEntityMaterializer` uses, so the NATS config broadcast + lifecycle init fire normally. **No module-owned DDL or Flyway**: a module gets exactly the schema powers the admin API already exposes. Existing collections are skipped (reinstall/upgrade never clobbers live schema or data) and uninstall does not drop them; a malformed manifest is rejected before anything is created. (c) **Module webhooks**: one generic platform-owned route `POST /api/modules/webhooks/{tenantId}/{moduleId}` (`ModuleWebhookController`) dispatches the raw body + signature headers to the `ActionHandler` the manifest names in `webhookHandlerKey` — this is what replaces a per-module `@RestController`. **Unauthenticated by design and the platform verifies nothing**: the module resolves its own credential and checks the signature (the raw body is passed verbatim, since re-serializing breaks an HMAC). Nothing-to-dispatch-to returns a uniform 404 so a caller cannot enumerate a tenant's modules; a handler rejection is 401, a thrown handler 500. Registered in gateway `unauthenticated-paths` + `IpRateLimitFilter` (300/window, same as billing webhooks). (d) **Module UI**: a manifest `uiBundlePath` names a browser bundle inside the same JAR, served by `GET /api/modules/{moduleId}/ui-bundle.js` (scoped to the caller's ACTIVE modules, checksum + signature re-verified on read) and evaluated by `services/moduleUiBundles.ts` via a blob URL — its top-level code calls `ComponentRegistry.registerPageComponent`, making this the **first production caller** of the plugin-sdk registry. Opt-in via `<PluginProvider loadModuleBundles>`, enabled only by `App.tsx` so shared test wrappers issue no extra `GET /api/modules`. ⚠️ Same-origin: the bundle runs with the admin session's full DOM/cookie access and the JAR signature is the entire trust model. **(e) Module-published services**: `KeltaModule.getServices()` returns platform-defined-port → implementation, registered tenant-scoped in `ModuleServiceRegistry` (runtime-core) on load and withdrawn by identity on unload. This is the **reverse** of `ModuleContext` and the only way platform code can ask a module a question *inline* — handlers and hooks are dispatch-only, and no Spring bean can reach through `SandboxedModuleClassLoader`. Callers resolve per call (`find(tenantId, Port.class)`) and fall back to compiled-in behaviour when empty, so publishing nothing changes nothing. The port must be the **platform's** interface: a copy bundled in the JAR is refused at registration, since a child-first loader would otherwise yield two same-named classes failing later as a `ClassCastException`. Two modules cannot claim one port per tenant (refused, not last-write-wins, so behaviour never depends on load order), and a refusal withdraws that module's already-accepted ports. **First real port**: `EntitlementProvider` (+ the shared `MemberEntitlements` record, moved into runtime-core so the platform and a module cannot drift on how a tenant-authored value coerces to an int/bool/list). `ModuleAwareEntitlementService` (`@Primary`) prefers a tenant's module provider and falls back to the compiled-in `EntitlementServiceImpl`, so `AlertDispatchService`, `WatchController` and the quota hook are unchanged and a tenant without the module behaves exactly as before. **The platform binds `TenantContext` (id AND slug) around the call** — a module cannot resolve a slug, and a null slug reads the public schema instead of the tenant's rather than failing; an unresolvable slug refuses the module path instead of answering from the wrong data. Module answers are never cached (a module cannot consume the NATS invalidation), and a module that throws or returns null falls back rather than taking the alert path down. **Running on prod since 2026-08-31**: `kelta-billing` is installed and ACTIVE on the `spotopened` tenant, publishing `EntitlementProvider` on all pods, and a compiled-in Spring controller (`GET /api/billing/me`) resolves a member's plan through it — verified against data that exists only in the module's collections, so the answer could not have come from the compiled-in fallback. The compiled-in billing code and its tables remain in place; `POST /api/modules/kelta-billing/disable` reverts resolution instantly | "Kelta Exchange" marketplace catalog (browse/install listings) not built; a module still cannot register a new `CredentialType` (that registry is built once at boot from a Spring `List<CredentialType>`) or a new `@RestController` (Spring MVC resolves routes at context start) — but it **can** now declare HTTP routes in its manifest, dispatched by the platform's `ModuleHttpController` under `/api/modules/{moduleId}/x/**`, which is what let billing's member endpoints leave the platform. Module UI has no browser-side isolation (no iframe/CSP sandbox). **`kelta-modules/billing` is LIVE-VERIFIED** (2026-08-31, sandbox tenant `billing-stripe-verify`, real Stripe test-mode keys): checkout → payment → signed webhook `200` → subscription mirrored `active` with a non-null `currentPeriodEnd` → `resolve-entitlements` returning `PRO`/`watches: 25` → portal session → pass expiry → the wildcard quota hook rejecting a create past the entitlement limit on an arbitrary (non-billing) collection. Verified against a JVM worker image — a native image cannot execute an uploaded JAR and every module silently becomes inert stubs. **The compiled-in `kelta-worker` billing still stays in place**: cutover is gated on the behavioural differences below, not on the module working. **Cutover is blocked on more than behaviour**: `EntitlementService` is called synchronously by compiled-in, non-billing code — `AlertDispatchService` (alert fanout hot path), `WatchController` (watch limit + entitled channels) and `FlowConfig`'s quota-hook wiring — which a module could not serve until `getServices()` existed; and `/api/billing/{plans,me,checkout-sessions,portal-sessions}` is consumed by the **external** `app.spotopened.com` frontend (nothing in `kelta-ui` calls it), which a module now provides through manifest-declared HTTP routes under the platform-owned `/api/modules/{moduleId}/x/**` prefix (`ModuleRouteRegistry` + `ModuleHttpController`) — a module still cannot register a `@RestController`, but no longer needs to. Prod also carries a live subscriber in the compiled-in tables while the module owns parallel collections, so cutover includes a data migration. Empirically, prod has **zero** entitlement rules, so the `appliesTo=PORTAL` gap below is theoretical there today. **Permanent behavioural differences given the module boundary**: entitlements are uncached (a module cannot consume the NATS invalidation, and a stale entitlement is worse than a slow one); pass expiry is an action scheduled by a flow, not a `@Scheduled` bean (a module has no scheduler — safe because expiry is read-time); the quota hook **skips `appliesTo=PORTAL` rules** because a module cannot read `X-User-Type` (no `jakarta.servlet`/`org.springframework` on its classpath) — use `appliesTo=ALL` (see that module's README for the full gap list, incl. weaker webhook idempotency: a module writes via `QueryEngine` with no transaction of its own, so it converges on redelivery rather than claiming each event id transactionally) |
| Integration | (see Complete) | **Script execution**: `GraalVmScriptExecutor` (GraalVM `org.graalvm.polyglot`, sandboxed) IS the active `ScriptExecutor` bean (`FlowConfig.scriptExecutor`) — the prior "no-op" note was stale. Reachable from flows (`InvokeScriptActionHandler`) and now from record-event scripts (unified record slice 7). `LoggingScriptExecutor` remains only as a fallback impl. Connected Apps: **both OAuth grants now work end-to-end, no redeploy** (2026-07-05). kelta-auth resolves connected-app OAuth clients **dynamically** from the `connected_app` table via `ConnectedAppRegisteredClientRepository` (a wrapper over the JDBC store; ~30s cache) — the analogue of the OIDC/SAML dynamic repos — so client-credentials **and** authorization_code apps register live and edits/deactivations take effect without a kelta-auth restart. This replaced the old startup `ConnectedAppClientSynchronizer`, which only *added* authorization_code apps at boot (edits never propagated, deactivations never removed the client, and UI-created client-credentials apps were never registered at all). `require_pkce` (public SPA clients) + `consent_required` are honored; the built client id = `connected_app.id` (stable across `/oauth2/authorize` → token exchange). Every issued token — cc **and** authorization_code — is recorded by `ConnectedAppTokenRecorder` (pinned `jti` → `connected_app_token` row, refreshes `last_used_at`, `TOKEN_ISSUED` audit row tagged `via`), so the app's token list / "Last used" / audit reflect real activity for both grants. **Admin UI**: `ConnectedAppsPage` gains an OAuth grant-types section (client_credentials / authorization_code checkboxes + PKCE + consent toggles → `grantTypes`/`requirePkce`/`consentRequired` on the `connected-apps` collection). The `/oauth2/{authorize,consent,token}` server + `JdbcOAuth2AuthorizationService` + consent page were already wired. Worker `POST /api/connected-apps/{id}/tokens` returns **410 Gone** (it can't mint without the raw secret; real token rows come from kelta-auth). **Security — not auto-merged.** |
| SAML 2.0 SSO (Rec 8) | **Config foundation**: `saml-providers` system collection (`saml_provider`, V146, RLS) — per-tenant IdP config (registrationId, idpEntityId, ssoUrl, idpCertificate PEM, nameIdFormat, email/profile attribute mappings, active); managed via the generic collection CRUD/MCP, mirroring `oidc-providers`. **SSO flow (kelta-auth)**: `spring-security-saml2-service-provider` + OpenSAML 5.1.6 (Shibboleth Maven repo — not on Central). `DynamicRelyingPartyRegistrationRepository` loads per-tenant config via `WorkerClient` and builds a `RelyingPartyRegistration` per `{tenantId}:{providerId}` (IdP verification cert parsed from the stored PEM, 5-min cache) — the SAML mirror of `DynamicClientRegistrationRepository`. `@Order(0)` SecurityFilterChain on `/saml2/**` + `/login/saml2/**` + `/logout/saml2/**` (`saml2Login` + `saml2Logout` + `saml2Metadata`); validated assertion → `FederatedUserMapper.mapSamlUser` (shares the JIT-provision tail with OIDC; email from the provider's `emailAttribute`→common attrs→NameID, profile via `profileAttribute`→`findProfileByName`, else `Minimum Access` fallback). **IdP-initiated SSO** works — Spring Security's ACS (`/login/saml2/sso/{registrationId}`) accepts unsolicited `Response`s (no AuthnRequest correlation is required), so an IdP-initiated assertion runs the same `mapSamlUser` tail as SP-initiated. **Single Logout (V159)**: optional per-IdP `sloUrl` (`saml_provider.slo_url`, surfaced on the `saml-providers` collection + `/internal/saml/*`). When set, `DynamicRelyingPartyRegistrationRepository` wires SLO on both sides — the IdP `singleLogoutServiceLocation` (assertingParty) + the SP endpoint `{baseUrl}/logout/saml2/slo/{registrationId}` — and the SP metadata advertises it; `saml2Logout` then honors **IdP-initiated `LogoutRequest`** (kills the session, returns a signed `LogoutResponse`) and validates IdP `LogoutResponse`s. Providers without an `sloUrl` keep their exact prior metadata (no SLO element). **SP-initiated Single Logout** now ships: the SAML subject identity (NameID + SessionIndexes + registrationId) is captured at login into the Redis-backed HTTP session (`SamlLogoutInfo`, registered for native JDK serialization in `AuthRuntimeHints`) — the `SamlFederatedLoginSuccessHandler` otherwise discards it when it swaps the `Saml2Authentication` for a platform token. The app's logout routes through a new initiator `GET /logout/saml2/initiate` (`SamlLogoutController`, permitAll on the SAML chain): a SAML session resolves a signed `LogoutRequest` (`OpenSaml5LogoutRequestResolver`, per-request RelayState nonce) saved via `HttpSessionLogoutRequestRepository` and redirects to the IdP SLO; the returning `LogoutResponse` (existing `saml2Logout` filters) is validated and `SamlLogoutSuccessHandler` redirects to the stashed same-origin `post_logout_redirect_uri` (the OIDC `end_session_endpoint`) — so a SAML user is logged out at the IdP, then the AS session, then back to the app. Non-SAML sessions (internal / OIDC-brokered) or providers without an SLO URL fall through as a safe passthrough to the same target, so the frontend routes every logout through the initiator without needing to know the login method. The post-logout target is stashed in the session (not RelayState) so no token leaks to the IdP, and is same-origin-validated (open-redirect safe). Frontend `AuthContext.logout()` wraps its OIDC end-session URL in the initiator. **Platform SP request signing** (`kelta.auth.saml.sp-signing-{certificate,private-key}` PEM, env-injected): one platform-wide SP keypair signs outbound AuthnRequests/LogoutRequests and is published via `/saml2/metadata/{registrationId}`; absent → sent unsigned. Login page renders SAML buttons → `/saml2/authenticate/{registrationId}`. Worker `/internal/saml/{providers,by-entity-id}` endpoints serve the config (FORCE-RLS, read via admin-bypass + `tenant_id` filter). The whole SAML chain + repo are gated on `FederatedUserMapper` (i.e. `EncryptionService` configured), mirroring OIDC. Tests: 10 registration-repo (incl. SLO wired/omitted) + 5 SAML-mapper + 2 login-button + 4 worker-endpoint + 4 SP-initiated-logout controller + 5 logout-success/redirect-validation + 1 login-capture + AuthContext Vitest + harness scenario on real Postgres | Positive Playwright e2e needs a live deployed IdP (post-deploy, like other new admin flows) — inbound IdP-initiated SLO, SP-initiated SLO, and SP metadata are all done; SP metadata advertises a signing cert only when the SP keypair is configured. **Security — not auto-merged.** |
| Page builder / screen builder | `PageBuilderPage` editor + CRUD; `ui-pages` has `slug`/`published`/`config` (V116); **render contract**: `GET /api/pages/{slug}/render` (`PageRenderController`/`PageRenderService`) serves a published+active page as a **versioned** `PageRenderContract` (`{version, slug, title, path, variables, dataSources, tree}` — bumped to **v2** in slice 1g: `tree` is the whole `config` map verbatim and page-level `variables`/`dataSources` are surfaced as siblings, empty for legacy pages, as a pure pass-through that resolves no bindings/dataSources server-side, preserving Cerbos/FLS; consumed by the binding/logic slices 2d/2e); **widget descriptor registry + shared `RenderTree`** (slice 2a): the runtime `PageTreeRenderer` renders through one registry-driven path (`PageBuilderPage/widgets/{registry,renderTree}`) instead of a per-type switch — built-ins register as descriptors under `widgets/builtins`, plugin page components fall back via `componentRegistry`, gated by `RENDER_TREE_V2` with the legacy switch kept as a fallback; gateway static route `/api/pages/**` registered (Rec 1 slice 1a); **builder persists the component tree into the `config` JSON column** (slice 1b — previously `components`/`layout` were sent as top-level attributes and silently dropped by the worker, so saved pages lost their tree); **end-user renderer**: `CustomPage` (route `/:tenant/app/p/:slug`) fetches the render contract and renders the tree via the schema-driven `PageTreeRenderer` (heading/text/button/image/card/container/table/form → React, plugin components via `componentRegistry`) — slice 1c; **`UIPageConfigEventPublisher` broadcasts `kelta.config.page.changed.<pageId>`** on page create/update/delete for multi-pod cache invalidation (slice 1d); **table data binding**: a `table` node with `props.dataView = {collection, fields, limit}` renders live rows — `DataTableNode` fetches `/api/{collection}` via the authorized JSON:API path (gateway+worker enforce Cerbos/FLS; denied fields stripped server-side) and shows the declared columns (slice 1e-render); **builder PropertyPanel data-source editor** for table+form (collection/fields/limit → `props.dataView`, slice 1e-ui); **form create binding**: a `form` node renders an input per declared field and submits a new record via `apiClient.postResource('/api/{collection}', …)` — the authorized JSON:API create path (Cerbos/FLS enforced) — slice 1f; **page create auto-derives the required `slug`**: `UIPageSlugHook` (before-save on `ui-pages`) slugifies `name` (→`path`→`title`) and disambiguates per tenant when absent — the `ui_page.slug` column is `NOT NULL`/`UNIQUE` (V116) but builder/API/MCP payloads don't send it, so creates 500'd before this fix; **schema-driven inspector + palette (slice 2b)**: the palette (`PageBuilderPage/palette/Palette.tsx`) renders from `widgetRegistry.listByCategory()` grouped by category, and the inspector (`PageBuilderPage/inspector/Inspector.tsx`) loops over `descriptor.propSchema`, mapping each `PropFieldKind` to a field editor under `inspector/fields/*` — the hardcoded `AVAILABLE_COMPONENTS` array, `ComponentPalette`, per-type `PropertyPanel` blocks, and the `Canvas`/`Preview` per-type previews are deleted (the canvas node chip + preview now render through the descriptor/`RenderTree`). `bindable` props get an `fx` literal↔expression toggle (`BindableField`) that writes `{ $bind, mode:'expr' }` (the picker stores the bare token; `{{…}}` is display-only); `EventListField` authors `PageAction[]` into `node.events` (add/remove/reorder, tabs over `descriptor.supportedEvents`). **Binding resolution lands in 2d; the action runtime in 2e (both shipped)**; **layout engine + `@dnd-kit` canvas (slice 2c)**: new `grid`/`row`/`column`/`divider` layout widgets; per-child responsive `span {base,sm,md,lg}` → Tailwind `col-span-*` + breakpoint prefixes (`PageBuilderPage/canvas/spanClasses.ts`, 48 literal classes for the JIT scanner); the native-HTML5 stack-only canvas is replaced by `canvas/Canvas.tsx` — one `DndContext` (Pointer + Keyboard sensors, `closestCenter`) hosting the palette draggables + per-container `SortableContext` reorder, **drop-into-container** (no longer appends to root — routes through `model/treeOps.ts` `insertNode`/`moveNode`), and **grid-span resize** snapping to the 12-col grid (`canvas/dnd/useSpanResize.ts`, only `span` persisted — no px/row/column); `model/treeOps.ts` (pure `insert`/`move`/`remove`/`updateProps`/`setSpan` with no-op + cycle guards) and `model/migrate.ts` convert a legacy `position {row,column,width,height}` tree to `grid`/`column`+`span` on builder load (idempotent, persisted on next save). **`handleSavePage`/`mergeConfig` widened to stamp `schemaVersion: 2` and round-trip `variables`/`dataSources`/`access` (the page-level siblings persist only if the save call passes them)**; unsaved-changes confirm + `beforeunload` guard added; `config.layout` (create-form `layoutType` select) is now inert legacy (removal deferred). `@dnd-kit/{core,sortable,utilities}` added to `kelta-ui/app`, scoped to the page canvas only; **data binding & expressions (slice 2d)**: any prop may be a binding `{ $bind, mode? }` resolved **client-side** (`model/resolveBindings.ts` over `@kelta/formula`; `path` mode walks a dotted/`[n]` path, `expr` mode flattens scope leaves then `evaluate`s; `{{…}}` interpolation; `getPath` refuses `__proto__`/`constructor`/`prototype`); page-level **variables** (`usePageVariables`, defaults seeded) + on-load **dataSources** (`usePageDataSources` fetches over the authorized JSON:API path — caps `MAX_PAGE_DATA_SOURCES=12`/`MAX_REPEATER_ROWS=200` in `model/limits.ts`); a **page-settings drawer** (`inspector/pageSettings/*`) authors variables/data sources (persisted via the 2c save path); new `field-value`/`list`/`repeater` widgets (the repeater re-resolves children per `item` row); `CustomPage` builds the runtime `BindingScope` (vars+data+page) and threads it into `RenderTree`; **events & actions (slice 2e)**: page widgets fire an ordered `PageAction[]` per event (`onClick`/`onSubmit`/`onLoad`/`onChange`) via the **client-side action runtime** (`PageBuilderPage/runtime/executeAction.ts` + `useActionRunner`) — `runFlow` (`POST /api/flows/{id}/execute` `{input}` → optional `awaitResult` poll `GET /api/flows/executions/{id}` to a terminal status, reading the execution id from `data.id`), `navigate`/`openUrl` (router / `window`, **URL scheme allow-list `assertSafeUrl` blocks `javascript:`/`data:`** — `runtime/urlSafety.ts`), `createRecord`/`updateRecord` (JSON:API `POST`/`PATCH`, Cerbos + write-FLS enforced server-side), `refreshData` (invalidate the named data-source query), `setVar` (page-variable store), `showToast` (sonner); actions run **sequentially** and the first reject **stops the chain** + surfaces one error toast; action values may be `{$bind}` resolved client-side against the event scope (2d); the `button`/`form` built-ins fire events **only in `mode:'runtime'`** (the editor preview is inert); `inspector/fields/EventListField` authors them (add/remove/reorder + per-action sub-forms). **2e owns the post-deploy `e2e-tests/tests/page-builder-v2.spec.ts`** (asserts a persisted record from a button event, not just render); **typed form widgets (slice 2f)**: the `form` widget renders via `@kelta/components` `ResourceForm` (typed input per `FieldType`, Zod validation, field-level authz, pluggable field-renderer registry wired via `setComponentRegistry` to `LookupSelect`/`MultiPicklistSelect`/native `<select>`/`RichTextEditor` — `widgets/builtins/{forms,formFieldRenderers,registerFormFieldRenderers}.tsx`); standalone Input-category widgets `text-input`/`number-input`/`checkbox`/`dropdown`/`datepicker`/`lookup`/`multi-picklist`/`rich-text` (`widgets/builtins/inputs/*`) map a `FieldType`→control, sourcing picklist options from `GET /api/picklist-values?filter[…]` (FIELD/GLOBAL via `fieldTypeConfig.globalPicklistId`, shared `usePicklistOptions`) and reference options from the target collection (`useLookupOptions`→`useLookupDisplayMap`); the old text-only `FormCreate` body is removed; **rich-text/HTML display is sanitized through the same `FieldRenderer` `rich_text` (`stripHtml`) path — never `dangerouslySetInnerHTML` on bound HTML**; submit stays on the authorized JSON:API path (Cerbos/write-FLS server-side, worker validates required/type/unique); client (Zod) validation is advisory only. New `builder.{form,input}.*` i18n keys; **widget breadth (slice 2g)**: six first-class `WidgetDescriptor`s (one file each under `widgets/builtins/`, one `Render` shared by editor preview + runtime) — `chart` (category `chart`; `recharts` bar/line/pie selectable via `props.chartType`, bound to an **already-resolved** array at `props.dataView` with `props.xKey`→axis + `props.series[].key`→series, editor sample fallback + runtime "No data"), `tabs` + internal `tab-panel` (category `navigation`/`layout`; radix `@/components/ui/tabs`, one `tab-panel` child per `props.tabs[]` entry matched by `value`, each panel's sub-tree rendered through the shared `renderChild` — switching triggers swaps the sub-tree; `tab-panel` is `paletteHidden:true` so the registry-driven `Palette` resolves it but never lists it), `nav` (category `navigation`; `@kelta/components` `Navigation` from `props.items[]`, empty state "No menu items."), `icon` (category `content`; `lucide-react` static `icons` map by PascalCase-converted name, `HelpCircle` fallback for unknown names — `dynamicIconImports` is absent in `^0.563.0`), `link` (category `content`; router `Link` for an internal `to` / `<a>` for external `href`, bindable `label`/`to`/`href`, inert in editor mode), and **`image` polish** (now bindable `src`/`alt` + an `objectFit` select, replacing the bare 2a image; `data-testid`s unchanged). **Security**: `link.href`/`image.src` are scheme-validated via the shared `runtime/urlSafety` allow-list (`{http,https,mailto,tel,relative}`, the same helper 2e's `navigate`/`openUrl` use) before render — a bound `javascript:`/`data:` collapses to `''` (inert link / placeholder-or-null image), proven by blocked-`javascript:` tests. New `builder.widget.*` i18n keys (labels + empty states). **Conditional visibility + computed variables ship (2026-07-08, app-platform slices 1+2, FE-only)**: universal bindable `visible` prop — `renderTree` skips a runtime node whose present `visible` resolves hidden (`false`/`'false'`/`0`/`''`/`null`; absent = visible, unresolvable binding ⇒ hidden fail-closed), editor never hides (SelectableNode ghosts a literal false + badges a binding), Inspector grows a universal Visibility row (BindableField + fx toggle, zero per-descriptor schema edits); `PageVariable` gains `kind:'computed'`+`expression` — `model/computedVars.ts` Kahn-orders bare-identifier expressions over static vars + computed siblings (cycle ⇒ null + dev warn; **the formula parser is flat-key only: dotted `vars.a` throws ⇒ null; `data.*` not readable in computed expressions v1**), merged into `scope.vars` in `CustomPage`; `usePageVariables` excludes computed from seeding and its `setVar` rejects computed names (single guard site, error toast); VariablesSection authors kind/expression. **Builder undo/redo ships (2026-07-08, app-platform slice 4, FE-only — PHASE 3 COMPLETE)**: `hooks/useEditorHistory` (generic, dependency-free) holds a 50-deep clone history of the authored artifact `{components, variables, dataSources}`; recording is effect-based with a 0ms-deferred `record` (400ms coalescing collapses typing bursts, identical-snapshot guard swallows the seed echo, `applyingHistoryRef` keeps undo applications from re-recording); ⌘Z/⇧⌘Z (inert in text inputs — native undo wins) + toolbar ↶/↷ buttons; history resets on page open; undoing back to the last-saved snapshot clears `hasUnsavedChanges` (saved-marker refs updated on seed + save). The `WidgetDescriptor` type gained `paletteHidden?`; the `Palette` filters it; `vitest.config.ts` now dedupes `react-router-dom`/`react-router` (mirrors `vite.config.ts`) so the router-aware `nav` widget tests run. No backend/API/NATS/Flyway change; **per-page authorization (slice 1h)**: an optional `config.access.requiredPermission` gates render on a `profile_system_permission` grant — `PageRenderService.render(slug, profileId)` checks it via `BootstrapRepository.findProfileSystemPermissions` (the same in-controller system-permission lookup `/api/admin/**` uses, **not** a Cerbos PDP call); `PageRenderController` resolves the profile from the gateway-forwarded `X-User-Profile-Id` (`CerbosPermissionResolver`); **deny → 404, not 403** (a restricted page's existence is never leaked — the denial flows through the same `Optional.empty()→404` branch as an unknown slug), absent key ⇒ the published+active+tenant gate is unchanged and no permission lookup runs; the builder page-settings drawer gains an **Access** permission picker (`AccessSection`, options from the single-sourced `SYSTEM_PERMISSIONS` catalog). No DB column / migration — the key nests in `config`; **end-user nav surfaces pages**: `EndUserShell`/`TopNavBar` now render `ui-menu-items` whose `path` is `/p/<slug>` or `/app/p/<slug>` as **page** nav tabs (routing to `…/app/p/<slug>`) alongside the existing `/resources/<collection>` **collection** tabs — the menu→tab mapping moved into a pure, unit-tested `shells/EndUserShell/navTabs.ts` (`buildNavTabs`/`menuItemToTab`); `NavTab` is now `{key,kind:'collection'\|'page',target,label,icon}` and the mobile sheet groups Collections vs Pages. The menu is data-only per tenant — a `ui-menus` + `ui-menu-items` config drives it (e.g. CouchPicks `Main Navigation`: a Dashboard page item + the core catalog collections); **Apps/nav v2 ships (2026-07-08, app-platform slice 3, backend+FE)**: an app IS a `ui-menu` — V164 adds `icon`/`is_default`/`active` to `ui_menu` (+`SystemCollectionDefinitions` fields); the shell renders ONE active app's items (`activeMenus`/`resolveActiveMenu` in `navTabs.ts`: stored preference → `isDefault` → first by `displayOrder`; **intentional behavior change: multi-menu tenants no longer see all menus flattened**) with a `TopNavBar` app switcher (hidden <2 active apps; selection persisted via `usePreferenceValue('nav','active-app')`); `MenuBuilderPage` authors icon/default/active; NEW NATS subject `kelta.config.menu.changed.<tenantId>` (`MenuConfigEventPublisher` before-save hooks on `ui-menus`+`ui-menu-items`, broadcast-consumed by `MenuCacheInvalidationListener` → every pod evicts `SystemCollectionCache` menu entries — before this only the serving pod evicted and the fleet rode the 10-min Caffeine TTL); **`metric` widget (live KPI count)**: a `data`-category `WidgetDescriptor` (`widgets/builtins/metric.tsx`) rendering a single live aggregate — the total record count of a bound collection — instead of fixed text; it fetches one page (`apiClient.getPage('/api/{collection}?page[size]=1')`) over the authorized JSON:API path (Cerbos/FLS enforced) and reads `PageResponse.totalElements` (← `meta.totalCount`), transferring no rows, with `number`/`compact` Intl formatting, an editor sample, and an unbound placeholder. v1 counts the whole collection (no filter UI yet); **custom page as app home**: a page can override the end-user app's default landing page via `config.isHomePage` (authored by a "Set as app home" toggle in the page-settings drawer — `HomePageSection`, persisted through the same `mergeConfig` save path). `AppHomePage` resolves the override from the bootstrap `config.pages` (`resolveHomePageSlug` — first published+active page that opts in) and renders it in place via `<CustomPage slug=…>` (a new optional `slug` prop), so the URL stays `/app/home`. No DB column / migration — the flag nests in `config`; **submenus ship (2026-07-10)**: `ui-menu-items` gains a self-referential `parentId` lookup (V166, FK SET NULL — deleting a group floats children to top level) and `path` is now optional (a group header is an item with children, no path); bootstrap assembles one level of nesting (`bootstrapCache.buildItemTree`, cycle-safe flat fallback), `navTabs` maps parents to `group` tabs, and `TopNavBar` renders groups as dropdowns (mobile sheet + GlobalSearch/AppHome collectors flatten children). Builder tree UI predates this but its save path still PATCHes items as a menu attribute — persisting nesting from the MenuBuilder is a follow-up; nesting is authored via API/MCP today | positive Playwright e2e once deployed (incl. submenu dropdown); MenuBuilder nesting persistence |
| External-entity storage (connectors) | **Dispatch seam (Rec 4 slice 4a)**: `StorageAdapter` gained `default String storageType()`; `DispatchingStorageAdapter` (`@Primary`, runtime-core) routes every storage op to the adapter backing the target collection via `storageConfig().adapterConfig()["adapterType"]` (absent/unknown → `PhysicalTableStorageAdapter`; fail-safe fallback never silently mis-routes); external backends implement the `ExternalStorageAdapter` marker + override `storageType()`. Zero behavior change today (no external adapter registered). **REST adapter (slice 4b)**: `ExternalRestStorageAdapter` (`ExternalStorageAdapter`, `storageType=external-rest`) maps query/getById/create/update/delete to GET/POST/PUT/DELETE on `baseUrl+path[+/id]` over a tiny injectable `RestExecutor` seam (fake in tests, real `RestClient` impl deferred to wiring); pushes down pagination + sort + `EQ` filters as query params (best-effort), maps `idAttribute`→`id`, 404→empty, optional bearer token; schema ops no-op, `aggregate` unsupported. 13 unit tests (fake executor). **JDBC adapter (slice 4c)**: `ExternalJdbcStorageAdapter` (`storageType=external-jdbc`) maps CRUD/query to SQL over a foreign table via an injectable `ExternalJdbcConnectionProvider` seam (config: `jdbcUrl`/`username`/`password`/`table`/`idColumn`); pagination→LIMIT/OFFSET, sort→ORDER BY, `EQ`→`WHERE col=?`; values bound as params + every identifier (table/column/sort/filter field) validated against `^[A-Za-z_][A-Za-z0-9_]*$` before interpolation (SQL-injection-safe); `idColumn`→`id`; schema ops no-op, `aggregate` unsupported. 11 unit tests against real H2. Full runtime-core suite green. **Config persistence (slice 4d-1)**: `collection.adapter_config` JSONB column (V144) + `adapterConfig` modeled on the `collections` system collection (`SystemCollectionDefinitions`, JSON field → generic CRUD/MCP write path) + `CollectionLifecycleManager.buildDefinitionFromDb` parses it into `StorageConfig.adapterConfig` (malformed/empty → safe physical-table default). So an external `adapterType` now persists + loads + reaches the dispatcher. **Credential resolution (slice 4d-2a)**: new runtime-core `CredentialProvider` SPI (`resolve(credentialRef) → Optional<ResolvedCredential>`); both external adapters now resolve `adapterConfig.credentialRef` from the credential vault — REST applies auth by cred type (bearer/basic/api_key/oauth2, mirroring `CallApiActionHandler.applyAuth`), JDBC takes basic_auth username/password — so **secrets never live plaintext in `adapter_config`** (inline `bearerToken`/`password` remain a fallback for tests). 6 new tests. **Activation (slice 4d-2b)**: worker beans wired (`kelta-worker/.../service/storage/`) — `RestClientExecutor` (`RestClient`, no-throw on any status), `PooledExternalJdbcConnectionProvider` (HikariCP, one pool per `jdbcUrl+user`), `VaultCredentialProvider` (`CredentialProvider`→`CredentialResolver` via `TenantContext`, empty when vault/tenant/cred absent); both adapters registered as `@Bean`s in `StorageConfig` so `DispatchingStorageAdapter` collects them. **A collection with `adapterType=external-rest`/`external-jdbc` + `adapterConfig` now routes to the external backend end-to-end.** 7 worker tests. **Admin UI (slice 4e)**: `DataSourceSettings` component + a **Data Source** tab on `CollectionDetailPage` (user collections only) — pick physical / external-rest / external-jdbc, fill connection fields + a vault `credentialRef`, save → PATCHes the collection's `adapterConfig`; 5 Vitest. EN i18n (other locales fall back). **Virtual collections from OpenAPI (slice 4f)**: `OpenApiCollectionMapper` derives a flat collection shape from an imported operation's response schema (picks the 2xx JSON schema, unwraps a bare array or array-valued wrapper property → infers `dataPath`, maps OpenAPI type/format → `FieldType`, infers `idAttribute`); `ExternalEntityMaterializer` creates an external-rest collection + a field per derived property via `queryEngine.create` on the `collections`/`fields` system collections (the standard write path — config NATS broadcast + lifecycle init fire; external collections run NO physical-table DDL). `POST /api/api-specs/{specId}/operations/{opId}/materialize` (GET ops only) + the `materialize_api_collection` admin MCP tool. 11 worker unit tests + `ExternalCollectionMaterializeScenarioTest` (real Postgres: import → materialize → collection persisted/listed). **REST adapter hardening ships**: opt-in per-collection `adapterConfig.cacheTtlSeconds` (GET response cache, invalidated by writes through the adapter, 1000-entry safety cap) + `adapterConfig.rateLimitPerSecond` (dependency-free token bucket per `baseUrl`, burst = 1s of permits; exceeded → clear `StorageException`); both default off; injectable clock, +4 tests | connector packs (Salesforce/Slack) over the module SPI; 4e/4f Playwright e2e follows post-deploy (live-env constraint) |
| Offline data sync (PWA) | Frontend replica layer in `kelta-ui/app/src/offline/`: `IndexedDbOfflineStore` (IndexedDB via `idb` — records keyed `[collection,id]`, an outbox, and per-collection sync cursors; `InMemoryOfflineStore` for tests/SSR), `SyncEngine` (push outbox FIFO with optimistic temp-id swap on create + 409/4xx drop vs network-5xx stop-and-retry; pull deletions via `GET /api/{collection}/_changes?since=` [Rec 2B-1, #1066] then upserts via the existing `filter[updatedAt][gt]` JSON:API path), `resolveConflict` (server-wins / client-wins / last-write-wins), `useOnlineStatus`. A single server-clock `_changes` cursor is the canonical watermark for both queries; upserts reuse the authorized JSON:API path so Cerbos/FLS are enforced server-side. 27 Vitest tests (incl. `fake-indexeddb` contract tests). **Shell wiring: `OfflineIndicator` (`useOnlineStatus`) in `EndUserShell` renders an offline banner when connectivity drops (+3 Vitest).** **End-to-end wiring SHIPPED: `OfflineProvider` (`src/offline/OfflineProvider.tsx`) mounts a tenant-scoped `IndexedDbOfflineStore` + `SyncEngine(store, apiClient)` in the `EndUserShell` subtree (admin pages sit outside it; `useOffline()` returns `undefined` there so shared hooks stay online-only). Shared data hooks are offline-aware, gated on the optional context: `useCollectionRecords`/`useRecord` **write through** every online read into the replica and **serve `store.getAll`/`get`** offline (list applies client-side sort/filter/paginate); `useRecordMutation` **queues to the outbox** via `engine.queue` when offline (create/update/patch/delete; queued PATCH promoted to full PUT on replay) instead of hitting the network; `usePageDataSources` (CustomPage) mirrors the same write-through/cache-serve. On a false→true reconnect the provider runs `engine.sync([...knownCollections])` (push outbox + pull fresh) then invalidates the record queries. +16 Vitest (provider reconnect-flush, read write-through/offline-serve, mutation queueing, page-data offline).** **Cold-offline `CustomPage` ships**: the render contract is cached in a new `pages` object store (DB v2, incremental v1→v2 upgrade keeps existing replicas) — online loads write through, offline serves the cache, and a no-cache cold-offline visit gets a distinct "not available offline" state | Playwright offline-read/reconnect e2e post-deploy (live-env constraint). Known small gap: offline list read is best-effort (client-side filter/sort over cached rows, `total` = cached subset) **Outbox UI ships (2026-07-08, app-intelligence slice 1, FE-only)**: IndexedDB v3 adds a `failed` store — `SyncEngine.push()` retains 409/4xx-rejected replays (status + server error + failedAt) instead of dropping them silently (5xx/network still stays queued); new `retryFailed`/`discardFailed`/`onChange` engine surface + `useOfflineOutbox()`; `OfflineIndicator` shows queued-count offline, a failed-count banner even while online, and an expandable panel with per-op Retry/Discard. |
| PWA / offline (Rec 2) | **2A installable PWA**: `vite-plugin-pwa` (autoUpdate) on `kelta-ui/app` — web manifest + service worker precaching the app shell (Workbox), SPA navigate-fallback with `/api`/`/control`/`/internal`/`/actuator`/`/openapi` denylisted; API stays network-only. **2C native shell scaffold**: `capacitor.config.json` (`appId io.kelta.app`, `webDir dist`) + `cap:*` npm scripts + `kelta-ui/app/NATIVE.md` build guide wrap the PWA bundle in a Capacitor native WebView. The Capacitor toolchain is dev-installed (not a committed dep — keeps it out of the web CI/lockfile); the generated `ios/`/`android/` projects are `.gitignore`d and built on a native toolchain, not in CI. Native push reuses the existing `PushProvider` SPI + `FcmPushProvider` | **2B offline data DONE** (see the *Offline data sync (PWA)* row above): `record_tombstone` (V143) + `GET /api/{collection}/_changes?since=` cursor, IndexedDB replica + outbox + conflict resolution, and end-user runtime wiring (`OfflineProvider`, #1154). Remaining: ~~PNG maskable icons~~ **DONE** (raster `pwa-{192,512}` + `maskable-icon-512` [opaque bg, safe-zone] + `apple-touch-icon-180` in `public/`, wired in the `vite.config` manifest with `any`/`maskable` purposes; `index.html` apple-touch + `theme-color`; `png` added to the Workbox precache glob); app-bundle code-split — **partially done**: heavy admin pages (`FlowDesignerPage`/react-flow, `MonitoringPage`/recharts, `PageBuilderPage`/dnd-kit) are route-split via `React.lazy` into on-demand chunks (App.tsx imports pages per-module, not via the `./pages` barrel, so the eager graph no longer drags them in), dropping the main bundle ~4.35→3.2 MB; then ALL remaining admin pages route-split via `React.lazy` (App imports every page per-module; App.test mocks the page submodules), taking the main chunk to ~1.3 MB. Admin page chunks are emitted under `assets/admin/` (`build.rollupOptions.output.chunkFileNames`) and **excluded from the Workbox precache** (`globIgnores`) — runtime-cached on demand (`StaleWhileRevalidate`), dropping the install precache ~6.0→4.5 MB. **Code-split DONE.** **2C**: native build can't run in CI (toolchain-bound); **APNs delivery provider DONE** — `ApnsPushProvider` (`@ConditionalOnProperty kelta.push.provider=apns`) mirrors `FcmPushProvider` dependency-free: ES256 provider JWT signed via `java.security` (DER→JOSE R‖S) over the JDK native HTTP/2 client, iOS-only, `BadDeviceToken`/`Unregistered`/410 → invalid-token; config `kelta.push.apns.{team-id,key-id,bundle-id,auth-key-path,sandbox}`; 11 unit tests. Remaining: `@capacitor/push-notifications` device-token registration **wired** — `src/push/deviceRegistration.ts`: `registerDeviceToken` POSTs to the existing `POST /api/devices`, `initPushNotifications` (called from `EndUserShell`) acquires the token from the Capacitor plugin on a native platform and is a no-op on the web (the uninstalled plugin is kept out of the build via a variable-specifier dynamic import); 5 Vitest. Needs on-device verification (native toolchain, not CI). **Per-tenant APNs overrides DONE** — `TenantPushSettings` carries `push.apns.{teamId,keyId,bundleId,authKey}` (`hasApnsOverride()`); `ApnsPushProvider` signs the JWT with the tenant key/team/topic when a complete override is present (JWT cached per `team:kid` so tenant/platform tokens don't collide), else platform defaults — mirrors the existing FCM override path. **Rec 2 PWA/offline COMPLETE for all CI-testable scope** (only on-device native push verification remains). **Startup fix (2026-08-03, consumer-alerting slice 6)**: neither `FcmPushProvider` nor `ApnsPushProvider` carried `@Autowired` on its production constructor, and both also declare a package-private test constructor — so Spring found no no-arg constructor and **`kelta.push.provider=fcm|apns` failed worker startup outright**, undetected because nothing in the worker suite starts a context. Both fixed and locked by `PushProviderWiringTest` (see `concerns.md` → Test Coverage Gaps) |
| Governed AI agents | **Definition model + CRUD** (`AgentDefinition` → `ai_agent`, kelta-ai V4; `AgentService`/`AgentDefinitionRepository`/`AgentController` at `/api/ai/agents`). **Orchestration runtime**: `AgentRuntimeService` runs a bounded tool-use loop (`AgentModelClient` seam over `AnthropicService` attaches only the agent's allowed tool subset + model/maxTokens overrides; `ToolDispatcher` executes; non-permitted tools refused as defense-in-depth) with per-run iteration (8) + token (100k) caps, monthly-quota check + `recordUsage` per turn; `POST /api/ai/agents/{id}/run` returns final text + tool-call trace. **Audit + authz**: every run (incl. refusals) is persisted to `ai_agent_execution` (kelta-ai V5; tool-call trace + tokens + status) via `AgentExecutionService`, listable at `GET /api/ai/agents/{id}/executions`; runs require `X-User-Id` so tools execute as the invoking user — the worker's per-record Cerbos/FLS advice then applies downstream (no privilege escalation). **PII masking**: `PiiMaskingService` redacts email/SSN/card/phone from tool results before they reach the model or the audit trail (always-on; user input is not masked). **Admin UI**: `AiAgentsPage` (`/ai-agents`, Setup → Automation) — list/create/edit/delete + a run panel showing final text + tool trace + tokens; Vitest-covered (Playwright e2e follows once the page is deployed — the e2e suite runs against the live env). **AI page generation ships (2026-07-08, app-intelligence slice 2)**: new `propose_ui_page` tool (`ProposeUiPageHandler`) + `ProposalService.applyUiPageProposal` — validates the widget tree against the FE registry catalog (30 types, ≤200 nodes, depth ≤8, missing ids assigned) then `WorkerApiClient.createUiPage` POSTs `/api/ui-pages` with `published: false` + `config.schemaVersion: 2` (**drafts only — publish stays human, in the builder**); `SystemPromptService` gains a UI-Pages authoring section (widget catalog, `{$bind}` rules, bare-identifier expressions); FE `UiPageProposalCard` in AiChat (tree outline, Apply/Dismiss, applied → Page Builder link). NOTE: the widget allow-list is duplicated FE `widgets/builtins` ↔ kelta-ai `ProposalService.ALLOWED_WIDGET_TYPES` — adding a widget means updating both | — (stretch: `MCP_CALL` flow action, hybrid tsvector+vector ranking) |
| Sandbox environments + metadata promotion | **Tenant-backed sandboxes + real promotion pipeline** (V158; supersedes the hollow V122 logical-environment code — `cloneMetadata` snapshot-only, `executePromotion` never imported, no gateway routes, no authz). **Sandbox = real tenant**: `tenant.parent_tenant_id`; `SandboxProvisioningService.createSandbox` creates the linked tenant through the standard tenants write path (full hook chain: schema, profiles, OIDC, admin, Cerbos, Svix, Superset), slug `{parent}--{name}` (reject >63), copies parent limits/settings/**ip-allowlist** (fail-open filter would otherwise expose the clone), replaces the seeded admin's well-known password with a one-time random secret, then async-clones config via **package export (parent ctx) → hardened import (sandbox ctx)** with explicit `callWithTenant` hops. **Package import rebuilt** (`PackageImportService`): QueryEngine on system collections (quota hooks + DDL + NATS fire — old raw-SQL path created metadata rows with no physical tables), natural-key reference remap (unresolvable → item FAILED, never a dangling UUID), name-keyed upsert w/ `conflictMode=skip\|overwrite`, per-item `ImportReport`; coverage extended: + FLOW (`created_by` remapped), PAGE_LAYOUT+sections+fields, VALIDATION_RULE, GLOBAL_PICKLIST+values. **Package format v2** carries `source` provenance (`platform_instance` id, V158) — multi-cluster topologies are first-class: the only universal check is **source ≠ target**, enforced on promotion. **Promotion**: create (sandbox→production only) → approve (**approver ≠ creator**, 409) → execute (APPROVED strictly; snapshots TARGET first — snapshots now store full packages so rollback re-imports OVERWRITE; SELECTIVE natural keys + dependency pull-in; **ROLE/POLICY/ROUTE_POLICY/FIELD_POLICY never promoted** — authz-tampering guard) → rollback (local only; created items not deleted, documented). **Remote push targets**: env rows may carry `{remote_base_url, remote_tenant_slug, credential_ref}`; `RemotePromotionClient` pushes the package to the remote cluster's `/api/packages/import` with a **vault PAT** (never stored/inline), scheme-allowlisted URL, no redirects, bounded response; `POST /api/environments/{id}/test` checks connectivity; remote rollback → 409. **AuthZ**: new `MANAGE_SANDBOXES` system permission (seeded + V158 backfill; UI checklist), in-controller gates on environments/promotions; `/api/packages/**` gated `CUSTOMIZE_APPLICATION`; gateway static routes `packages`/`environments`/`promotions` added (all three controllers were 404 before). **V158 also retrofits RLS** onto the four V122 tables (never enabled; `promotion_item` gains `tenant_id` per the V85/V86 pattern). `EnvironmentsPage` admin UI (`/environments`) + `kelta sandbox`/`kelta promote` CLI. **Not auto-merged (destructive prod-config surface).** | Remote *sources* (push-only v1) + remote rollback; promotion doesn't propagate deletions (rollback likewise); scripts/record-types/list-views/layout-related-lists/layout-rules not yet in package coverage; NATS config-event burst on large promotes (sequential, same as bulk authoring); Playwright e2e post-deploy |
| Data masking | **Backend core + egress + UI shipped** (PRs 1 and 2, both security-typed/no-auto-merge). Mask shape on the field: `fieldTypeConfig.masking` `{type: FULL\|LAST4\|EMAIL\|CUSTOM, maskChar?, customPattern?}` (string-typed FieldTypes only — `FieldMaskingService.MASKABLE_TYPES`; invalid config degrades to FULL, never to plaintext). Exposure per profile: 4th `profile_field_permission.visibility` value **`MASKED`** → `CerbosPolicyGenerator` emits DENY `[unmask, write]` (write-deny makes `CerbosFieldWriteSecurityAdvice` strip an echoed placeholder — no stored-value corruption). **JSON:API enforcement (PR 1)**: `CerbosFieldSecurityAdvice` strip→mask + per-record `meta.maskedFields` (one extra cached batched Cerbos check ONLY on collections with masking config); `MaskedFieldPredicateInterceptor` uniform-403 `MASKED_FIELD_PREDICATE` guard on filter/sort probing; realtime `RealtimeBridge` omits record `data` when `RecordChangedPayload.containsMaskedFields`. **Egress masking (PR 2)**: `RecordMaskingService.maskRows` at the non-advice boundaries — `DataExportService` (CSV/JSON, requester threaded through the async job via `ExportPrincipal`) and `ReportExecutionService` (execute + CSV/PDF export, requester via `ReportExecutionController` + `CerbosPermissionResolver` → `MaskingPrincipal`); reports **reject** a filter/sort/group-by on a field masked for the executor (predicate/ordering/group-key leak). A `null`/system principal (scheduled report delivery, background export) is a documented system-trust tier that skips masking, same contract as flows/FLS. **Search + embeddings (PR 2)**: masking-configured fields excluded from the shared `search_content` tsvector + `display_value` (`SearchIndexService`, index-time/field-config-level — the index has no per-viewer identity) and never embedded (`EmbeddingOnWriteHook` skips a masking-configured source field); `FieldConfigEventPublisher` triggers `rebuildCollectionIndexAsync` when a field's masking config toggles (existing rows re-indexed to purge plaintext). **Admin UI (PR 2)**: FieldEditor "Data Masking" section (maskable types only), FieldPermissionEditor `MASKED` visibility + count, `InlineFieldValue` masked→locked read-only off `meta.maskedFields` (surfaced onto the flat record by `flattenResource.__maskedFields`, wired through `LayoutFieldSections`/`ObjectDataTable`/`RelatedList`). **MCP (PR 2)**: `add_field`/`update_field` masking args (`maskingType`/`maskingChar`/`maskingCustomPattern` → `fieldTypeConfig.masking`; rejected on non-string types). Fixed alongside (pre-existing FLS bugs, PR 1): per-action field-access cache key; `FieldPermissionSyncHook`; `BootstrapRepository` UUID→name translation for field-permission rows. No migration, no new NATS subject. Unit tests (worker + MCP + Vitest) + `FieldMaskingPermissionScenarioTest` (real-DB translation query). **Dashboards masked (2026-07-05)**: `DashboardDataController` resolves a `MaskingPrincipal` (`CerbosPermissionResolver`) and threads it into `DashboardDataService`; a masking-configured target collection **bypasses the shared widget cache** (not per-viewer) and applies per-viewer masking — table/recent rows via `RecordMaskingService.maskRows`, and chart group-by / table sort / any filter on a viewer-masked field rejected (`WidgetExecutionException`), mirroring reports. Metric aggregates are numeric-only so masked (string) fields never leak. No-masking collections + `null`/system principals unchanged. +5 `DashboardDataServiceTest`. Security — not auto-merged. **Duplicate detection masked (2026-07-05)**: `POST /{collection}/duplicates` rejects 400 a match on a field masked for the requester (`DuplicateDetectionService` + `MaskingPrincipal`) — grouping keys can't be masked without collapsing values, so reject (mirrors group-by); closed a plaintext + record-grouping leak. **Stale-vector purge (2026-07-05)**: on a masking-presence toggle, `FieldConfigEventPublisher` purges the dependent VECTOR columns (`VectorMaintenanceService` → `StorageAdapter.clearVectorColumn`) alongside the tsvector reindex, so pre-existing plaintext-derived embeddings can't leak via semantic-search ranking; rows re-embed on next write | **v2 (deferred, see concerns.md)**: flow/script/webhook/NATS-consumer contexts (system trust tier, same as FLS today), bulk-ops/merge write side (result payloads carry no field values), per-subscriber realtime, ABAC/conditional masking. Positive Playwright e2e post-deploy (live-env) |
| Semantic search / RAG | `EmbeddingService` SPI + dependency-free `HashingEmbeddingService` default (overridable via `@ConditionalOnMissingBean`); `VECTOR` field maps to `vector(N)`; **pgvector enabled** (lazy `CREATE EXTENSION` on first VECTOR column; `?::vector` writes; pgvector image for local/CI — prod Postgres needs the extension installed); `StorageAdapter.semanticSearch` cosine (`<=>`) query + `SemanticSearchService` + `POST /api/{collection}/semantic-search` (FLS-respecting; distances in `meta`); **HNSW cosine index auto-emitted**; `semantic_search` MCP user tool; **embed-on-write**: `EmbeddingOnWriteHook` (wildcard before-save) auto-populates a VECTOR field from a configured `embeddingSource` text field via the `EmbeddingService` (dimension-guarded; explicit vectors and untouched source skipped; **a masking-configured source is never embedded**); end-to-end verified against real pgvector (`PhysicalTableStorageAdapterVectorIntegrationTest`). **Masking-toggle purge**: `StorageAdapter.clearVectorColumn` (`UPDATE … SET vec = NULL`, PhysicalTable impl + Dispatching delegate, external adapters no-op) lets `VectorMaintenanceService` clear stale plaintext-derived vectors when a source field's masking toggles (wired from `FieldConfigEventPublisher`) — closes the semantic-search ranking inference leak | governed agent runtime (next Rec 3 slice) |

## 🔴 UI Complete, backend MISSING (do not assume these work end-to-end)

> **2026-07-02 audit correction.** A ground-truth pass (reading the controllers/services,
> not the docs) found this section was badly stale in the *pessimistic* direction — four of
> the five rows here had real, substantial, tested backend code. They moved to ✅/🟡 above.
> The lesson: several of these were reported "backend missing" when the true cause was a
> **missing gateway static route** (real controller, unreachable → UI 404 → placeholder).
> Verify reachability (gateway route present AND controller exists) before marking a row 🔴.

| Capability | Built (UI) | Not built (backend) |
|------------|-----------|---------------------|

## ⚪ Planned / enterprise gaps (not started)

HA/failover docs (RTO/RPO) · scheduled *backup* (data export itself ships — see ✅).

| Capability | Notes |
|------------|-------|

*(Telehealth: all 7 slices ship (portal identity → chat → scheduling → LiveKit video → visit UX → archival & retention). Moved to the 🟡 Partial table 2026-07-10 with slice 7 (V171); the only remaining production gates are flipping the retention purge out of dry-run after backup sign-off, and the TURN-TLS cert — both in concerns.md.)*
*(Data masking: PR 1 backend core + PR 2 egress/UI/MCP shipped — see the 🟡 Partial table; only v2 contexts [dashboards, system-tier flow/webhook] + post-deploy e2e remain.)*
*(Delegated administration moved to 🟡 2026-07-05 — scoped user management ships; see the Partial table.)*
*(Removed 2026-07-04: sandbox environments + metadata promotion → 🟡 shipped as
tenant-backed sandboxes; see the Partial table.)*

*(Removed 2026-07-02 as already-built: SCIM 2.0 provisioning → ✅; scheduled data export
→ ✅ via `ScheduledJobExecutorService` + `DataExportController`; per-tenant IP allowlist →
already ✅ in the Complete table.)*

---

This map is distilled from the engineering capability inventory. When you implement or
remove a feature, move its row to the correct section in the same PR so the next agent
trusts it.
