# Kelta Platform changelog

This file tracks merged autopilot work. Entries are appended by autopilot workers as the final step of each task, one line per merged change, in the format `- YYYY-MM-DD <type>(<scope>): <one-line summary> (<task-id>)`. Newest dates appear at the bottom; group entries under an H2 date heading.

## 2026-05-10

- 2026-05-10 chore(autopilot): seed CHANGELOG.md (CHORE-2026-05-10-0001)

## 2026-05-11

- 2026-05-11 chore(repo): add .editorconfig with project-wide indentation rules (CHORE-2026-05-10-0004)
- 2026-05-11 doc(architecture): document the autopilot loop — topology, queue lifecycle, label gate, migration lock, bug ingress (DOC-2026-05-10-0001)
- 2026-05-11 chore(security): add SECURITY.md with vuln reporting policy and autopilot gating note (CHORE-2026-05-10-0002)
- 2026-05-11 chore(docs): add CONTRIBUTING.md outlining the autopilot workflow (CHORE-2026-05-10-0003)
- 2026-05-11 chore(autopilot): use task title+type for fallback commit message in worker.sh so `gh pr create --fill` produces a useful PR title (CHORE-2026-05-10-0005)
- 2026-05-11 doc(ui): add UI component consolidation plan covering DataTable/FilterBuilder/FieldRenderer/ResourceForm/RelatedList unification, feature superset, and migration order (DOC-2026-05-10-0002)
- 2026-05-11 chore(cache): wire NATS broadcast listeners on gateway and worker for `kelta.config.domain.changed.*` and `kelta.config.feature.changed.*` cache invalidation (CHORE-2026-05-10-0007)
- 2026-05-11 chore(events): publish `kelta.config.domain.changed.<id>` and `kelta.config.feature.changed.<tenantId>` from admin domain/governor-limits endpoints so all pods evict caches across the fleet (CHORE-2026-05-10-0006)
- 2026-05-11 chore(events): publish `kelta.config.domain.changed.<id>` and `kelta.config.feature.changed.<tenantId>` from admin domain/governor-limits endpoints so all pods evict caches across the fleet (CHORE-2026-05-10-0006)
- 2026-05-11 chore(test-harness): skip Testcontainers PG in `KeltaStack` when `$CI_DB_JDBC_URL` is set; use shared `kelta-ci-db` pool with per-run schema isolation (CHORE-2026-05-10-0008)
- 2026-05-11 fix(runtime): reconcile schema before issuing FK constraint statements in `PhysicalTableStorageAdapter.initializeCollection` so a NATS UPDATED event on a pod that fell into the initialize path no longer fails with "column does not exist" when the table already existed but the FK column was added later (BUG-2026-05-11-0001)

## 2026-05-30

- 2026-05-30 fix(runtime): swallow `DuplicateKeyException` on `pg_type_typname_nsp_index` from concurrent CREATE TABLE in `PhysicalTableStorageAdapter.initializeCollection` so the losing pod in a multi-pod NATS race continues to reconcile the winner's table instead of failing initialization (BUG-2026-05-30-2228)

## 2026-06-01

- 2026-06-01 fix(e2e): point E2E workflow + `.env.example` at `app.kelta.io`/`api.kelta.io`/`auth.kelta.io` after the rzware.com cutover; the legacy hostnames no longer serve a valid cert so Playwright was failing every test with `ERR_CERT_AUTHORITY_INVALID` (BUG-2026-06-01-4324)

## 2026-06-03

- 2026-06-03 chore(runtime): populate JSON:API error envelope on every 4xx response — `code` now mandatory; added `@ExceptionHandler`s for `MethodArgumentNotValidException`, `ConstraintViolationException`, `HttpMessageNotReadableException`, `MissingServletRequestParameterException`, `MethodArgumentTypeMismatchException`, `NoHandlerFoundException`/`NoResourceFoundException`, `MethodNotAllowed`, `UnsupportedMediaType`, `ResponseStatusException`; fixed `JwtAuthenticationFilter` to emit `errors[]` array instead of singular `error` object (CHORE-2026-06-02-0008)
- 2026-06-03 chore(runtime): populate JSON:API error envelope on every 4xx response — `code` now mandatory; added `@ExceptionHandler`s for `MethodArgumentNotValidException`, `ConstraintViolationException`, `HttpMessageNotReadableException`, `MissingServletRequestParameterException`, `MethodArgumentTypeMismatchException`, `NoHandlerFoundException`/`NoResourceFoundException`, `MethodNotAllowed`, `UnsupportedMediaType`, `ResponseStatusException`; fixed `JwtAuthenticationFilter` to emit `errors[]` array instead of singular `error` object (CHORE-2026-06-02-0008)
- 2026-06-03 feat(runtime): filter orphan columns from JSON:API record responses — `DELETE /api/fields/{id}` only deprecates the underlying Postgres column (no DROP COLUMN), so `SELECT *` kept exposing deleted field names as `null` in `data.attributes`. `DynamicCollectionRouter.toJsonApiResourceObject` now drops keys that have no live `FieldDefinition`, no framework-metadata role (createdAt/updatedAt/createdBy/updatedBy/tenantId/recordTypeId), and no companion-column link to a still-live CURRENCY/GEOLOCATION field. Also corrects `McpApplicationTest.adminEndpointSurfaceCoversPhase6Through8` to include `delete_collection` added in #972 (TASK-2026-06-02-0005)

## 2026-06-08

- 2026-06-08 chore(ui): remove unused React imports and rename/mark unused locals across `kelta-ui/app` to clear all 86 TS6133 errors (CHORE-2026-06-08-0001)
- 2026-06-08 chore(ui): remove unused React imports and rename/mark unused locals across `kelta-ui/app` to clear all 86 TS6133 errors (CHORE-2026-06-08-0001)

## 2026-06-12

- 2026-06-12 chore(mcp): `create_flow` tool writes `flowType` (the real `flows` collection field) instead of the dead `triggerType` column; legacy `triggerType` argument is still accepted and remapped to `flowType` for back-compat (CHORE-2026-06-12-0001)
- 2026-06-12 feat(gateway): surface page[size] clamp in JSON:API metadata — `DynamicCollectionRouter.toJsonApiListResponse` now adds `metadata.requestedPageSize` and `metadata.pageSizeClamped=true` whenever the caller's `page[size]` exceeded `MAX_HTTP_PAGE_SIZE`, so an over-cap request no longer looks like a silent-empty-data response (TASK-2026-06-12-0004)
- 2026-06-12 feat(gateway): surface page[size] clamp in JSON:API metadata — `DynamicCollectionRouter.toJsonApiListResponse` now adds `metadata.requestedPageSize` and `metadata.pageSizeClamped=true` whenever the caller's `page[size]` exceeded `MAX_HTTP_PAGE_SIZE`, so an over-cap request no longer looks like a silent-empty-data response (TASK-2026-06-12-0004)
- 2026-06-12 feat(flow): surface swallowed Map iteration errors — `MapStateExecutor` now aggregates `{succeeded, failed, errors[capped@10], items}` into the Map result; sub-flow catches are recorded on `FlowExecutionContext`, propagated by `FlowEngine.executeSubFlow` via the new `SubFlowResult`, and the running `failedCount` is stamped onto the execution summary as `_failedCount`. New `MapState.FailOnPartial` (default false) flips a Map to `MapPartialFailure` when any iteration was caught, instead of silently passing through (TASK-2026-06-12-0005)
- 2026-06-12 feat(worker): expose scheduled-job status via `GET /api/flows/{id}/schedule` (cron, timezone, active, lastRunAt/lastStatus, nextRunAt) and `GET /api/flows/{id}/runs` (recent `job_execution_log` rows), plus a derived `scheduleStatus` that flags `UNSYNCED` when a SCHEDULED flow has no `scheduled_job` row or an active row with no `next_run_at` — so silently-unsynced flows (TASK-2026-06-12-0001 class of bug) no longer look healthy in the UI while never firing (TASK-2026-06-12-0003)
- 2026-06-12 feat(gateway): `?include=<field>` hydrates lookup FK fields — when the include name isn't a registered collection, `DynamicCollectionRouter.resolveIncludes` now falls back to resolving it as a field on the primary collection with a `referenceConfig`. The primary resource gets a `relationships.<field>.data = {type, id}` entry and the referenced row is pulled into `included[]`; the raw UUID stays on `attributes` for back-compat. Works for both LOOKUP-typed and legacy STRING-with-refConfig fields, with FK deduplication across rows for list responses (TASK-2026-06-12-0008)
- 2026-06-12 doc(flow): document the `{trigger,input,context}` initial state envelope and the `execute_flow` double-wrap rule (`body.input` ⇒ `state.input` ⇒ `$.input.<key>`); parallel `triggerConfig.inputData` convention for SCHEDULED flows. Tightened `ExecuteFlowTool` MCP description so callers see the wrap requirement before the misleading downstream error (DOC-2026-06-12-0001)
- 2026-06-12 feat(flow): add `InvokeFlow` state — a flow can synchronously call another flow in the same tenant by `FlowId`/`FlowName` with a templated `Input` map, merging the sub-flow result at `ResultPath`. `FlowStore` gains `findFlowDefinitionById/ByName`; `FlowEngine.executeInvokedFlow` increments a per-execution `invokeDepth` so direct/transitive recursion is bounded by `MAX_INVOKE_DEPTH=10` (`FlowDepthExceeded`). Sub-flow uncaught failures now propagate via `SubFlowResult.uncaughtError*` so the parent — and any Catch — can react. Works inside Map iterators (TASK-2026-06-12-0009)
- 2026-06-12 feat(flow): validate flow definition on save — new `FlowDefinitionValidator` (runtime-core) runs `FlowDefinitionParser` and a graph check (every `StartAt`/`Next`/`Default`/Choice target resolves to a defined state, `Map` iterators have a `StartAt`, transitions terminate). `FlowDefinitionValidationHook` (kelta-worker, order 50 — before `FlowConfigEventPublisher` and `FlowScheduleSyncHook`) calls the validator on flow create/update and rejects with a 400 listing every problem on the `definition` field, so malformed flows no longer save and only blow up at execution time (TASK-2026-06-12-0002)

## 2026-06-18

- 2026-06-18 feat(runtime): wire formula evaluation into `DefaultQueryEngine.computeVirtualFields()` — FORMULA fields now invoke `FormulaEvaluator.evaluate(expression, record)` on read (`getById`/`executeQuery`) using the `expression` key from `field_type_config`; missing/blank expression yields `null`, evaluation failures yield `"#ERROR"` so the UI always shows a live-computed value (TASK-2026-06-18-0004)
- 2026-06-18 feat(runtime): add collection-name-aware default methods to `BeforeSaveHook` — `beforeCreate(collectionName, record, tenantId)` and `beforeUpdate(collectionName, id, record, previous, tenantId)` delegate to the existing no-collection-name variants; `BeforeSaveHookRegistry.evaluateBeforeCreate/Update` now dispatch the new variants so wildcard hooks (`getCollectionName() == "*"`) can route on the source collection (TASK-2026-06-18-0002)
- 2026-06-28 chore(ui): fix remaining TypeScript type errors after `@kelta/*` module fix (TS1484/TS2322/TS2345/TS7006 etc.) — `kelta-ui/app` now type-checks and builds cleanly with `verbatimModuleSyntax`; relaxed generic constraints from `Record<string, unknown>` to `object` in `ExecutionLogModal` and the `jsonapi`/`useIncludedResources` helpers so domain interfaces (ProfileDetail, FlowExecution, EmailLog, …) flow through without needing index signatures; widened `t()` to accept either a params object or a string fallback; updated `App.test.tsx` mock to cover newly-added pages and the `isCustomDomainHost` helper (CHORE-2026-06-08-0003)
- 2026-06-28 chore(ui): fix remaining TypeScript type errors after `@kelta/*` module fix (TS1484/TS2322/TS2345/TS7006 etc.) — `kelta-ui/app` now type-checks and builds cleanly with `verbatimModuleSyntax`; relaxed generic constraints from `Record<string, unknown>` to `object` in `ExecutionLogModal` and the `jsonapi`/`useIncludedResources` helpers so domain interfaces (ProfileDetail, FlowExecution, EmailLog, …) flow through without needing index signatures; widened `t()` to accept either a params object or a string fallback; updated `App.test.tsx` mock to cover newly-added pages and the `isCustomDomainHost` helper (CHORE-2026-06-08-0003)
- 2026-06-29 feat(worker): add `FormulaComputeHook` — wildcard `BeforeSaveHook` (order 250) that pre-computes every FORMULA field on create/update; parses each expression, topologically sorts the formula DAG (Kahn's, alphabetical tie-break), evaluates dependencies first so cascading formulas resolve in one pass, merges `previous` state so partial updates see prior values, and short-circuits cycles + runtime failures to `#ERROR` (TEXT `returnType`) or `null` (NUMBER/BOOLEAN) — matching the read-path fallback in `DefaultQueryEngine.computeFormulaValue`. Wired via `FlowConfig` (TASK-2026-06-18-0003)
- 2026-06-29 feat(worker): add `FormulaRecomputeService` — `@Service` with `@Async("applicationTaskExecutor")` that pages the target collection in batches of 500 via `StorageAdapter` and re-evaluates a FORMULA field for every existing row, then issues a per-row touch UPDATE via `JdbcTemplate.batchUpdate` (FORMULA fields have no physical column today — the UPDATE sets `updated_at = updated_at` to invalidate downstream caches without rewriting audit state). `FieldConfigEventPublisher.afterCreate/afterUpdate` now diffs `fieldTypeConfig.expression` on the `fields` system collection and schedules the async recompute whenever a formula is created with an expression or its expression changes, so existing records stop evaluating against a stale formula on next read (TASK-2026-06-18-0005)

## 2026-08-31

- 2026-08-31 feat(mcp): add list_validation_rules, update_validation_rule, delete_validation_rule, list_unique_constraints, delete_unique_constraint admin tools and reword ADMIN_INSTRUCTIONS to not over-claim full counterpart coverage (TASK-2026-08-31-0001)
- 2026-08-31 feat(mcp): add delete_flow, list_flows, list_listviews, update_listview, delete_listview admin tools; ADMIN_INSTRUCTIONS now states delete_ counterparts exist for every create_ tool (TASK-2026-08-31-0002)
- 2026-08-31 feat(dispatcher): parameterize the worker per repository — optional `repo:` frontmatter field, RZWARE_REPO_* env registry with $HOME/GitHub/<name> fallback, per-task PR view, dynamic default branch from origin/HEAD, multi-repo dispatcher pull (TASK-2026-08-31-0003)
- 2026-08-31 feat(dispatcher): wire DAILY_COST_CEILING_USD gate into dispatch.sh — cost_ceiling_exceeded() sums today's cost-*.json in America/Denver; stops new claims on breach, logs once on transition (TASK-2026-08-31-0004)
- 2026-08-31 doc(dispatcher): turn multi-repo onboarding into a checklist, fix rzware-website naming (hyphen not underscore), add empty "Currently registered" list (DOC-2026-08-31-0001)
- 2026-08-31 feat(dispatcher): post Slack notifications on task failure and PR merge — lib/notify.sh exposes notify_slack, worker.sh calls it after queue_fail (exhausted retries) and queue_done (MERGED); failure is non-fatal via log_warn + || true (TASK-2026-09-01-0001)
- 2026-09-02 chore(dispatcher): TIMEOUT no longer closes PR or deletes branch — split from CHECK_FAIL|CLOSED; re-checks live merge state; releases for retry with PR intact; catches late merge on next attempt startup (CHORE-2026-09-01-0001)
- 2026-09-02 chore(dispatcher): worker.sh falls back to emf's worker-prompt.md when task repo lacks one, unblocking tasks targeting rzware-ceo and other repos not yet onboarded with their own worker-prompt (CHORE-2026-09-02-0001)
- 2026-09-13 chore(dispatcher): add etc/repos.yaml allowlist of 7 repos + queue_repo_allowed() helper in lib/queue.sh; covered by tests/repos-yaml-test.sh (CHORE-2026-09-13-0001)
- 2026-09-13 chore(planner): teach planner to assign epic/tier/touches/acceptance from ROADMAP.md; add lint-plan.sh field-level checks for epic pattern, tier range, and acceptance non-empty strings (CHORE-2026-09-13-0005)
- 2026-09-13 fix(gateway): add PAT grace cache so valid tokens authenticate during worker outages (BUG-2026-09-13-0001)
- 2026-09-13 chore(license): add SPDX AGPL-3.0-or-later package-info.java headers to kelta-gateway and runtime-core (CHORE-2026-09-13-0014)
- 2026-09-13 doc(emf): add NOTICE, rewrite CONTRIBUTING.md for external contributors, add License section to README.md (DOC-2026-09-13-0013)
- 2026-09-13 security(emf): add gitleaks pre-commit hook, CI scan workflow, and full-history baseline scan (clean, 2900 commits) (SEC-2026-09-13-0015)
- 2026-09-13 feat(dispatcher): reviewer stage + auto-merge policy dispatch in worker.sh; new lib/deploy-hooks.sh (reviewer, merge policy, deploy hook) + review-prompt.md; covered by tests/review-test.sh (TASK-2026-09-13-0016)
- 2026-09-14 feat(dispatcher): implement _hook_argocd_image_bump + run_health_check (OPERATING-MODEL.md §9/§10 item 12) — bump rewrites main-<sha> in the homelab-argo overlay + writes /srv/rzware-ceo/state/deploy-health/<name>; run_health_check sleeps 5 min, curls repos.yaml health_url, reverts + posts #rzware-ops + files a needs_clarification on non-2xx; covered by tests/health-test.sh (TASK-2026-09-14-0001)
- 2026-09-14 fix(scripts): rollback.sh now treats HEAD already matching `revert: roll back image bump*` as a benign no-op (exit 0, no git operations) instead of falling through to the manual-intervention refusal, so a duplicate rollback invocation against an already-reverted deploy no longer fails CI; added scripts/rollback.test.sh covering all three branches (PLT-154)
- 2026-09-14 fix(mcp): stop installing curl in kelta-mcp's runtime image just to fetch the OpenTelemetry agent jar; download it in the existing builder stage (curl + disk headroom already there) and COPY it into the runtime stage, eliminating the `apt-get install` step that was running out of scratch disk space on CI runners (PLT-163)
- 2026-09-14 feat(quickstart): swap docker-compose.yml's default-profile postgres/redis/nats/mailpit images from harbor.rzware.com's internal pull-through cache to their public upstreams so a stranger's `docker compose up` no longer fails on image pull; add a README Quickstart section and a CI `quickstart` job (JVM build path, 300s budget from stack start to a successful first-collection API call) plus a `stranger-test/README.md` placeholder (KLT-186)
- 2026-09-14 feat(worker): add `POST /api/admin/users/{id}/tokens` — MANAGE_USERS-gated admin-on-behalf-of PAT minting reusing PersonalAccessTokenController's mint logic, audited under a new `PAT_ADMIN_CREATED` event type; exposed in Setup › Users (mint-token action) and via `kelta users token-create` (KLT-187)
- 2026-09-14 feat(license): add root AGPL-3.0-only LICENSE + README badge/commercial-license notice per D-017 approval; existing per-file SPDX-License-Identifier headers verified against root LICENSE — found they read `AGPL-3.0-or-later` (added by CHORE-2026-09-13-0014), not `AGPL-3.0-only`, so they do not match; left unchanged per task scope (LICENSE/README only, no code changes) and flagged for a follow-up decision (KLT-189)
- 2026-09-14 fix(observability): set `management.opentelemetry.resource-attributes.service.instance.id: ${HOSTNAME:local}` in kelta-worker/gateway/auth/mcp/ai's application.yml so every replica's OTLP resource is distinct instead of merging into one Mimir series and inflating `rate()` panels on rollout (KLT-191)
- 2026-09-15 feat(ui): render picklist authored label/color (from picklist-values `label`/`color`) in badges (FieldRenderer picklist/multi_picklist), kanban lane headers, the record form select, inline editor, and the filter bar's filter-value badge — presentation-only, grouping/filtering/PATCH payloads still key on the raw stored value; new `usePicklistDisplayMap` hook (KLT-194)
- 2026-09-15 fix(ui): FieldRenderer's rich_text `stripHtml` no longer hard-caps stripped text at 200 chars, so the detail page (`truncate={false}`) shows the full value with `whitespace-pre-wrap`; the list/preview path keeps its own 100-char cut with a full-text tooltip (KLT-195)
- 2026-09-15 fix(ui): wire the picklist label/color display maps built for KLT-194 into the surfaces that were still dead code — the end-user object list table (`ObjectDataTable`), the record-detail read view + inline editor (`LayoutFieldSections`/`InlineFieldValue`/`ObjectDetailPage`), and the list page's `FilterBar`; new `usePicklistDisplayMaps` bulk hook fans out one query per picklist/multi_picklist field (KLT-194)
- 2026-09-15 fix(runtime-core): `PhysicalTableStorageAdapter#buildSelectClause` now skips fields whose `FieldType#hasPhysicalColumn()` is false, so a sparse `fields[]` naming a ROLLUP_SUMMARY or FORMULA field no longer 500s on a missing-column SQL error — post-retrieval computation in `DefaultQueryEngine` already fills the value back in on the narrowed read (KLT-199)
- 2026-09-15 fix(query): add `FilterOperator.parse` as the single operator vocabulary shared by the API filter grammar, dashboard widgets, and the MCP `query_collection` tool; `FilterCondition.fromParams` now rejects a malformed `filter…` key (e.g. `filter[status][in][]=a`) with 400 `INVALID_QUERY` instead of silently returning the unfiltered collection, `DashboardDataService.mapOperator` throws `WidgetExecutionException` naming an unknown operator instead of defaulting to EQ, and `QueryCollectionTool` accepts `IN` (CSV or array) in a single query (KLT-203)
- 2026-09-15 fix(worker,mcp): `DashboardDataService.mapOperator` special-cases `"contains"` to case-insensitive `ICONTAINS` (matching the legacy switch and `ReportExecutionService`) instead of letting it fall through to `FilterOperator.parse`'s canonical case-sensitive `CONTAINS`, preventing a silent widget-filter behavior change; sync kelta-mcp's `USER_INSTRUCTIONS` to state `IN` is supported and only `OR` needs multiple queries (KLT-203)
- 2026-09-15 fix(runtime-core): reference validation errors carry `meta.field`/`meta.value`/`meta.targetCollection` alongside `requestId` — `FieldError.reference(field, target, value)` includes the offending id in the detail message and a `referenceTargetMissing` variant covers a misconfigured target collection (no `value`); `GlobalExceptionHandler` merges `FieldError.meta()` into the JSON:API error's `meta` (KLT-204)
- 2026-09-15 fix(runtime-core): `PhysicalTableStorageAdapter` classifies `DataAccessException` by Postgres SQLSTATE (class 22 data-exception codes plus `42703`/`42883`) into a new `StorageQueryException` instead of always wrapping into `StorageException`, so a bad filter value (e.g. `filter[id][eq]=not-a-uuid`) now 400s as `INVALID_QUERY` with `meta.sqlState` and a best-effort field pointer instead of 500ing; every other SQLSTATE (connection loss, deadlock, internal syntax error) still 500s as `StorageException` (KLT-205)
- 2026-09-15 fix(packages): metadata packages round-trip whole tenants — `POST /api/packages/export` with no id lists (and `kelta metadata export` with no flags) exports the whole tenant via `PackageService.exportAllOptions`, defaulting name/version to the tenant slug and `1.0.0`; `kelta metadata apply --conflict skip|overwrite` finally sends `conflictMode`, so re-applying an edited package converges; `PackageImportService` imports menu items parents-first and remaps `parent_id` through the ids the parents got in this import (natural key now includes the parent label) instead of writing the source id verbatim; new `LAYOUT_RELATED_LIST` package type (natural key `collection:layout:relatedCollection:relationshipField`) and `PICKLIST_VALUE.color` verified across the round trip (KLT-209)
- 2026-09-15 doc(authoring): add `docs/authoring/{jsonapi,page-layouts,list-views,dashboards,ui-pages,ui-menus}.md` as the metadata-authoring reference; `kelta docs <topic>` (unknown topic exits 2 listing valid topics) and a new `cli_docs` local MCP tool serve them, freshness-pinned to the source files; kelta-mcp serves the same content as `kelta://docs/<topic>` resources on both `/mcp/user` and `/mcp/admin` (new `AdminResource` marker interface + admin resources capability), classpath copies pinned by `DocResourceTest`; completed `AGENTS.md`'s filter-operator list (was missing `in`/`isnull`/`icontains`/etc.) and pinned it against `FilterOperator`; removed the three dead FEATURES.md links from kelta-marketing's docs page (KLT-211)
- 2026-09-15 fix(cli): `mapError`/`toErrorPayload` keep the full JSON:API `errors[]` array (source, meta, and every entry) instead of truncating to `errors[0].{code,detail,meta.requestId}`; stderr envelope becomes `{"error":{code,status,detail,source,meta},"errors":[...]}`, table-mode error output lists every entry too; `kelta api`'s non-GET-is-dangerous behavior documented in the manifest's `errorEnvelope` string and `AGENTS.md` (KLT-216)
- 2026-09-15 feat(views): shared `list-views` rows publish their renderer — `V196__list_view_view_type.sql` adds `view_type` (NOT NULL DEFAULT `TABLE`, CHECK `TABLE|KANBAN|CALENDAR|GALLERY`) + `type_config` JSONB, declared in `SystemCollectionDefinitions.listViews()`, so an admin can hand out a kanban board instead of a column set every user re-configures; `mapSharedListView`/`parseTypeConfig` map the row into `SavedView` and fall back to the table renderer for an unrecognized `viewType` or a half-formed config, a user's toolbar switch persists as a per-user `user-ui-preferences` override (`useViewTypeOverrides`, `prefType: 'list-view-type'`) that never touches the shared row, and Setup › List Views, `kelta list-views create|update --view-type/--lane-field/--card-fields` and the MCP `create_listview`/`update_listview` `viewType`/`typeConfig` params all write the same row (KLT-196)
- 2026-09-15 feat(nav): `ui-menu-items.path` keeps its query string for `/resources/<collection>?...` items — `parseResourcePath` (`navTabs.ts`) splits the collection name from the query, `NavTab.query` carries it through, and `TopNavBar`'s `tabPath` (desktop + mobile sheet) appends it to the `…/o/<collection>` href so a menu item can deep-link into a shared view (`?view=shared:<id>&pageSize=100`); `AppHomePage` and `GlobalSearch`'s collection collectors route through the same helper so they don't mistake the query string for part of the collection name (KLT-197)
- 2026-09-15 feat(analytics): dashboard widgets can opt out of the page's time range — `DashboardDataService.buildTimeFilters` honors `config.ignoreTimeRange` (no time filter at all) and `config.fixedTimeRange` (TODAY|7D|30D|90D|1Y regardless of the page's selected range), precedence `ignoreTimeRange` > `fixedTimeRange` > runtime `timeRange` > `config.timeRange`; `DashboardViewPage`'s `WidgetFrame` shows an "All time"/fixed-range chip on any opted-out widget (KLT-198)
- 2026-09-15 feat(analytics): `DashboardDataService.executeChartWidget` resolves a LOOKUP/MASTER_DETAIL `groupByField`'s raw ids to the target collection's display value in one batched `id IN (...)` query (capped at `maxGroups`), putting the resolved name in `point.label` and the raw id in `point.key`; falls back to the id when the target's display field is masking-denied for the viewer; non-reference `groupByField`s are unchanged (no `key`). `DashboardViewPage`'s `ChartWidget` drills through on `point.key ?? point.label` so cached payloads without `key` keep working (KLT-200)
- 2026-09-15 fix(layouts): `layout-fields.columnNumber` defaults to `0` instead of `1` (`V197__layout_field_column_zero.sql`, `SystemCollectionDefinitions.layoutFields()`) to match `LayoutFieldSections.tsx`'s 0-based column indexing, and `CreateLayoutTool`'s modulo fallback for an omitted `columnNumber` drops its `+1` so a two-column section without explicit placements lands fields at `0,1,0,1` instead of `1,2,1,2` (KLT-201)
- 2026-09-15 fix(security): scope `GET /api/fields` to the caller's tenant — `fields` is now `tenantScoped(true)` with a denormalised `field.tenant_id` (V198, backfilled from `collection.tenant_id`, RLS enabled) instead of the router adding no predicate, closing a cross-tenant field-metadata enumeration gap; `SystemCollectionSeeder`/`MigrationFieldRepository`/`ExternalEntityMaterializer`/`ModuleCollectionProvisioner` updated to stamp the new NOT NULL column; get-by-id on tenant-scoped system collections now 404s another tenant's row router-side (`visibleToTenant`) so the answer holds where RLS is a no-op (KLT-206)
- 2026-09-15 feat(api): `GET /api/collections/{name}/schema` answers for system collections as well as tenant ones (`CollectionSchemaController`, off `CollectionRegistry` with the router's on-demand load; `{name, displayName, systemCollection, fields:[{name, type, required, default, enum, reference:{target}, isRelationship, description}]}`, 404 on an unknown name) — so a client authoring `page-layouts`/`layout-fields`/`list-views`/`dashboard-components` no longer has to read `SystemCollectionDefinitions` source. `FieldDefinition` gains a `description` component (canonical constructor is now 13-arg; the previous 12-arg form delegates, so existing call sites are untouched), every one of the 889 system fields declares one (`SystemCollectionDescriptionsTest` fails on a new field without it), `layoutType`/`visibility`/`sortDirection`/`componentType`/`accessLevel`/`reportType` declare their allowed values via `withEnumValues`, and `CollectionLifecycleManager` carries `field.description` through for tenant collections. `OpenApiGenerator` now documents system collections, `filter[{field}][{op}]` (operator list read off `FilterOperator.values()`) and `fields[{type}]`, emits response/list schemas carrying enum/default/description/required with relationship fields under `relationships`, and documents field validation as **400** (422 is left only on the custom formula-rule path that actually returns it) (KLT-207)
- 2026-09-15 fix(api): `DynamicCollectionRouter#toJsonApiResourceObject` now echoes a LOOKUP/MASTER_DETAIL field's raw id into `attributes.<field>` as well as `relationships.<field>.data.id`, in list/single/`included[]` reads, so a client that PATCHes back a read-back `attributes` block no longer sees the field vanish; `CerbosFieldSecurityAdvice`'s existing unconditional attributes-strip already covers the new attribute (no code change needed there, added regression coverage), and `flatten.ts#flattenResource` already skips a relationship name once seen in `attributes`, keeping `kelta <collection> list` output unchanged (KLT-210)
- 2026-09-15 fix(mcp): `get_collection_schema` and the `kelta://collections/{name}` resource now call `GET /api/collections/{name}/schema` once instead of `/api/collections/{name}` + a broken `filter[collectionName]` fields lookup that the worker rejected with 400 (so `fields` was always `null` on the resource); the tool also embeds `validationRules` (resolving the collection's id, then `GET /api/validation-rules?filter[collectionId][EQ]=...`) unless `includeRules:false`. `describe_api`'s description now says what the OpenAPI spec covers and points at `get_collection_schema` for per-collection detail; `get_collection_schema` and `describe_api` descriptions each carry a worked example (KLT-208)
- 2026-09-15 fix(ci): `verify_image_exists()` in the `deploy` job's "Update image tags via kustomize" step now retries the Harbor manifest check up to 5 times with a 5s backoff instead of failing on the first non-200, since a successful `docker/build-push-action` push is not immediately queryable via the manifests API (registry read-after-write race); exhausted retries still hit the existing `::error::Refusing to bump ...` failure path (PLT-230)
- 2026-09-15 feat(dashboards): new `DashboardComponentValidator` (shared by `DashboardComponentConfigHook` on write and the new `POST /api/dashboards/{id}/validate` dry-run endpoint) rejects an unknown `config.collectionName`, a field name that doesn't exist on the target collection, a ROLLUP_SUMMARY/FORMULA field used as `groupByField`/`aggregateField`, a non-DATE/DATETIME `timeField`, an unknown filter operator, an unknown `componentType`, and a `columnPosition` outside `1..dashboard.columnCount` — each as a 400 with a `/data/attributes/...` pointer instead of only surfacing at render time; `DashboardDataService.executeWidget` now classifies a runtime `InvalidQueryException` naming a field the collection no longer has into `"Unknown field 'x' on collection 'y'"` instead of the generic "Internal error executing widget"; `dashboard_component.report_id` moves from MASTER_DETAIL to LOOKUP (nullable, `ON DELETE SET NULL`, `V199__dashboard_component_report_id_optional.sql`) so a collection-targeting widget no longer needs a dummy report row (KLT-213)
- 2026-09-15 feat(layouts): new `PageLayoutTreeController`/`PageLayoutTreeService` (kelta-worker) apply a whole page layout in one idempotent call — `GET`/`PUT /api/page-layouts/{id}/tree` and `PUT /api/collections/{name}/layouts/{layoutName}/tree` (creates the layout by name when absent) take `{name, layoutType, isDefault, description, headerConfig, sections:[{heading, columns, collapsed, fields:[{name, column, label, helpText, readOnly, required}]}], relatedLists:[…]}` with fields addressed by **name**, upsert sections on `heading` / placements on field name / related lists on (collection, relationship field), delete what the body drops (placements before their cascading section) and return the diff `{created, updated, deleted, unchanged}`, so a second identical PUT writes nothing and GET round-trips through PUT with no diff. Validation is all-or-nothing and pointer-bearing (`/sections/0/fields/2/name`): unknown field, `column` outside the section's 0-based `columns`, a field placed twice, a missing/duplicate heading, a related list whose relationship field isn't a lookup back to the layout's collection, any unknown property, and an echoed `layoutId`/`collection` naming a layout other than the one addressed (the echo keys exist only so a GET body round-trips unedited, so a mismatch is a 400 rather than one layout's tree silently overwriting another's). Writes ride `QueryEngine` (the `PackageImportService.upsertViaEngine` pattern) so `PageLayoutConfigEventPublisher`/`Layout{Section,Field,RelatedList}RefreshHook` and the `kelta.config.layout.changed.<layoutId>` broadcast fire with no listener change; gated in-controller on `CUSTOMIZE_APPLICATION` since `/api/page-layouts/**` and `/api/collections/**` are existing static gateway routes carrying only `API_ACCESS` (no `RouteConfigService` change needed) (KLT-214)
- 2026-09-15 feat(ui): surface `field.description` as end-user help text — `useCollectionSchema`'s `FieldDefinition` now types the already-spread `description`; `LayoutFormSections` corrects its dead `schemaField.helpText` fallback to `schemaField.description` (layout `helpTextOverride` still wins) so create/edit forms show it under the input; `LayoutFieldSections` shows a help-icon tooltip trigger beside the field label on detail pages via a new optional `FieldSection.renderLabel` slot (`@kelta/components`), rendering nothing when both the override and description are empty (KLT-202)
- 2026-09-15 feat(list-views): `ListViewConfigHook` validates `list-views` writes — `columns`/`sortField`/filter fields must exist on the target collection (`/data/attributes/columns/<i>` pointer), filter operators parse via `FilterOperator.parse` and are canonicalized (`equals` → `eq`), `rowLimit` must be one of `{10,25,50,100}`, and a second `isDefault` for the same collection+visibility is rejected as `409 DEFAULT_VIEW_EXISTS` (`meta.existingId`) via the new generic `HookConflictException`/`GlobalExceptionHandler` mapping; `create_listview`/`update_listview` MCP tools forward `rowLimit` (KLT-212)
